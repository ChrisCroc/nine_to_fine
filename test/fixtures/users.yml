# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# Fixtures intentionally empty. User has unique constraints via Devise
# (email, username) — generated empty fixtures `one: {}` / `two: {}` caused
# UniqueViolation on insertion. Add real fixtures here only when a test
# actually needs seeded users.
