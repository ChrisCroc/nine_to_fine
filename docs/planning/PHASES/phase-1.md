# Phase 1 — Rails pro + Tailwind (sem 22-25, 25 mai → 21 juin)

**Focus apprentissage** : Tailwind CSS (Tailwind Labs YouTube + docs officielles).

**Odin Rails** : Active Record avancé (callbacks, scopes, query optim), Forms avancées, Authentication "from scratch" (comprendre ce que Devise fait sous le capot).

**Cadence Nine to Fine** : pleine production l'après-midi (~5 j/sem).

## Tableau récap

| Sem | Dates | Objectif | Livrables |
|---|---|---|---|
| 22 | 25-31 mai | Scaffolding des **models structurels** Nine to Fine + Tailwind setup | Models Category, Garment, Outfit, OutfitGarment, Tag, Tagging (les 6 nécessaires pour le CRUD sem 22-24). Tailwind opérationnel. 1ère page stylée. CRUD Garment + Tailwind v4 sur les 5 vues. |
| 23 | 1-7 juin | CRUD Outfit + composition de tenues + Active Storage | OutfitGarment join (form `fields_for` + `accepts_nested_attributes_for`), page de composition d'outfit, upload photo (Active Storage) sur Garment, premier polish UI sur la page de show outfit. |
| 24 | 8-14 juin | **Tags + recherche light** + **suite RSpec rétroactive sur `GarmentsController`** (dette de sem 22) | Tags via input texte comma-separated (parse côté model + `find_or_create_by`), recherche par nom (1 input) + filtres `link_to` (couleur OU category OU tag, un seul à la fois). Premiers tests RSpec (Outfit + rétroactifs Garment + tagging). Setup gem `bullet` (development). |
| 25 | 15-21 juin | Likes + Comments + Follows + profil public + **LANCEMENT RECHERCHE EMPLOI** + skill `job-tracker` | Models sociaux générés (Like, Comment, Follow polymorphic). UI Likes / Comments / Follow fonctionnelles. Profil user public (`UsersController#show`). **Skill Claude Code `job-tracker` créée samedi 20-21 juin** (~1-2 j atelier). **LinkedIn + GitHub à jour. 3 premières candidatures Rails.** |

## Mini-bullets jour-type

### Sem 22 (en cours)

Voir tableau détaillé jour par jour ci-dessous.

### Sem 23 (1-7 juin)

> **Re-sync 3 juin** : ~1 jour d'avance pris (composition tirée à lundi, Active Storage à mardi, polish UI Outfit à mercredi). Bullets ci-dessous alignés sur le réel ; détail jour par jour dans le Suivi.

- Lundi : CRUD Outfit (routes + controller + 5 vues + `_form`) + composition via `garment_ids=` + combobox Tom Select. ✅
- Mardi : Active Storage sur Garment (upload + variants WebP + validation type/taille) + tests Minitest. ✅
- Mercredi : design des vues Outfit — collage adaptatif (mosaïque plafonnée + eager loading anti-N+1), PR #69. ✅
- Jeudi : tags comma-separated (concern `Taggable` + `tag_names` virtual attribute + `after_save sync` + pills partial) + tests Minitest + gem `bullet` (2 N+1 préexistants corrigés). PR #74. ✅
- Vendredi : **non fait** — journée buffer décrochée (fatigue post grosse semaine prod). 2 soirs exos sandbox manqués + reco vidéos non exécutées. ❌
- Samedi : **non fait** — 4-5h rattrapage prévues, décrochage maintenu. ❌

### Sem 24 (8-14 juin) — en cours

> **Note** : tags comma-separated **déjà livrés sem 23 jeudi** (PR #74). Lundi-mardi recentré sur filtres `link_to` + tags cliquables.

- Lundi : Odin Turbo + Tailwind Typography (lecture profonde, le matin). Aprem CLI : **3 PR livrées** — back (PR #79 `GarmentFilter` pipeline `inject` + dispatch `send`, 4 filtres AND combinables + brand proactif), tests (PR #81, 10 cas Minitest incl. test propriété sécurité cross-user), front complet (PR #83 `Garment::COLORS` + validation `inclusion` + normalisation données dev + helper `pill_class` + index `turbo_frame_tag "garments_list"` + pattern `_top` + tags cliquables `shared/_tags`). Design system typo (Playfair+Inter) reporté à mardi (déjà au programme polish navbar/footer).
- Mardi : Odin Stimulus (matin) → **recherche par nom** (1 input + scope simple `where("name ILIKE ?", "%#{q}%")`) + **polish navbar + footer** avec design system typo.
- Mercredi : Tailwind Transitions & Animation (avant-midi) → **first-loaded RSpec setup** (gem + spec_helper + factory_bot ou fixtures) + 1 spec sanity.
- Jeudi : Odin Mailers (matin) → suite **tests RSpec `GarmentsController` rétroactifs** (7 actions + IDOR).
- Vendredi : **suite RSpec controllers + tagging tests + audit `bullet`** (corriger N+1 détectés). PM : lancement recherche emploi reporté à sem 25 (cf. PLANNING).

### Sem 25 (15-21 juin) — ⭐ plan révisé vendredi 12 juin (9 jours d'avance acquis)

⭐ **Phase 1 techniquement bouclée vendredi 12 juin** (Odin Rails chap. 1-41 ✅ + Tailwind v4 étapes 1-14 ✅ + sem 22-24 livrables ✅). Les 9 jours d'avance accumulés sont **dispersés en sessions consolidation** dans sem 25 (vs avancer Phase 2 React de 1 semaine = rejeté pour éviter de construire sur du flou).

⭐ **2 chapitres flous identifiés par Chris** méritant cours dédié pratique-first :
- **Metaprogramming Ruby/Rails** ([[metaprogramming-ruby-and-rails-first-pass]]) — note "1er passage à approfondir" jeudi 11 juin, cours Priorité 1 dans `to-do.md`
- **Stimulus avancé** — 4 controllers en prod mais TOUS sur pattern minimaliste, jamais pratiqué Outlets / `dispatch()` / `static classes` / TargetConnected / state machine via Values

⭐ **Turbo Streams = consolidation PAR LA PRATIQUE** sem 25 — au lieu de coder Likes/Comments en HTML simple puis upgrade, code-les **directement en broadcast Turbo Streams moderne** (pattern de [[action-cable-fundamentals]] : `after_create_commit { broadcast_append_to outfit }` + `turbo_stream_from @outfit`). Pratique réelle = consolidation.

- **Lundi 15** : Likes model polymorphic + ⭐ **UI Likes broadcast Turbo Streams** sur Outfit show (pattern moderne `after_create_commit` + `turbo_stream_from`, pas HTML simple). Soir : exo sandbox Turbo Streams 8 actions dans `~/code/sandbox/rails/`.
- **Mardi 16** : Comments model polymorphic + ⭐ **UI Comments broadcast Turbo Streams** sur Outfit show (même pattern). Soir : exo sandbox Turbo Stream templates `.turbo_stream.erb`.
- ⭐ **Mercredi 17 (journée consolidation hybride)** :
  - Matin (2h30) : **Cours Stimulus avancé** + 3 exos sandbox dans `ChrisCroc/sandbox-stimulus` = (1) Outlets entre 2 controllers ; (2) `dispatch()` pub/sub notif globale ; (3) state machine via `XValueChanged` callback réactif. Note Obsidian rédigée [[stimulus-advanced-patterns]] (à créer).
  - Avant-midi (2h30) : **Cours Metaprog Priorité 1** + 3 exos sandbox dans `~/code/sandbox/rails/metaprogramming/` = (1) clone `attr_accessor` maison ; (2) Builder DSL via `method_missing` + `respond_to_missing?` ; (3) refactor `GarmentFilter` avec `define_method` (Q6 reportée chap 40). Note Obsidian rédigée [[metaprogramming-deep-dive]] (à créer, remplace 1er passage).
  - Aprem (2h30) : Likes broadcast Turbo Streams sur N to F (vraie pratique = consolidation Turbo Streams par production réelle).
- **Jeudi 18** : Follow model polymorphic + scoping `current_user.followed_users` + profil user public (`UsersController#show` + listing garments/outfits/follows). Soir : exo sandbox Active Record Queries avancées (scopes nommés + subqueries + `merge`).
- **Vendredi 19** : polish profil + ⭐ **lancement recherche emploi** = 3 premières candidatures Rails + LinkedIn polish + GitHub README pinned `nine-to-fine` updaté. Soir : exo sandbox APIs Phase 4 prep (`anthropic-sdk-ruby` warmup ou mini service object `WeatherApi`).
- **Samedi 20-21 juin** : création skill Claude Code `job-tracker` (~1-2 j, atelier Claude). Slash commands `/candidatures add|list|relance|stats`. Stockage markdown ou JSON.

**Bilan sem 25 attendu** :
- 5 exos sandbox dirigés (1 par soir lun-ven) — routine soir réamorcée après 4 soirs cumulés sem 24
- Stimulus avancé consolidé (cours + 3 exos = 5h cumulées)
- Metaprog consolidé (cours + 3 exos = 5h cumulées)
- Turbo Streams pratiqué en production réelle (Likes + Comments broadcasts moderne)
- Programme social complet (Likes + Comments + Follow + profil)
- Recherche emploi lancée (3 candidatures + LinkedIn)
- Skill `job-tracker` créée

**Phase 2 React démarre sem 26 (22 juin) inchangée** — ne pas décaler la timeline emploi septembre.

## Détail jour par jour — Sem 22 (en cours)

| Jour     | Date       | Programme                                                                                                                                                                                                                                                                                                                                                      | Statut                                                                                                                                                                 |
| -------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Lundi    | 25 mai     | Conception complète (user stories, schéma DB, kanban GitHub, roadmap) + install et config Devise (username, email, password)                                                                                                                                                                                                                                   | ✅                                                                                                                                                                      |
| Mardi    | 26 mai     | Model User + ApplicationController (authenticate_user! + permitted_parameters) + PagesController (home publique) + navbar + flashes + devise views                                                                                                                                                                                                             | ✅                                                                                                                                                                      |
| Mercredi | 27 mai     | Setup environnement de travail : `~/.claude/CLAUDE.md` global + `CLAUDE.md` projet + `PLANNING.md` 18 sem + fix CI (PR #29) + upgrade image_processing 2.0.1 (PR #30) + vault Obsidian `DevJobReady`                                                                                                                                                           | ✅                                                                                                                                                                      |
| Jeudi    | 28 mai     | Générer les **6 models structurels** dans l'ordre : Category → Garment → Outfit → OutfitGarment → Tag → Tagging (`rails g model X`) + migrations + `db:migrate` + vérifier associations en console + **tests Minitest pour chaque model** (validations, associations, cas d'erreur) + **fixtures `test/fixtures/x.yml`**. **Like/Comment/Follow reportés à sem 25** (générés au moment de leur implémentation). **AiSuggestion reporté à Phase 4 sem 36-37** (intégration agent IA). RSpec maintenu à sem 24. | ✅ Exos sandbox du soir (polymorphic + piège `default: 0`/`validates presence`) **reportés samedi 30 mai**. |
| Vendredi | 29 mai     | CRUD Garment (controller + vues new/show/index/edit) + tester le flow en local + premier styling Tailwind sur la page Garment                                                                                                                                                                                                                                  | ✅ CRUD Garment livré (PR #42 mergée). Styling Tailwind v4 des 5 vues livré fin d'aprem (PR #45, branche `feature/garment-crud-tailwind`). **Dette technique notée : 0 test controller écrit, reportés à sem 24 avec l'introduction RSpec.** |
| Samedi   | 30 mai     | **Refonte stratégique Phases 2-3-4 + restructuration documentaire** (priorité matin, voir Suivi). Rattrapage Odin courts (Asset Pipeline + Importmaps + Turbo Drive). Exos sandbox reportés (polymorphic + piège default). Tailwind layouts + navbar responsive (si temps). Routine `/end-of-day` (skill Claude Code). Ménage dotfiles (coupable). | ✅ Refonte stratégique livrée (PR #49 mergée). Règle péda 11 codifiée (PR #50). Rattrapage Odin 3 chap. + 3 notes Obsidian. Brainstorm `/end-of-day` **interrompu** (à reprendre, voir Suivi Blocages). **Tous les autres chantiers (exos sandbox, Tailwind layouts, dotfiles) reportés à dimanche journée d'exos.** |
| Dimanche | 31 mai     | **Journée d'exos** (élargissement de la dérogation initiale "3 exos courts"). 5 exos prévus : polymorphic Rails sandbox (P1), piège `default: 0` + `validates presence`, `_field` partial DRY, Tailwind grid 60/40 sandbox, vidéo Tailwind layouts si temps. | 🟡 À faire (dérogation Dimanche OFF #1/3 sur 6 sem glissantes — voir Suivi) |

**Tableaux jour-par-jour sem 23, 25** : à compléter au démarrage de chaque semaine (lundi).

## Détail jour par jour — Sem 24 (en cours)

| Jour     | Date       | Programme                                                                                                                                                                                                                                                                                                                                          | Statut                                                                                                                                                                                                                                                                          |
| -------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Lundi    | 8 juin     | Matin Odin chap. 36 Turbo (Frames + Streams + format negotiation, lecture profonde). Avant-midi Tailwind Typography (font-family + size + weight + leading + tracking + decoration). Aprem CLI : filtres `link_to` (couleur + category + tag cliquables) sur index garments via Turbo Frame + application design system typo (Playfair + Inter). | 🟢 Matin + avant-midi ✅ (notes [[turbo-frames-streams-and-format-negotiation]] + [[typography-fundamentals]]). Aprem **3 PR livrées** : (1) `GarmentFilter` Query Object back (pipeline `inject` + dispatch `send`) + controller + 4e filtre `brand` proactif + smoke test 5 cas SQL ✅ (PR #79, note [[query-objects-pipeline-inject-and-dynamic-dispatch]]) ; (2) suite Minitest 10 cas incl. ⭐ test propriété sécurité cross-user (PR #81, note [[minitest-garment-filter-query-object]]) ; (3) **front complet** : `Garment::COLORS` + validation `inclusion` + normalisation données dev (12 valeurs FR/composées) + controller exposing `@filter_params` + helper `pill_class` + index refondue avec `turbo_frame_tag "garments_list"` + propagation `merge`/`except` + `with_indifferent_access` + cast `.to_i` + pattern `_top` pour échapper au frame + tags cliquables `shared/_tags` (PR #83, notes [[turbo-frames-in-production-and-content-missing-trap]] + [[inclusion-validation-with-allow-blank]]). **Reportés mardi** : design system typo (déjà au programme polish navbar/footer), exo dirigé soir, reco 2 vidéos fin de journée. |
| Mardi    | 9 juin     | Matin Odin chap. 37 Stimulus (lecture profonde, chap. dense). Aprem CLI : recherche par nom (`ILIKE`) + polish navbar/footer avec design system typo.                                                                                                                                                                                                | 🟢 Matin + avant-midi ✅ (cf. statut détaillé ci-dessus, non répété). **Aprem partie 1 ✅ (recherche live, PR #83 mergée le matin)** : empile back + front + Turbo Frame. Back = 5e tamis `GarmentFilter#by_search` avec `ILIKE` + `sanitize_sql_like`. Front = controller Stimulus `search_form_controller` avec debounce 300ms. ⭐ form HORS de la frame pour préserver focus pendant debounce. Notes [[garment-filter-search-extension-ilike-sanitize]] + [[stimulus-debounce-and-turbo-frame-focus-preservation]]. **Aprem partie 2 ✅ (brainstorm UX 4 sub-projects + livraison 2 sur 4, PR #91 mergée)** : sub-project #1 ajout catégories Dress/Suit/Swimwear via seeds ✅, sub-project #2 color selector Tom Select avec pastilles colorées ✅ (constante `Garment::COLOR_HEX` + test invariant Minitest + Stimulus controller `color_select_controller.js` + `f.select` 4 args + style centralisé `.ts-control`). 2 pièges majeurs vécus : `#` manquant dans hex CSS → pastilles grises ; classes Tailwind sur `<select>` source → double encadré. ⭐ bridge data-attributes → JS data object Tom Select strippe `data-` (vu en live console.log). Spec + plan rédigés via skill `writing-plans`. Note [[tom-select-render-customization-color-selector]] très détaillée (4 Q/R entretien + pipeline complet 10 étapes). **Sub-projects #3 (photo preview new) + #4 (tag autocomplete) reportés** (option A choisie dans brainstorming). **Backlog `FEATURES_FUTURES.md` enrichi** : `pg_search` (contexte aujourd'hui) + nouvelle entrée "Sidebar filtres togglable". **Lint CI rattrapé** : Rubocop `Layout/SpaceInsideHashLiteralBraces` sur `db/seeds.rb` (Swimwear hash) via autocorrect. **Reports VERS MERCREDI 10 juin** : (a) tests Minitest `by_search` (~15-20 min), (b) polish navbar/footer typo Playfair+Inter (~1h-1h30), (c) **cleanup SUIVI hebdo** (condenser sem 23 vers Obsidian + codifier rolling lundi, cible 54 k → ~25 k chars). **Reports VERS SAMEDI 13 juin** : exo dirigé soir mardi (Stimulus debounce sandbox, spec succincte dans SUIVI sem 24 mardi) — cumulé avec lundi 8 juin soir. **Reco 2 vidéos** ✅ sélectionnées par CLI (Auto-submitting Forms 27 mars 2023 + Stimulus 2.0 Values 7 déc 2020, les 2 GoRails Pro) et visionnées en autonomie le soir, notes Obsidian rédigées demain matin en rétroactif. **Méta** : 2 PRs Dependabot mergées (PR #85 + #86) intercalaires + 5 fautes méta Claude actées (3 relectures + 2 vulgarisation insuffisante sur Tom Select/Stimulus). |
| Mercredi | 10 juin    | Matin Odin chap. 38 Mailers OU continuation Stimulus si trop dense. Avant-midi Tailwind **Transforms** (translate, rotate, scale, skew, perspective — la suite de Transitions/Animation du 9 juin, cf. PROGRESSION.md). Aprem CLI : **(reports mardi)** tests Minitest `by_search` (~15-20 min, 3 cas dans `garment_filter_test.rb` : match partiel ILIKE, combo avec color, échappement du `%`) + **polish navbar/footer typo design system Playfair+Inter** (~1h-1h30, déjà acté lundi avant-midi à appliquer en CLI) PUIS si temps RSpec setup (gem + spec_helper + factory_bot) + 1 spec sanity (sinon RSpec décalé jeudi).                                                                                                                                                          | 🟢 Matin + avant-midi ✅. **Démarrage de session (méta)** : refonte stratégique cleanup SUIVI + architecture `@import` (3 itérations brainstorming) — sem 23 archivée verbatim dans `ARCHIVE/2026-06.md` créé + condensée dans SUIVI-COURANT (57 k → 41 k chars), routine condensation hebdo codifiée CLAUDE.md étape 7 (cible ≤ 8 k chars/sem condensée), SUIVI retiré du `@import` auto + étape 0 obligatoire Read SUIVI codifiée. Bilan tokens `@imports` : 22 k → ~12 k (−45 %). **Matin Odin chap. 38 Mailers ✅** + Rails Guides 8.1 (sections 1-2/6-7/8.2/9-10). Couvert : pattern moderne `Mailer.with(params).action.deliver_later` + sérialisation GlobalID Active Job/Solid Queue Rails 8 vs ancien `Mailer.action(arg)` ; **2 familles de callbacks** — `*_action` (variable `mail`, avant/après construction) vs `*_deliver` (variable `message`, avant/après envoi SMTP) avec 6 use cases canoniques (hydrater @vars, override SMTP runtime multi-tenant, killswitch `perform_deliveries = false`, headers custom, sandbox staging, audit log, `.touch(:delivered_at)`) ; **vues `app/views/<mailer>/<action>.html.erb` + `.text.erb`** vs layout `mailer.html.erb` (multipart MIME auto) ; `attachments.inline[..]` vs `attachments[..]` + cas N to F Active Storage WebP (`.processed` mandatory) ; `*_url` mandatory + `default_url_options host:` ; 2 contraintes styling HTML emails (pas stylesheet externe, support CSS limité Outlook → tables HTML) ; `.touch(:column)` AR direct SQL skip validations (lien rétro [[callbacks]] chap. 27) ; 3 mécaniques d'abort (`perform_deliveries = false`, `throw :abort` before_deliver, `body = ...` before_action) ; Interceptors/Observers globaux vs callbacks locaux ; Mailer Preview vs Letter Opener duo. ⭐ **Lien rétro chap. 27** : combo `after_save` + `deliver_later` + transaction rollback = job orphelin + `RecordNotFound` worker crash → toujours `after_commit on: :create`. Knowledge Check Odin (6) répondues par écrit par Chris : 3 correctes / 2 confusions importantes / 1 faute structurelle / 1 style obsolète / 1 zappée. Toutes corrigées en chat. ⭐ **3 questions style entretien 90 sec rédigées par Claude** : (1) `deliver_later` + `after_save` vs `after_commit` rollback, (2) Active Storage inline mail récap N to F, (3) Mailer Preview vs Letter Opener duo complémentaire. **Décision pédago tranchée** : pas de config Action Mailer dans Nine to Fine ce matin (4 raisons : viole ritual matin, YAGNI, premature config, risque oubli contexte). Setup repoussé sem 25 (1er mailer follow notif). Notes Obsidian : [[mailers-fundamentals]] (17 sections, 12 pièges, 6 KC) + [[../../../Entretiens/Rails/mailers]] (Q1/Q2/Q3 format Q/Réponse/Pièges/Follow-up) + index Rails MAJ. **Avant-midi Tailwind Transforms ✅** (6 pages doc v4 fetchées en parallèle : translate + rotate + scale + skew + transform-origin + intro 3D context perspective/transform-3d). Couvert : 4 opérations 2D + pivot + 3D. Sémantiques différentes pour le même N (`translate` = spacing scale, `rotate` = degrés, `scale` = pourcentage). Translate axes XYZ + fractions = % de l'élément (centrage absolu `left-1/2 -translate-x-1/2`). Scale presets 0-200 + **`-scale-x-100` = flip horizontal idiome icônes** + pas de layout shift (avantage perf + piège clipping/overlap). ⭐ **LE mécanisme clé v4 — composition via CSS variables** : chaque utility écrit dans SA variable (`--tw-translate-x`, `--tw-rotate-z`, etc.), règle racine compose le `transform` final → on empile N transforms sans écrasement (en CSS pur, `transform: rotate()` puis `scale()` s'écraseraient). 3D context : 3 ingrédients (perspective parent + transform-3d parent + transform enfant). **Performance** : transform + opacity = composite GPU 60 fps ; width/height/margin/padding = layout reflow saccades. JAMAIS animer w/h pour entrées d'éléments. **Application N to F** : 5 patterns (card lift index, pills filtre lift, like button press, flip icône expand, modal entrance scale+opacity). ⭐ **3 questions style entretien 90 sec rédigées par Claude** : (1) hover scale card + clipping/overlap + `relative + hover:z-10`, (2) combiner rotate + scale via CSS variables vs CSS pur, (3) modal entrance + pourquoi PAS w/h (rendering pipeline GPU vs layout). 12 pièges gravés. Note Obsidian : [[transforms-fundamentals]] (12 sections + Q1/Q2/Q3 intégrées + liens rétroactifs [[transitions-and-animations-fundamentals]] + [[backgrounds-borders-effects-v4]]) + index Tailwind MAJ (étape 12 ✅). **Aprem CLI ✅** : 4 chantiers livrés (PR #95 mergée = 3 tests Minitest by_search soldant dette PR #83 + design system typo Playfair+Inter v1 desktop-first → ⚠️ piège mobile-first flaggé en live, PR #96 RSpec setup + factory_bot + 5 factories + migration `garment_filter_test.rb` → `_spec.rb` 13 examples verts + CI workflow adapté `bin/rails test && bin/rspec` pendant transition, PR #97 routine end-of-day + 3 notes Obsidian, PR #98 mobile-first values dans `@theme` h1=32px h2=20px caption=14px). **Footer abandonné** (usage moderne apps mobile-first sans footer global, à créer dans `pages/home.html.erb` only si mentions légales B2C plus tard). 11 fichiers Minitest restants à migrer en autonomie jeudi matin (estimation ~1h45-2h30, pattern documenté). 3 notes Obsidian rédigées : [[rspec-and-factory-bot-setup]] + [[minitest-to-rspec-migration-patterns]] + [[v4-design-system-and-mobile-first-pitfall]] + index Rails Testing + Tailwind Typography MAJ. Memory globale `project-nine-to-fine-mobile-first` créée. Détail complet dans SUIVI-COURANT. **Méta pattern** : 3× sur-optimisation Claude sur métrique tokens corrigée par Chris recadrage objectif réel (Claude contexte coaching + Chris notes complètes + zéro bruit). Reconnaissance d'erreur explicite + révisions honnêtes (option F → B+C → finale "retirer @import + étape 0"). |
| Jeudi    | 11 juin    | Matin Odin chap. **40 Advanced Topics** lecture profonde (6 sections : Advanced Routing + Controllers sans model & REST + Advanced Layouts + Metaprogramming + Design Patterns SOLID + i18n). Avant-midi Tailwind **Interactivity** (19 utilities). Aprem CLI : migration 11 fichiers Minitest restants en RSpec + démarrage RSpec `GarmentsController` rétroactif (7 actions + IDOR, dette technique sem 22).                                                                                                                                                                                                                                              | 🟢 Matin ✅ + Avant-midi ✅. **Matin Odin chap. 40 Advanced Topics ✅** — lecture profonde **6 sections** couvertes avec décisions clés tranchées + 6 notes Obsidian (5 solides + 1 first-pass) + 1 nouveau dossier vault `Apprentissage/Rails/Routing/` créé. **Section 1 Advanced Routing** ([[advanced-routing-fundamentals]]) : singular `resource :profile` + nested 1 niveau max + member/collection règle discriminante "besoin d'`:id` ?" + redirects/wildcards. 4 Q répondues incl. correction confusion `?locale=likes` member vs collection. **Section 2 Controllers sans model + REST** ([[controllers-without-models-and-rest-principles]]) : mythe 1-controller-1-model cassé (PagesController exemple N to F déjà existant), namespace `Admin::`/`Api::V1::` + héritage BaseController DRY, pattern "verbe → nom" Skinny Controllers. **Q4 répondue** : `SuggestionsController` Phase 4 IA (B ✅) → entrée FEATURES_FUTURES.md ajoutée (transition action → futur model `Suggestion` avec 5 besoins post-V1 : tracking, feedback, cache, audit, analytics portfolio). **Section 3 Advanced Layouts** ([[advanced-layouts-and-content-for]]) : `yield :name` + `content_for` accumule vs `provide` court-circuite + `content_for?` check, nested layouts pattern 2 préféré (layouts complets + partial `_head.html.erb`), body class scoping `<body class="<%= controller_name %> <%= action_name %>">`. ⭐ **Piège bloc `do...end` capture espaces** dans `<title>`. **Q5 répondue** (4 changements diagnostiqués). **Quick fixes UX/SEO ajoutés à `to-do.md` Dettes techniques** : `<html lang="<%= I18n.locale %>">`, title dynamique fallback ternaire, meta description fallback, body class scoping. ⭐ **Détection `I18n.locale => :en`** → corrections inline FR→EN dans note Obsidian + to-do.md (9 textes) + nouvelle memory globale `project-nine-to-fine-locale-english` créée. **Section 4 Metaprogramming** ([[metaprogramming-ruby-and-rails-first-pass]]) ⚠️ **1er passage à approfondir** — Chris a flaggé "encore flou" après décortication détaillée. Cours dédié programmé `to-do.md` Priorité 1 (1h-1h30 : 3 armes en Ruby pur, lecture source Rails `attr_accessor`/route helpers, exos sandbox `~/code/sandbox/rails/metaprogramming/` clone `attr_accessor` + Builder DSL `method_missing` + refactor `GarmentFilter` define_method Q6, question entretien 90 sec). Note 1er passage couvre : ⭐ **décortication ligne par ligne `GarmentFilter`** avec analogie centrale **chaîne de production usine** + bloc par bloc (FILTERS constante gelée, initialize `.to_h.with_indifferent_access.slice(*FILTERS)`, apply `FILTERS.inject(@scope)` + `next relation if blank?` + `send("by_#{key}", ...)`, méthodes privées `by_X`) + exécution réelle pas à pas sur URL `?q[color]=black&q[brand]=Uniqlo` + schéma mental ASCII + 3 super-pouvoirs combinés (inject Ruby standard, send LE metaprog, `ActiveRecord::Relation` lazy). Quick reference 3 armes + 4 pièges sécurité. **Section 5 Design Patterns + SOLID** ([[solid-design-principles-and-anti-patterns]]) : 5 lettres décortiquées avec exemples N to F concrets. ⭐ **OCP prouvé 2× en live** dans `GarmentFilter` (PR #79 ajout `:brand` proactif zéro modif `apply` + PR #83 ajout `:search`). 5 anti-patterns (God Object, Spaghetti, Cargo cult, Premature optimization, Magic numbers). **Q7 répondue** (concern `Likeable` B vs méthodes directes Outfit A) : Chris a coché ISP+SRP corrects mais **manqué OCP** qui est LE principe central (scénario "Garment likeable dans 3 mois" → zéro modif Outfit avec B vs duplication 4 méthodes avec A). ⭐ **Réponse 90 sec entretien clé en main** : 5 principes × 5 exemples N to F en 5 phrases. **Section 6 i18n** ([[i18n-internationalization-fundamentals]]) : vocabulaire + config `default_locale`/`available_locales` + YAML hiérarchiques + ⭐ **lazy lookup `t('.title')`** + interpolation `%{name}` + pluriels CLDR + 3 stratégies set_locale + ⭐ **sécurité whitelist mandatory** (anti path traversal yml). **Q8 répondue** : 3 implémentations set_locale, C seule sécurisée (whitelist via `I18n.available_locales.include?`). ⭐⭐ **Décision pédagogique tranchée jeudi (méta majeur)** : pas de setup i18n pre-emptive sur Nine to Fine = YAGNI + cargo cult + premature optimization (anti-patterns Section 5 appliqués 30 min plus tard, méta cohérent). Chris a challengé ma proposition de setup minimum → bon réflexe, j'ai reconnu et arrière. Scope complet **7 étapes** documenté `to-do.md` Dettes techniques avec **trigger explicite** : "le jour où tu veux vraiment servir une 2e langue, typiquement démo recruteur francophone Paris". État détecté `I18n.locale => :en` + 17 locales `available_locales` viennent de Devise auto-mergé (pas de `rails-i18n`/`devise-i18n` installés). **Knowledge Check (7 Q) + 3 questions style entretien 90 sec** à faire en rétro plus tard (règle péda 9). **Avant-midi Tailwind Interactivity ✅** — 19 utilities couvertes en lecture profonde, 1 note Obsidian solide [[interactivity-fundamentals]]. **Catégories** : pointeur souris (`cursor-*`, `pointer-events-*`), sélection (`select-*`), tactile (`touch-action-*`), scroll behavior (`scroll-smooth/auto`), scroll snap (`snap-x/y/both/mandatory/proximity/start/center/end/normal/always` + scroll-padding/margin), scrollbar (`scrollbar-width/color/gutter-*`), formulaires (`accent-*`, `caret-*`, `color-scheme-*`, `appearance-*`, `field-sizing-*`), perf (`will-change-*`, `resize-*`). ⭐ **Quick wins N to F identifiés** : `touch-manipulation` sur tous boutons mobiles (anti delay 300ms iOS Safari, gain UX énorme), combo `disabled + cursor-not-allowed + opacity-50` boutons désactivés, `accent-zinc-900` futurs checkboxes filtres, `motion-safe:scroll-smooth` anchor links. ⚠️ **À ne PAS faire** : `will-change-transform` partout (explosion mémoire GPU mobile = anti-pattern Premature Optimization), `select-none` sur contenu copiable, `pointer-events-none` au lieu de `disabled` HTML. ⭐ **3 questions style entretien 90 sec rédigées** : (1) **300ms delay iOS Safari** diagnostic + fix `touch-manipulation` + trade-off perte double-tap zoom ; (2) **`select-none` sur code de confirmation** anti-pattern UX + propose `select-all` ; (3) **`will-change-transform` sur grille 200 cards** réaction code review + mémoire GPU + pattern `hover:will-change`. 12 pièges gravés + synthèse décision rapide 10 cas. **Aprem CLI ✅ — PR #102 mergée** : migration des **7 specs models** (Minitest → RSpec) en mode coach pédago strict ligne par ligne, ~3h de coaching intense. **Specs livrés** : (1) `category_spec.rb` 6 examples (validations + #destroy `restrict_with_error` + DB-level uniqueness `RecordNotUnique`) ; (2) `tag_spec.rb` 8 examples (validations + unicité scopée user case-insensitive + polymorphic associations via `source_type` Garment vs Outfit) ; (3) `user_spec.rb` 12 examples **créé from scratch** (dette test sem 22 soldée — Devise `:validatable` injecte auto validations email + password + 3 cascade `dependent: :destroy` Garment/Outfit/Tag) ; (4) `garment_spec.rb` 21 examples (validations + COLOR_HEX invariant statique + associations + ⭐ **Active Storage section 4 tests** avec `define_singleton_method(:byte_size)` hack pour stub 11MB + ⭐ **Taggable concern section 7 tests** avec sémantique `nil`/`""`/valeur) ; (5) `outfit_spec.rb` 10 examples (validations + unicité scopée + smoke test Taggable) ; (6) `tagging_spec.rb` 6 examples (polymorphic + uniqueness sur `:tag_id`) ; (7) `outfit_garment_spec.rb` 5 examples (join pur + uniqueness sur `:garment_id`). **Total ~68 examples RSpec verts**. **Factories ajoutées** : (a) `outfit.rb` avec **callback `after(:build)`** auto-créant un garment scopé user pour passer `validates :garments, presence: true` (garde double `if .empty? && .user.present?`) ; (b) `outfit_garment.rb` simple. **Config ajoutée** : `config.file_fixture_path = Rails.root.join("test/fixtures/files")` dans `spec/rails_helper.rb` pour Active Storage testing. **CI workflow fix** : ordre `bin/rails test && bin/rspec` cassait sur 17 fails (`Username has already been taken`) parce que fixtures Minitest (`users.yml`) polluaient la DB avant RSpec → inversé en `bin/rspec && bin/rails db:test:prepare && bin/rails test` (RSpec transactional rollback nettoie tout). 7 Minitest models supprimés. **3 nouvelles notes Obsidian** : [[factory-bot-advanced-patterns]] (after(:build) + bypass `Model.new` + transient attribute + 7 pièges), [[rspec-active-storage-testing]] (file_fixture_path config + attach pattern + define_singleton_method hack + 7 pièges), [[rspec-taggable-concern-testing]] (les 7 tests + tableau sémantique nil/""/valeur + `Garment.find` vs `garment.reload` + smoke test pour concern partagé + 6 pièges). Note [[minitest-to-rspec-migration-patterns]] enrichie section 8 "Patterns vus EN LIVE jeudi 11 juin" (9 sous-sections) + statut migration models. Index Rails Testing MAJ avec 3 entrées. **Reports VEN 12 juin aprem** : migration 3 specs controllers Minitest restants (`garments`, `outfits`, `pages`) + démarrage RSpec `GarmentsController` rétroactif avec test IDOR cross-user (dette technique sem 22 vendredi 29 mai) + audit `bullet` complet si temps. **Méta erreurs Claude session** : (a) j'ai conçu la factory `outfit` avec un piège anticipé (garde `.empty?` ne distingue pas "non spécifié" de "explicitement vide" → fail test "is invalid without at least one garment" en live, fix immédiat via bypass `Outfit.new`) ; (b) j'ai oublié de flagger le préalable `file_fixture_path` AVANT de livrer la section Active Storage du spec (Chris a perdu ~5 min sur 3 fails ArgumentError) — mea culpa explicite + engagement à encadrer les préalables critiques avec `⚠️ Avant de continuer` sur les sections complexes. |
| Vendredi | 12 juin    | Matin Odin chap. **41 Websockets and Action Cable** (lecture profonde, dernier chapitre du parcours Odin Rails — clôture la consolidation post-bootcamp). Avant-midi Tailwind **Customization avancé v4** (`@apply`, custom variants `@variant`, custom utilities `@utility` — étape 14 index Tailwind, bouclage Phase 1 Tailwind avant transition Phase 2 React sem 26). Aprem CLI : suite RSpec controllers (3 fichiers Minitest restants `garments`/`outfits`/`pages` → request specs + démarrage RSpec `GarmentsController` rétroactif avec ⭐ test IDOR cross-user, dette technique sem 22) + audit `bullet` complet si temps. | 🟢 Matin ✅. **Matin Odin chap. 41 ✅** lu en lecture profonde + Rails Guide 8.1 Action Cable Overview sections 1-7 (sections 8-11 scaling/deployment/testing reportées sem 25 quand on code dessus). Coaching Claude sur la stack mentale Hotwire (Action Cable EN DESSOUS de Turbo Streams = sucre Turbo `turbo_stream_from @outfit` génère auto channel générique `Turbo::StreamsChannel` signé → tu n'écris JAMAIS de Channel custom pour les Turbo Streams broadcasts), l'auth via Warden (`env["warden"].user`) vs cookies.encrypted direct (4 obstacles : nom cookie variable, structure Warden interne fragile, sérialisation Marshal/JSON, session middleware absent dans pipeline WebSocket), le piège `this`/`bind` JS vanilla (analogie lettre+enveloppe vide, `addEventListener("e", this.method)` perd `this` — Stimulus fait le bind pour toi mais Action Cable JS vanilla non), Solid Cable Rails 8 défaut (cohérent stack `solid_queue` + `solid_cache` N to F = 3 pieds tabouret sans dépendance externe, 1 Postgres suffit Kamal Hetzner), ⭐ Connection permissive vs reject (pattern pro mix public/privé — `reject_unauthorized_connection` casse Turbo Streams sur pages publiques même si auth pas requise côté controller, solution = Connection permissive + `return reject unless current_user` per-channel). **Coaching mot par mot** sur 4 lignes denses : `if verified_user = User.find_by(id: cookies.encrypted[:user_id])` (assignation dans condition + parens RuboCop convention + 4 niveaux Symbol/String), `def appear(data); current_user.appear(on: data["appearing_on"])` (Symbols vs Strings dans la même ligne — `on:` Symbol hash Ruby pur, `["appearing_on"]` String hash JSON désérialisé), `class ChatChannel < ApplicationCable::Channel` (héritage `<` + résolution namespace `::` + pattern Application* + exception Action Cable double-classe), `this.update = this.update.bind(this)` (analogie lettre+enveloppe vide + bug silencieux `addEventListener` sans bind). **Décision pédago tranchée** : skip tuto Odin "Basic Messaging App" (vanilla JS pattern legacy 2018-2021 + viole règle péda 9 projets Odin remplacés par sandbox + Nine to Fine + `rails new` from-scratch lourd 60-90 min → 2-2h30 réelles + pattern obsolète vs Turbo Streams Rails 8 moderne) → exo sandbox focused Solid Cable + Turbo Streams samedi 13 juin + vraie pratique Action Cable sem 25 sur Nine to Fine likes/comments broadcast. **Note Obsidian rédigée** : [[action-cable-fundamentals]] (16 sections, 12 pièges gravés, Knowledge Check Odin 5 Q répondues par écrit, ⭐ 3 questions style entretien 90 sec rédigées : Turbo Streams cassés pour anonymes diagnostic+fix / implémentation likes broadcast N to F code complet `after_create_commit` + `broadcast_append_to` + `turbo_stream_from` vs vanilla JS legacy / pourquoi Solid Cable défaut Rails 8 vs Redis "no PaaS dependency by default" + trade-offs latence vs simplicité infra). Index Rails Obsidian section "Action Cable / WebSockets" remplacée (à venir → entrée pleine). PROGRESSION.md MAJ : **parcours Odin Rails terminé** ✅ (chap. 1-41 couverts en lecture profonde post-bootcamp). Application sem 25 actée : Connection permissive + Solid Cable install + likes/comments broadcast via `after_create_commit` + `broadcast_append_to` + `turbo_stream_from @outfit`. Lien stratégique rétroactif [[turbo-frames-streams-and-format-negotiation]] (lundi 8 juin sem 24) + [[callbacks]] (chap. 27 — `after_commit on: :create` mandatory pour broadcasts, sinon job/broadcast orphelin si transaction rollback). **Avant-midi Tailwind Customization avancé v4 ✅** lu en lecture profonde (docs "Adding custom styles" + "Functions and Directives") + ⭐ **application directe Nine to Fine en CLI mode coach pédago strict, PR #104 mergée** (groupe matin Action Cable + avant-midi Tailwind data-loading). Note Obsidian rédigée : [[customization-advanced-v4]] (12 sections, 12 pièges, 3 questions style entretien 90 sec, application directe PR #104 + future N to F sem 25+). ⭐ **Méta-feedback Chris en milieu de matinée** : *"je trouve ça inutile sans pratique c'est juste lire de la notation qui est aussi vite oubliée si on ne la mets pas en pratique"* → reconnu honnêtement (cf. Suivi avant-midi), décision de couper créneau matin/avant-midi en 1h lecture + 1h30 pratique immédiate pour chapitres de **doc de référence notationnelle** (vs chapitres conceptuels Action Cable / Callbacks / SOLID où lecture profonde sans pratique fonctionne bien). **Exo livré PR #104** (3 fichiers, anti-double-submit + state visuel custom) : (a) `application.css` += `@custom-variant data-loading (&[data-loading="true"]);` + `@utility btn-base { @apply ... }` (scope C : forme + focus ring universel, peinture en HTML pour rester ouvert OCP à futurs btn-primary/btn-danger via Rule of 3) ; (b) `submit_button_controller.js` Stimulus minimaliste (start/end mutent `this.buttonTarget.dataset.loading`) ; (c) `_form.html.erb` garment += data-controller submit-button + data-action multi-events (submit→start, turbo:submit-end→end) sur form + classes `data-loading:cursor-wait data-loading:opacity-50` + data-submit-button-target=button sur submit. **3 pièges majeurs vécus en live** : (1) ⭐ **typo `jsutify-center` dans `@apply` casse TOUTE la directive** Tailwind v4 (pas juste la classe inconnue) → bouton "carré" sans aucune mise en forme + erreur claire dans terminal `bin/dev` (à toujours surveiller) ; (2) ⭐ **`pointer-events-none` bloque `cursor-wait`** car browser ignore les events sur l'élément → cursor passe à travers → anti-pattern documenté hier dans [[interactivity-fundamentals]] que j'ai recommandé sans relire ma note (méta : relire les notes Obsidian avant de coder) ; Turbo pose déjà `disabled` natif → pas besoin de redoubler l'anti-clic ; (3) **Stimulus target manquant** (data-submit-button-target oublié sur submit) = mutation silencieuse + erreur console JS verbose (toujours console ouverte en debug). **Phase 1 Tailwind bouclée** ✅ — toutes étapes index Tailwind couvertes (étape 14 ✅), transition Phase 2 React fondamentaux sem 26 (22 juin). PROGRESSION.md MAJ. Index Tailwind Obsidian MAJ (nouvelle entrée customization-advanced-v4 + étape 14 ordre lecture ✅). **Aprem ✅ — PR #106 mergée** : 3 request specs livrés (pages 2 examples + outfits 13 + garments 15 = 30 nouveaux examples) + 7 IDOR cross-user sentinels (3 outfits + 4 garments, dette technique sem 22 vendredi 29 mai soldée) + cleanup complet `test/` (`git mv test/fixtures/files spec/fixtures/files` + `git rm -r test/` + retrait `config.file_fixture_path` custom) + simplification CI (job `test` 4 cmd → 2 cmd, suppression job `system-test` vide) + branch protection rule MAJ (suppression check `system-test` required). **Total suite RSpec** : 113 examples verts. **3 notes Obsidian** : [[rspec-request-specs-fundamentals]] + [[idor-sentinel-pattern-rspec]] + enrichissement [[minitest-to-rspec-migration-patterns]] section Controllers. **Clôture session — audit bullet exécuté** : 9 flows critiques verts (home + garments index nu/filtré couleur/recherche + show + new + outfits index + show + new + edits) ; ⚠️ 1 warning persistant sur POST /outfits create (`Garment#photo_attachment` USE eager loading detected, call stack `outfits_controller.rb:19 if @outfit.save`) ; tentative fix `set_garments` includes → warning inverse "AVOID" → revert ; cause exacte non identifiée (hypothèses : cas edge `has_many through:` ou middleware/callback caché) → **investigation reportée samedi 13 juin ou sem 25 ~30-45 min** (entrée détaillée `to-do.md` Dettes techniques). Branche `feat/audit-bullet` supprimée sans PR (aucun fix appliqué). 2e PR planning `chore/end-of-day-fri-jun-12` créée pour couvrir l'audit bullet + entrée clôture SUIVI + to-do.md MAJ. |
| Samedi   | 13 juin    | Rattrapage / projet / personal to-do (Huntr Le Wagon 🔴, contentieux serrurier 🔴, relire Turbo Frames/Streams 🟡, décision SPA vs MPA NtF 🟡 — voir vault Obsidian `personal-to-do.md`). **+ Rattrapage exos dirigés sandbox** : (a) 2 soirs sem 23 (ven 5 + sam 6) non faits ; (b) lundi 8 juin soir (exo Minitest sandbox sur mini Query Object OU portage des tests Minitest du `GarmentFilter` — étape 3 reportée) ; (c) **mardi 9 juin soir confirmé non fait** — exo Stimulus debounce dans `~/code/sandbox/stimulus/` (controller `debounced_search_controller.js` + HTML standalone, debounce 300ms via `static values`, `this.timeout` propriété pure, lifecycle `disconnect()` + `clearTimeout` obligatoire, cas limite pédagogique du timer fantôme à reproduire, spec succincte intégrale dans SUIVI sem 24 mardi). Pile les concepts à consolider en priorité : Stimulus 3.x syntaxe v3 (post-Odin chap. 37 du 9 juin) + Query Object pattern + Turbo Frame test helpers (cf. [[gorails-turbo-frame-test-helpers]]).                                                                                                                                                              | ⏳                                                                                                                                                                                                                                                                              |
| Dimanche | 14 juin    | OFF.                                                                                                                                                                                                                                                                                                                                               | —                                                                                                                                                                                                                                                                               |

## ⭐ Bilan Phase 1 — bouclée vendredi 12 juin, 9 jours en avance

**Date de bouclage** : vendredi 12 juin 2026 (sem 24), au lieu du **dimanche 21 juin** prévu initialement (fin sem 25).

**Avance acquise** : 9 jours sur 28 jours prévus Phase 1 (32 % d'avance).

### Cause de l'avance

- Slot matin Odin Rails très productif (1 chap. / jour en moyenne, parfois 2 en paire — chap. 34+35 Bundling jeudi 4 juin, chap. 6 sections Advanced Topics jeudi 11 juin).
- Slot avant-midi Tailwind v4 productif aussi (1 section / jour en moyenne, doc dense mais bien structurée).
- Rythme aprem N to F productif (notamment sem 24 lundi = 3 PR livrées, jeudi = 7 specs RSpec + ~68 examples).
- **Sub-projects opportunistes** acceptés en cours de route (color selector Tom Select, design system typo Playfair+Inter, RSpec setup complet avec migration totale Minitest → RSpec) qui ont enrichi le scope sans casser la timeline.

### Décision de placement des 9 jours d'avance

**Option rejetée** : avancer Phase 2 React de 1 semaine (sem 25 = React démarrage au lieu de sem 26). Raison du rejet : **construire sur du flou** Phase 1 = bugs cumulés en Phase 2-4. Pattern pro = consolider AVANT d'avancer.

**Option retenue** : **disperser des sessions consolidation dans sem 25** sans toucher au démarrage Phase 2 sem 26 (22 juin) ni à la timeline emploi septembre.

Détail dans la section "Sem 25 (15-21 juin)" ci-dessus.

### Chapitres consolidés sem 25 (vs simple lecture profonde Phase 1)

1. ⭐ **Metaprogramming Ruby/Rails** (cours dédié 1h30 + 3 exos sandbox = 5h cumulées) — résolution du "1er passage flou" jeudi 11 juin.
2. ⭐ **Stimulus avancé** (cours dédié 1h + 3 exos sandbox = 5h cumulées) — pratique des 5 patterns avancés jamais codés (Outlets, `dispatch()`, `static classes`, TargetConnected, state machine via Values).
3. ⭐ **Turbo Streams broadcast pattern** (pratique directe sur Likes + Comments N to F, pas de cours séparé) — coder Likes/Comments en broadcast moderne dès la 1ère implémentation = consolidation par production réelle.
4. **Sandbox routine soir réactivée** (5 exos × 30-45 min sem 25) — Turbo Streams 8 actions, Templates `.turbo_stream.erb`, AR Queries avancées, APIs Phase 4 prep + reste cumulés sem 24.

### Lien stratégique avec timeline emploi

- **Phase 1 bouclée 12 juin** ✅
- **Phase 1 consolidée + features sociales N to F livrées + recherche emploi lancée** = fin sem 25 (21 juin)
- **Phase 2 React démarre sem 26** (22 juin) inchangée
- **Jalon mi-juillet N to F v1 déployée** (sem 29) inchangé
- **Jalon fin septembre 2026** (job offer Rails junior Paris ou full remote) inchangé

### Conditions d'alerte sem 25

- Si **lundi-mardi sem 25** Likes broadcast Turbo Streams patine (> 1.5 j) → simplifier en HTML simple, prévoir upgrade Turbo Streams en sem 26-27.
- Si **mercredi 17 journée consolidation hybride** trop dense cognitivement → décaler 1 cours à jeudi matin en repoussant Follow d'½ journée.
- Si **vendredi 19 lancement emploi** patine → décaler les 3 candidatures à samedi (avec création skill `job-tracker`).
