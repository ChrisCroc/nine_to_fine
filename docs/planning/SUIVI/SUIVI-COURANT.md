# Suivi vivant — Sem en cours + précédente

Journal de bord récent. À mettre à jour à la fin de chaque journée via la routine `/end-of-day` (à créer sem 22+).

**Politique de rolling** : ce fichier contient **la semaine en cours + la semaine précédente complète**. Début de mois (au plus tard le premier lundi du mois suivant), archivage de tout le mois précédent vers `ARCHIVE/{YYYY-MM}.md`.

**Politique de verbosité** : journal **condensé**. Les pavés techniques (pièges, concepts maîtrisés, choix de design, idiomes Rails 8, classes Tailwind) **vivent dans les notes Obsidian** (`~/Obsidian/DevJobReady/Apprentissage/`). Le Suivi les **référence par lien**, ne les duplique pas. Le backlog forward-looking (exos en attente, dettes, brainstorms reportés, chantiers coupables) vit dans `~/Obsidian/DevJobReady/to-do.md`.

**Règle de lecture au démarrage de session** :
- Mardi-vendredi : focus journée veille + sem en cours.
- Lundi (ou 1ère session de la sem) : lecture intégrale sem précédente + sem en cours qui démarre, avec récap accomplissements + blocages + ajustements de la sem précédente avant le programme du jour.

**Archives** : voir `docs/planning/SUIVI/ARCHIVE/{YYYY-MM}.md`.

---

### Sem 23 (1-7 juin 2026) — condensée

**Archive verbatim** : voir [`ARCHIVE/2026-06.md`](ARCHIVE/2026-06.md) (section sem 23, jour par jour).

**Livraisons clés**

- **CRUD Outfit + composition** (PR #53/#54/#55, lun 1er juin) : controller 7 actions + setter `garment_ids=` (jointure pure, pas `fields_for`) + `validates :garments, presence: true` + combobox Tom Select multi-select à pills via importmap + Stimulus. Notes : [[has-many-through-composition-and-presence-validation]], [[tom-select-importmap-stimulus]], [[erb-nested-anchor-and-loop-tag-pitfalls]].
- **Active Storage sur Garment** (PR #59 + tests #61, mar 2 juin) : `has_one_attached :photo` + variants WebP + validation `active_storage_validations` (`content_type` + `size` + `spoofing_protection: true`) + swap `mini_magick`→`ruby-vips`. Piège purge `""→DeleteOne` neutralisé. Notes : [[active-storage-fundamentals-and-variants]], [[active-storage-validations-and-pitfalls]], [[object-fit-and-aspect-ratio]], [[minitest-active-storage-validations]].
- **Outfit collage adaptatif** (PR #69, mer 3 juin) : partial unique `_collage` taille-agnostique + eager loading anti-N+1 (`includes(garments: [:category, { photo_attachment: :blob }])`) + mosaïque plafonnée à 4 tuiles + compteur `+N`. Notes : [[eager-loading-sort-by-vs-order-and-nested-attachments]], [[outfit-collage-adaptive-mosaic]].
- **Tags comma-separated** (PR #74, jeu 4 juin) : concern `Taggable` mutualisé Garment+Outfit + attribut virtuel `tag_names` + `after_save :sync_tags` (`find_or_create_by` + downcase) + gem `bullet` (2 N+1 préexistants corrigés). Notes : [[taggable-concern-and-tag_names-virtual-attribute]], [[minitest-taggable-concern]], [[bullet-n+1-detection-and-eager-loading-fixes]].
- **Exo sandbox `auth-from-scratch`** (lun 1er soir, `~/code/sandbox/rails/` commit `f0c3609`) : `has_secure_password` from scratch sans Devise. Note : [[auth-from-scratch-sandbox-exo]] + entretien : [[Entretiens/Rails/auth-from-scratch]].

**Odin Rails (lectures profondes matin)** : chap. 27 Callbacks (mar 2), chap. 30+31 APIs paire (mer 3), chap. 34+35 CSS+JS Bundling paire (jeu 4). Notes : [[callbacks]], [[apis-and-building-your-own]], [[working-with-external-apis]], [[bundling-vs-importmap-rails-8]].

**Tailwind (lectures profondes avant-midi)** : Theme + Functions/Directives (mar 2), Dark mode + Colors palette (mer 3), Backgrounds/Borders/Effects/Opacity (jeu 4). Notes : [[theme-variables-and-directives]], [[dark-mode]], [[colors-palette]], [[backgrounds-borders-effects-v4]].

**CI/CD méta** : PR #65 (routine reco vidéos fin de session activée), PR #66 (workflow auto-fix Dependabot bundler CHECKSUMS — testé en réel sur PR #63 jbuilder + #64 puma ; friction connue : `GITHUB_TOKEN` ne retrigger pas CI → close+reopen manuel), PR #68 (push filter CI `master`→`main`).

**Décisions de design tranchées (ne pas re-proposer)**

- **Position spatial outfit** (drag-drop canvas, hyp. B) : reporté backlog → composition simple V1.
- **Compteur `+N` mosaïque** : non-cliquable V1 (card-lien + pills de `show` couvrent déjà le besoin).
- **API JSON N to F V1** : pas d'API (Hotwire monolith, pas de SPA à servir). Option `/api/v1/` portfolio Phase 4 à trancher.
- **Dark mode V1** : non (photos vêtements = perception colorimétrique correcte requise).
- **Tom Select setup** : pin CDN sur build complete wrappé `+esm` (pas vendoring importmap qui 404).
- **Processeur images Rails 8** : `:vips` (cohérence Dockerfile libvips + `brew install vips` dev).
- **Intégration Anthropic Phase 4** : gem officielle `anthropic-sdk-ruby` + service object `app/services/ai/outfit_suggester.rb` + credentials chiffrées `credentials.yml.enc`.
- **Sélection garments par clic dans l'index** (remplace Tom Select à ~100 pièces) : reporté backlog `FEATURES_FUTURES.md`, post-emploi après index filtrable.

**Concepts maîtrisés en live (ne pas réexpliquer comme débutant)**

- `has_many through:` + setter virtuel `garment_ids=` (jointure pure, pas `accepts_nested_attributes_for` / `fields_for`).
- `validates :garments, presence: true` voit la collection en mémoire avant save (≠ DB).
- Eager loading nested attachments : `photo_attachment: :blob` (proxy), pas `:photo`.
- Concern polymorphique mutualisé (`Taggable` sur Garment+Outfit) avec `as: :taggable`.
- Garde `nil` (préserver) vs `[]` (vider) pour attribut virtuel `tag_names`.
- Minitest 6 (Rails 8.1) a retiré `Object#stub` → stub Ruby pur via `define_singleton_method`.
- `respond_to do |format|` + négociation Accept header + `as_json only:` whiteliste (pas `except:`).
- Active Storage : piège purge `""→DeleteOne` (file_field vide), garde `garment_params` retirant `:photo` blank.
- 3 vidéos GoRails visionnées en autonomie mer soir : [[preserving-file-uploads-on-edit]], [[gorails-importmaps-with-rails]], [[strict-loading-activerecord]].

**Friction / blocages capturés**

- Setup Tom Select / importmap : ~35 min trial-and-error (lun 1er) → règle mémoire `feedback-verify-external-setup`.
- Questions de vérification floues 2× (mar 2 matin) → règle mémoire `feedback-concrete-verification-questions`.
- **Ven 5 + sam 6 décrochés** (fatigue post grosse semaine prod) → 2 soirs sandbox manqués → cumul exos reportés sem 24 (samedi 13 juin rattrapage).

---

### Sem 24 (8-14 juin 2026)

**Accompli**

- **Lundi 8 juin (démarrage, Claude app)** — Récap sem 23 + diagnostic du retard (ven/sam non faits + 2 soirs d'exos sandbox manqués). Programme du jour annoncé : Odin Turbo + Tailwind Typography + (aprem en CLI) filtres `link_to` + tags cliquables. **Kanban GitHub réaligné** : 15 issues closes en mode coach (`gh issue close X -c "..."` exécutés par Chris) → US01/02/03/06/07/08/09/10/11/12/13/14/15/16/17. Restent ouvertes : US04/05 (sem 25 profil), US18-21 (sem 25 social), US22-24 (Phase 4 IA). **3 notes Obsidian GoRails rétroactives** rédigées par Claude (vidéos regardées jeudi 4 juin soir) : [[preserving-file-uploads-on-edit]], [[gorails-importmaps-with-rails]], [[strict-loading-activerecord]] (⚠️ vidéo > 2 ans, API Rails 8 vérifiée inchangée). Index Rails Obsidian mis à jour avec les 3 entrées. **`personal-to-do.md` créé** dans la vault Obsidian (racine `DevJobReady/`) : checkbox + jour + priorité 🔴/🟡/🟢 ; 4 items posés pour samedi 13 juin (Huntr 🔴, relire Turbo Frames/Streams 🟡, décider SPA/MPA NtF 🟡, contentieux serrurier 🔴).

- **Lundi 8 juin (matin, Claude app)** — Odin chap. **36 Turbo** en lecture profonde + coaching Claude (vulgarisation prioritaire, Frames + Streams + format negotiation = avancé sous-le-capot). Couvert : modèle mental Hotwire (Drive + Frames + Streams + Stimulus + Native), **Turbo Frames** (PIP scope, `turbo_frame_tag @model` idiomatique via `dom_id`, `src` eager + `loading="lazy"`, `data-turbo-frame="_top"` pour sortir, ciblage externe, `aria-busy`), **Turbo Streams** (8 actions append/prepend/replace/update/remove/before/after/refresh, `target` singulier vs `targets` CSS selector pluriel, helpers Rails `turbo_stream.replace(..., partial:)`, templates `.turbo_stream.erb`, broadcast Action Cable via `after_create_commit { broadcast_append_to }` + `turbo_stream_from @record`), **négociation de format** (`respond_to` + `Accept:` header : Turbo n'est pas magique, il ajoute juste `text/vnd.turbo-stream.html` en tête de l'`Accept`). Bonus hors Odin : **morphing Turbo 8** (`<meta name="turbo-refresh-method" content="morph">` + `data-turbo-permanent` + `broadcasts_refreshes`). **3 questions de vérification entretien détaillées répondues par Claude** : (1) scoping frame avec lien interne ("Modifier" reste dans le frame = ✅ ; "Retour à index" reste dans le frame = ❌ frame vidé → fix `data: { turbo_frame: "_top" }`) ; (2) `update` vs `replace` avec Stimulus `data-controller` attaché (update conserve l'instance, replace fait disconnect+connect — choix prod = replace + partial self-contained) ; (3) piège `406 Not Acceptable` si on supprime `format.html` quand un client envoie `Accept: text/html` (Turbo OFF, JS désactivé, crawler) + pire : `create!` exécuté avant `respond_to` → record en DB + erreur 406 affichée. Knowledge Check Odin (4 Q : SPA, Hotwire, Frame, Stream) répondu par écrit. **Application directe Nine to Fine** : filtres `link_to` aprem sem 24 (frame `garments_list`), likes/comments sem 25 (stream replace + broadcast). Note Obsidian : [[turbo-frames-streams-and-format-negotiation]]. Index Rails Obsidian mis à jour.

- **Lundi 8 juin (avant-midi, Claude app)** — Tailwind docs **Typography** en lecture profonde + coaching Claude (vulgarisation forcée — 2 reprises de Chris sur clarté pendant Turbo le matin, règle `feedback-vulgarize-advanced-rails` re-flaggée pour Tailwind v4 aussi). Couvert : **Font family** (`font-sans/serif/mono` + `ui-sans-serif`/`ui-serif` qui pointent vers la police système native, customization via `@theme --font-display/--font-body`, namespace `--font-X` génère utility `font-X`, ordre critique des `@import` Google Fonts AVANT `@import "tailwindcss"`, feature settings OpenType). **Font size** (13 tailles `text-xs` à `text-9xl`, **rupture à `text-5xl`** : line-height par défaut couplé proportionnel sur les petites/moyennes tailles, forcé à 1.0 sur les ≥48px, shorthand `text-base/7` en `--spacing` units, bundling v4 `--text-h1--line-height/--letter-spacing/--font-weight`). **Font weight** (100→900, **piège faux gras synthétique** si la font ne charge pas le poids, variable fonts `font-[450]`). **Line height** (formule `calc(--spacing × n)`, unitless vs unit héritage, `leading-*` ≠ `space-y-*`). **Letter spacing** (em-based pour scale avec font-size, conventions pro : `-tracking-tight` sur gros titres, `tracking-wider` sur majuscules). **Text decoration** (5 propriétés combinables : line, color, style, thickness, offset + pattern lien moderne). **Q1 coaching** : diagnostic du chevauchement `text-5xl + leading-7` (ratio 28/48 = 0.58 = télescope vertical) + fix (retirer `leading-7`, garder défaut). 11 pièges gravés. **Proposition design system Nine to Fine actée** dans la note (à valider en CLI cet aprem) : Playfair Display (display) + Inter (body), échelle 5 tailles sémantiques bundlées v4 (`--text-h1/h2/h3/body/caption`). Note Obsidian : [[typography-fundamentals]]. Index Tailwind Obsidian mis à jour.

- **Lundi 8 juin (aprem, Claude Code CLI)** — **`GarmentFilter` Query Object livré (back complet, smoke test 5 cas OK), PR #79 mergée**. Mode coach pédago strict (Chris code, Claude guide ligne par ligne avec analogies). Spec verbale en chat (pas de doc spec séparée).

  - **Pivot design en début de session** : abandon du "un seul filtre à la fois" (spec sem 23) → **filtres combinables AND** (couleur ET catégorie ET tag ET brand). Argument Chris : Zalando/Uniqlo le font, c'est UX standard. Conséquence : **option B (routes nested) éliminée** (combinatoire `/categories/X/tags/Y/garments` ingérable) → **C choisi** (objet `q` paramétrisable `?q[color]=red&q[category_id]=3`).

  - **Design choisi** (récap des 9 décisions chat) : URL `?q[...]` plat ANDé, pipeline `inject` + dispatch dynamique via `send("by_#{key}")`, PORO (pas ActiveModel), sécurité par scope d'entrée `current_user.garments`, tri APRÈS filter au controller, strong params (`permit`) + slice défensif (3 filets de défense en profondeur), tests Minitest planifiés (9 cas), emplacement `app/queries/garment_filter.rb`. Périmètre V1 : `GarmentFilter` uniquement, pas d'`OutfitFilter` (YAGNI).

  - **Concepts/idiomes Ruby couverts** : `%i[...]` symbol array literal (et **piège virgule** silencieux : `tag_id,` devient `:"tag_id,"`), `.freeze` sur constantes (vs piège `FILTERS << :evil`), variable d'instance `@` vs locale (portée controller→vue), `||` nil-coalescing, `Hash#slice` + `*` splat, `inject` (accumulator pattern), blocks `{ |a, b| ... }`, `send` dispatch dynamique, string interpolation `"#{key}"`, safe navigation `&.`, early return `return X if cond`. Côté AR : `.where(col: val)` SQL paramétrisé, `.joins(:tags)` via `has_many through:` polymorphique (2 INNER JOIN générés), `.where(tags: { id: val })` syntaxe hash imbriqué, **lazy evaluation** (SQL exécuté à l'itération, pas au `.new`).

  - **Open/Closed Principle prouvé en live** : Chris a ajouté proactivement un 4ème filtre `brand` (`FILTERS` + `by_brand` + `:brand` au `permit`) → **zéro modif sur `apply`/`results`**. Le pattern marche.

  - **Smoke test 5 URLs** (logs SQL vérifiés) : `/garments` nu (identité) ✅ ; `?q[color]=red` (`WHERE color`) ✅ ; `?q[tag_id]=1` (`INNER JOIN taggings → tags` + `WHERE tags.id`) ✅ ; `?q[category_id]=1` (`WHERE category_id`) ✅ ; combo `color+category_id` (AND combiné dans un seul `WHERE`) ✅. Eager loading préservé via scope avant filter (`includes` reste actif, pas de N+1).

  - **Bugs traversés** : (1) `NameError uninitialized constant GarmentFilter` au 1er run = Zeitwerk ne re-scanne pas `app/queries/` créé après boot → restart serveur. (2) Virgule dans `%i[color category_id tag_id, brand]` → `:"tag_id,"` au lieu de `:tag_id` → filtre tag silencieusement cassé. (3) `private` indenté à 0 espaces (style RuboCop attend 2).

  - **Defense en profondeur** : 4 filets (scope `current_user.X` + `permit` controller + `slice(*FILTERS)` filter + `.where(col: val)` SQL paramétrisé). Une URL `?q[evil]=hack&q[user_id]=99999&foo=bar` est nettoyée en cascade : `foo` viré par `params[:q]` (hors scope), `user_id`+`evil` virés par `permit`.

  - **Reports résolus en fin d'aprem** : **Étape 3 tests Minitest livrée, PR #81 mergée** — 10 cas dans `test/queries/garment_filter_test.rb` (identité, chaque filtre seul incluant le JOIN polymorphique tag, combos AND 2 et 4 filtres, edge cases `slice` + `.blank?`, ⭐ **test de propriété de sécurité cross-user**). Fixture `black_jacket` (user=john, color=black) ajoutée à `garments.yml` pour rendre le cas 10 non-trivial (sans elle, l'isolation aurait passé par hasard). Bug vécu pédagogique : typo `result_empty` vs `results_empty` (Minitest `NameError: undefined local variable`). 10 runs, 19 assertions, 0 failure. Notes : [[minitest-garment-filter-query-object]].

  - **Reports résolus en fin d'aprem (front) — PR #83 mergée** : story 3+4 livrée en bloc. Mode coach pédago strict.

    - **Refactor model `Garment`** : constante `COLORS = %w[black white grey beige brown red orange yellow green blue purple pink].freeze` + `validates :color, presence: true, inclusion: { in: COLORS, allow_blank: true }` (`allow_blank` pour ne pas doubler le message d'erreur avec `presence`). Test Minitest associé.

    - **Normalisation des données dev** (avant validation `inclusion` activée) : 12 valeurs polluées détectées via `Garment.where.not(color: Garment::COLORS).pluck(:color).uniq` (français : `noir`/`blanc`/`rouge`/`vert`/`gris`, féminins/pluriels `noires`/`blanche`, composés `light grey & purple`/`blue denim`/`light blue`, typo `blanc ` avec espace en trop). Script `update_all` exécuté en `rails runner` avec mapping explicite. Cas tranché par Chris : `light grey & purple` → `grey` (V1 mono-color, multi-color reporté backlog). Vérification finale : 0 violation restante.

    - **Controller** : `GarmentsController#index` expose `@filter_params` (déjà permit-é, safe pour la vue, **convention "la vue ne touche jamais à `params` brut"**) + `@available_colors` (= constante model), `@available_categories` (= `Category.order(:position)`), `@available_brands` (= `current_user.garments.where.not(brand: [ nil, "" ]).distinct.pluck(:brand).sort`), `@available_tags` (= `current_user.tags.order(:name)`).

    - **Helper** : `pill_class(active)` dans `garments_helper.rb` (DRY des classes Tailwind actif/inactif, fond zinc-900+champagne si actif, fond blanc translucide si inactif).

    - **Vue index refondue** : `turbo_frame_tag "garments_list"` enveloppe **bandeau filtres + grille**, en-tête (h1 + boutons Add/Create) **hors** du frame (statique). Bandeau = 4 lignes (Color / Category / Brand / Tag) chacune avec pill "All" + pills par valeur. Propagation des filtres actifs via `current.merge(...)` (ajout/remplacement d'une dimension) et `current.except(:dim)` (suppression d'une dimension). `current = @filter_params.to_h.with_indifferent_access` au début de la frame pour permettre `current[:color]` (symbol) sur un hash à clés strings (piège classique Rails). Cast `.to_i` sur `current[:category_id]` et `current[:tag_id]` pour comparer correctement string-from-URL ↔ integer-from-AR. État vide adapté au contexte (filtres trop stricts vs pas encore de garments).

    - **Pattern Turbo Frame `_top`** : 2 `link_to` à l'intérieur du frame `garments_list` pointant vers des pages **sans** frame matchant doivent avoir `data: { turbo_frame: "_top" }` pour échapper au frame : (1) `link_to garment` qui wrap chaque card (→ show garment), (2) `link_to "Add your first piece..."` dans l'état vide (→ new garment). **Sans `_top`, Turbo affiche "Content missing" à la place de la grille** (bug latent : ne se déclenche qu'au premier clic post-rendering du frame).

    - **Partial `shared/_tags`** : pills tag deviennent `link_to tag.name, garments_path(q: { tag_id: tag.id }), class: "... hover:bg-zinc-200 ... transition"` (style cliquable + hover discret). Bug existant corrigé au passage : `flew-wrap` (typo) → `flex-wrap`.

    - **Bugs traversés en session** : (1) `NameError uninitialized constant GarmentFilter` (initial back, Zeitwerk + restart serveur) — vu sem 23 ; (2) typo `result_empty` vs `results_empty` (tests Minitest) ; (3) Chris a oublié de virer la virgule dans `%i[]` (vu back) ; (4) **"Content missing" Turbo** (le bug du jour qui a fait perdre du temps) = link_to dans le frame ciblant une page sans frame ; (5) virgule manquante entre `data: { turbo_frame: "_top" }` et `class:` dans `link_to` (Ruby `SyntaxError`) ; (6) RuboCop `Layout/EmptyLinesAroundModuleBody` sur le helper (autocorrect `bundle exec rubocop -a`).

    - **Reports actés au backlog `FEATURES_FUTURES.md`** : (a) **multi-color par garment** (suite de la discussion "light grey & purple") ; (b) **normalisation `brand` au save** (callback `before_save :normalize_brand` avec `.strip.titleize`, pour éviter la pollution future "Uniqlo"/"uniqlo"/"UNIQLO ").

  - **Reportés mardi 9 juin** : (a) **application design system typo** (Playfair Display + Inter + `@theme --text-h1/h2/h3/body/caption`) — déjà au programme polish navbar/footer mardi ; (b) **exo dirigé du soir** (non fait) ; (c) **reco 2 vidéos fin de journée** (non faite).

  - Notes Obsidian : [[query-objects-pipeline-inject-and-dynamic-dispatch]] (back, PR #79) + [[minitest-garment-filter-query-object]] (tests, PR #81) + [[turbo-frames-in-production-and-content-missing-trap]] (front, PR #83, nouvelle) + [[inclusion-validation-with-allow-blank]] (model, PR #83, nouvelle). Index Rails Obsidian mis à jour (sections "Active Record / Queries" + "Active Record / Validations" + "Asset Pipeline & Hotwire / Turbo Frames + Streams" + "Testing").

- **Lundi 8 juin (soir)** — **Exo dirigé sandbox non fait** (reporté samedi 13 juin rattrapage). **2 vidéos GoRails visionnées en autonomie par Chris** (accès Pro), notes Obsidian rédigées le 9 juin matin :
  - **[Stimulus JS Framework Introduction](https://gorails.com/episodes/stimulus-js-framework-introduction)** (Chris Oliver, 23 jan 2018, 18 min, Free) — **forward-looking** : préparation Odin chap. 37 Stimulus du mardi 9 juin matin. ⚠️ **Vintage lourde** : démontre Stimulus 0.9.0 (pré-1.0), syntaxe targets `data-target="X.name"` morte depuis v2 (remplacée par `data-X-target="name"` + déclaration obligatoire `static targets = [...]`). Concepts stables (3 piliers controllers/actions/targets + lifecycle `initialize`/`connect`/`disconnect` + philosophie "modeste" opposée React) OK pour le modèle mental. Note Obsidian : [[gorails-stimulus-introduction]].
  - **[Adding Turbo Frame Test Helpers](https://gorails.com/episodes/adding-turbo-frame-test-helpers)** (Chris Oliver, 16 juin 2025, 11 min, Pro) — **backward-looking** : consolide le `turbo_frame_tag "garments_list"` de la PR #83 mergée le matin/aprem même. **Forward-looking** aussi : prépare le setup RSpec mercredi 10 juin + tests rétroactifs `GarmentsController` jeudi 11 juin. Pattern helper Minitest `assert_turbo_frame(id, &block)` qui wrap `assert_select "turbo-frame#X"` (intention explicite + parser Nokogiri vs regex fragile) + variante system test Capybara + matcher RSpec `have_turbo_frame`. ⭐ **Test du piège `Content missing`** (system spec qui clique une card depuis le frame et vérifie qu'on sort vers la show sans erreur Turbo) — celui qui aurait attrapé le bug `_top` du lundi avant prod. À intégrer aux specs system jeudi/vendredi. Note Obsidian : [[gorails-turbo-frame-test-helpers]].

- **Mardi 9 juin (matin, Claude app)** — Odin chap. **37. Stimulus** en lecture profonde + handbook officiel (Origin / Introduction / Hello Stimulus) + reference complète (Controllers / Actions / Targets / Values / CSS Classes / Lifecycle Callbacks) + 3 exos sandbox dans `~/code/sandbox/stimulus/` (repo `ChrisCroc/sandbox-stimulus` créé en cours de session). Couvert : **philosophie "modeste"** Stimulus (vs React/Redux, sparkle sur HTML serveur, slogan "framework for the HTML you already have"), **4 piliers** Controllers + Actions + Targets + Values, **lifecycle** complet (initialize 1× / connect N× / disconnect N× + XTargetConnected/Disconnected + XValueChanged), naming conventions strictes (camelCase JS / kebab-case HTML / snake_case fichiers), registration auto via importmap, cross-controller communication (`dispatch` pub/sub + Outlets v3). **Exos sandbox** : (1) `hello_controller` (1er wiring, validation `data-controller` + `data-target` + `data-action`) ; (2) `slideshow_controller` v1 impératif (`this.index` + appel manuel `showCurrentSlide()`) ; (3) `slideshow_controller` v2 réactif (`static values = { index: Number }` + `indexValueChanged()` callback auto — la marche d'escalier vers le pattern réactif) ; (4) `content_loader_controller` (`fetch` + `setInterval` polling + `clearInterval` cleanup obligatoire dans `disconnect()`). **Pièges vécus en live** : interpolation Ruby `#{}` vs JS backticks `${}` (vu sur `hello_controller`) ; type minuscule `string` vs `String` qui raise `ReferenceError` (vu sur `content_loader_controller`). **3 questions style entretien junior Rails 90 sec répondues par écrit** : (1) `initialize()` vs `connect()` avec instanciation Chart.js + impact Turbo Drive ; (2) pattern impératif vs réactif avec compteur de panier (3 arguments concrets pour Values) ; (3) bug du timer fantôme `setInterval` sans cleanup (200 req/s en prod, code du fix). Knowledge Check Odin (4 Q) répondu par écrit. **Méta sandbox** : repo `ChrisCroc/sandbox-stimulus` créé (public, init commit du template `hotwired/stimulus-starter` + 3 exos). Convention commits sandbox `exo(<concept>): <desc>` actée. Note Obsidian : [[stimulus-fundamentals]]. Index Rails Obsidian mis à jour.

- **Mardi 9 juin (avant-midi, Claude app)** — Tailwind docs **Transitions & Animation** en lecture profonde (transition-property + duration + timing-function + animation, 4 pages). Couvert : distinction **transitions** (état A→B, déclenché par hover/focus/classe JS) vs **animations** (keyframes cycliques). **Famille `transition`** : utility défaut (liste longue propre color/opacity/shadow/transform), utilities ciblées (`transition-colors`, `transition-opacity`, etc.), **`transition-all` = code smell** (perf catastrophique sur listes + animations parasites + FOUC). `duration-*` (échelle 0-1000ms, défaut 150ms sweet spot, > 500ms = perception latence). `ease-*` (linear/in/out/in-out, défaut `cubic-bezier(0.4, 0, 0.2, 1)` Material), custom cubic-bezier `[bracket]` + `@theme --ease-X`. `delay-*` (tooltips, staggers). **5 animations built-in** (`animate-spin`/`ping`/`pulse`/`bounce`/`none`) avec patterns (spinner submit, badge ping, skeleton pulse). **Custom animations** : `--animate-X` + `@keyframes X` dans `@theme` (pas de fade-in/slide-up natif, à déclarer). ⭐ **Accessibilité non-négociable** : `motion-safe:` et `motion-reduce:` sur toutes animations infinies et transforms (WCAG 2.3.3 — risque nausée/épilepsie). **3 questions style entretien répondues par écrit** : (1) `transition-all` vs `transition` en code review (3 arguments concrets perf + parasites + FOUC + version corrigée) ; (2) spinner accessible avec `motion-safe:animate-spin` + texte alternatif "Loading…" ; (3) custom `animate-fade-in` Nine to Fine via `@theme` + `@keyframes` + intégration ERB + bonus cascade `stagger` avec `animation-delay`. 10 pièges gravés. Application N to F directe sem 24+ : 8 cas (pill filtre hover, card hover, submit spinner, skeleton loader, fade-in cards index, like button press, modal backdrop, toast slide-in). Note Obsidian : [[transitions-and-animations-fundamentals]]. Index Tailwind Obsidian mis à jour.

- **Mardi 9 juin (méta CI/CD)** — **2 PRs Dependabot bundler mergées** : PR #85 `image_processing 2.0.1 → 2.0.2` + PR #86 `bootsnap 1.24.5 → 1.24.6`. Workflow auto-fix lockfile CHECKSUMS (PR #66 sem 23) avait bien tourné dès l'ouverture des PRs (8 juin 19:36) mais la CI principale n'avait pas retriggered sur les fix commits (sécurité anti-loop GitHub sur push depuis `GITHUB_TOKEN`). Solution = close+reopen manuel pour forcer la CI sur HEAD (= le commit auto-fix). 5/5 checks SUCCESS sur les 2 PRs après reopen. Merge squash + delete-branch sur PR #85 OK direct. PR #86 a échoué la 1ère fois au merge (`mergeStateStatus: BEHIND` — main avait avancé suite au merge #85, branch protection rule exige "branche à jour"). `gh pr update-branch 86` (merge main into branch) → nouvelle CI re-déclenchée → 5/5 SUCCESS → merge squash OK. main HEAD final : `774d3bc`. Pull local + `bundle install` requis pour synchroniser les nouvelles versions de gems en dev. **Dette technique notée** : friction récurrente close+reopen+update-branch manuel à chaque Dependabot bundler. 3 solutions long terme à évaluer (PAT au lieu de `GITHUB_TOKEN` dans auto-fix workflow ; `workflow_dispatch`/`repository_dispatch` re-trigger ; Auto-merge GitHub native).

- **Mardi 9 juin (après-midi, Claude Code CLI)** — **Recherche live par nom sur l'index garments, PR à venir (en cours par Chris)**. Mode coach pédago strict. Empile 3 technos Hotwire en une seule feature : back (5e tamis `GarmentFilter#by_search`), front (controller Stimulus debounce), Turbo Frame (rafraîchissement grille). Spec verbale en chat (pas de doc séparée).
  - **Back** : `:search` ajouté à `FILTERS` + méthode `by_search(relation, value)` avec `ILIKE` + `ActiveRecord::Base.sanitize_sql_like` + paramètre lié `?`. Open/Closed Principle **prouvé une 2e fois en live** (2 lignes ajoutées : constante + méthode, zéro touche `apply`/`results`/`initialize`/`slice`). Le piège du `%` décortiqué : sans échappement, taper `%` produit `WHERE name ILIKE '%%%'` = match toute la garde-robe (aberration UX, pas faille de sécurité). **2 mécanismes anti-attaque distincts** : paramètre lié `?` (anti-injection SQL) + `sanitize_sql_like` (anti-aberration UX). Strong params controller : `:search` ajouté au `permit` (défense en profondeur préservée). Détail + 4 questions style entretien 90 sec → [[garment-filter-search-extension-ilike-sanitize]].
  - **Front Stimulus + Turbo Frame** : controller `search_form_controller.js` avec **debounce 300ms paramétrable via `static values`** + nettoyage timer dans `disconnect()` (anti timer fantôme — exactement le pattern `content_loader` du matin). ⭐ **Leçon UX majeure** : **form HORS de la frame** pour préserver le focus pendant le debounce (form DANS = focus perdu à chaque swap). Trade-off documenté + alternative Turbo 8 morphing reportée. ⭐ **Concept central débloqué** — les 3 catégories de "trucs" qu'un controller peut stocker : `targets` (DOM, déclaré pour scan Stimulus), `values` (HTML config, déclaré pour cast + accesseur), **propriété JS classique** `this.xxx` (état interne pur, JS dynamique, aucune déclaration — comme `this.timeout` ici ou `this.select` dans Tom Select). Tableau de décision réutilisable. **`requestSubmit` vs `submit` DOM** : avec Turbo, toujours `requestSubmit` (déclenche l'événement `submit` que Turbo écoute). Hidden inputs pour préserver les filtres actifs en GET (form GET sérialise tous ses champs visibles + cachés). **Convention de nommage Stimulus actée** : `scheduleSubmit`/`triggerX`/`toggleY`, jamais shadow d'API DOM (`submit`/`click`/`focus`). 12 pièges gravés + 4 questions style entretien → [[stimulus-debounce-and-turbo-frame-focus-preservation]].
  - **2 bugs vécus en live et corrigés** : (1) signature `by_search(value)` au lieu de `(relation, value)` + `@scope` au lieu de `relation` → `ArgumentError` au runtime + casse la pile de tamis sur la dimension search (ma faute de relecture, règle 6 CLAUDE.md global re-violée — actée pour la suite : relire le code proposé dans le contexte du fichier que je viens d'afficher) ; (2) form DANS la frame → focus perdu à chaque debounce (ma faute d'archi initiale, j'avais sous-estimé le coût UX). Corrections faites par Chris après diagnostic, tout marche en manuel sur localhost après corrections.
  - **Découverte isolation user en live** : Chris a tapé `Garment.where("name ILIKE ?", "%re%").pluck(:name)` en console → retour `["chemise légère blanche"]` (qui matche "re" dans "légère"), mais cette pièce n'apparaît pas dans son index. Explication : la console cherche dans **toute** la table `garments`, l'index est scopé `current_user.garments`. La chemise appartient à un autre user (seed/ancien compte). C'est l'isolation user qui marche correctement — preuve en live de la propriété de sécurité cross-user testée explicitement dans `garment_filter_test.rb` (cas 10, PR #81).
  - **Backlog `FEATURES_FUTURES.md` mis à jour** : (a) **Full-text search `pg_search`** (entrée existante enrichie avec contexte aujourd'hui + rejet explicite de la search multi-colonnes naïve pour ambiguïté UX) ; (b) **Sidebar filtres togglable** (nouvelle entrée, priorité moyenne, post-emploi — Chris a qualifié le bandeau de pills horizontal d'"horrible", veut basculer vers une sidebar type Zalando/Asos/Uniqlo togglable type drawer responsive). Stack Stimulus avancé (multi-targets + values + localStorage) + Tailwind transitions/responsive.
  - **Reste à faire en fin d'aprem** : (a) tests Minitest pour `by_search` (3 cas à ajouter dans `garment_filter_test.rb`) ; (b) polish navbar/footer avec design system typo (Playfair + Inter) ; (c) brainstorm UX sur 4 points (selector couleurs comme catégories, ajout catégories dresses/suits/swimwear, preview photo en `new`, suggestions tags existants).
  - **Reports** : exo dirigé soir sandbox + reco 2 vidéos fin de journée à caler en fin de session.

- **Mardi 9 juin (après-midi partie 2, Claude Code CLI)** — **Brainstorm UX 4 sub-projects + livraison 2 sur 4, PR #91 mergée**. Brainstorming structuré via skill `superpowers:brainstorming`. Mode coach pédago strict, vulgarisation forcée à plusieurs reprises (Chris a re-flaggé "tu n'as pas vulgarisé" à 2 occasions sur Stimulus + Tom Select → reset complet et reprise ligne par ligne).
  - **Sub-project #1 ✅ ajout catégories** Dress/Suit/Swimwear via `db/seeds.rb`. Trivial (5 min) + piège typo vécu (`poistion: 8` au lieu de `position: 8` → `RecordInvalid` au seed).
  - **Sub-project #2 ✅ color selector Tom Select** (spec + plan rédigés via skill `writing-plans`, 4 tâches TDD livrées). Constante `Garment::COLOR_HEX` (13 entrées Tailwind v4 shade-500 + cream HTML) + test invariant Minitest `COLORS.sort == COLOR_HEX.keys.sort`. Stimulus controller dédié `color_select_controller.js` (séparé de `select_controller.js` Outfit). `f.select` à 4 arguments avec 3-tuples `[label, value, { "data-hex": ... }]` pour injecter data-attributes par option. Discipline TDD Red→Green explicite (porte verrouillée → clé → porte ouverte). **Stack 4 couches** : Rails ERB + Stimulus + Tom Select + CSS centralisé `.ts-control` dans `application.css`. ⭐ **Le bridge data-attributes → JS data object** : Tom Select v2 strippe le préfixe `data-`, donc `data-hex` HTML → `data.hex` JS (vu en live via `console.log`). ⭐ **2 pièges majeurs vécus** : (a) `#` manquant dans `COLOR_HEX` → CSS invalide → pastilles toutes grises ; (b) classes Tailwind sur le `<select>` source → Tom Select les copie sur `.ts-wrapper` → double encadré avec `.ts-control` centralisé. Fix = pas de classes sur `<select>`, tout via `.ts-control`. 4 typos Ruby/Tailwind aussi vécues (`c.capitalized` au lieu de `c.capitalize`, virgule au lieu de `:` dans hash, `fovus:` au lieu de `focus:`). Détail + 4 Q/R entretien → [[tom-select-render-customization-color-selector]].
  - **Sub-projects #3 (photo preview new) + #4 (tag autocomplete) REPORTÉS** : option A choisie dans le brainstorming priorisation (quick wins d'abord + specs pour le reste un autre jour). Specs photo preview + tag autocomplete à brainstormer plus tard.
  - **Backlog `FEATURES_FUTURES.md` enrichi** au début de l'aprem partie 1 : `pg_search` + sidebar filtres togglable. À noter pour la sem 25 (avec features sociales).
  - **Specs + plans rédigés** : `docs/superpowers/specs/2026-06-09-color-selector-tom-select-design.md` + `docs/superpowers/plans/2026-06-09-color-selector-tom-select.md`. Commités sur la branche `feat/categories-colorpicker` avant l'implémentation.
  - **Lint CI échec rattrapé** : Rubocop `Layout/SpaceInsideHashLiteralBraces` sur `db/seeds.rb:20` (Swimwear hash sans espace avant `}`). Fix via `bundle exec rubocop -a` + commit `style:` + re-push → lint vert → merge. Friction CI à noter : prendre le réflexe `bundle exec rubocop` localement avant chaque commit, OU installer un pre-commit hook.
  - **Reports VERS MERCREDI 10 juin** : (a) **tests Minitest `by_search`** (3 cas dans `garment_filter_test.rb`, ~15-20 min — match partiel ILIKE, combo avec color, échappement du `%`) ; (b) **polish navbar/footer typo design system Playfair+Inter** (~1h-1h30) ; (c) **cleanup SUIVI hebdo** différé à demain matin au démarrage de session — condenser sem 23 vers `~/Obsidian/DevJobReady/Suivi/2026-W23.md` + résumé highlight dans SUIVI-COURANT + codifier la routine rolling lundi dans `CLAUDE.md` projet étape 7 démarrage de session (cible : SUIVI-COURANT 54 k → ~25 k chars).
  - **Reports VERS SAMEDI 13 juin (exos dirigés cumulés)** : exo dirigé soir mardi 9 juin **confirmé non fait** → reporté samedi. **Spec succincte exo Stimulus debounce** (pour retrouver demain/samedi) : repo `~/code/sandbox/stimulus/`, nouveau controller `debounced_search_controller.js` + bloc HTML standalone (input + ul vide), comportement = debounce 300ms paramétrable via `static values = { debounceMs: Number }`, target `input` + `results`, propriété d'instance pure `this.timeout` (pas `values`, pas `targets`), méthode `scheduleAppend(event)` (jamais `submit` — collision API DOM, leçon mardi aprem), lifecycle `disconnect()` obligatoire avec `clearTimeout(this.timeout)`. Cas limite pédagogique à reproduire : coder d'abord SANS `clearTimeout` dans `disconnect()`, puis dans la console JS `document.querySelector('[data-controller]').removeAttribute('data-controller')` + 1 dernière frappe juste avant → `<li>` apparaît après déconnexion = preuve du timer fantôme, fix → silence radio. Question entretien 90 sec en clôture : "Tu codes un autocomplete Stimulus qui hit ton API toutes les frappes. Tu mets quel debounce ? Justifie. Et pourquoi le `clearTimeout(this.timeout)` dans `disconnect()` est non-négociable en prod ?". Commit cible : `exo(debounce): debounced search with values, target lifecycle and timer cleanup`.
  - **Reco 2 vidéos fin de journée** ✅ sélectionnées par Claude Code CLI (WebFetch GoRails vérifiée — search épisode KO 520, fallback par series Stimulus JS + Hotwire) et **visionnées en autonomie par Chris ce soir** (accès Pro). Notes Obsidian à rédiger demain matin en rétroactif comme pour les 3 vidéos lundi soir sem 24.
    - **[Auto-submitting Forms & Custom Turbo Stream Actions](https://gorails.com/episodes/custom-turbo-stream-actions-and-auto-submitting-forms)** (Chris Oliver, 27 mars 2023, 10 min, Pro) — **backward** : couvre le pattern auto-submit form on input change + Turbo Streams pour rafraîchir la liste = squelette exact du `search_form_controller` + `turbo_frame_tag "garments_list"` codé mardi aprem. Source code dispo : https://github.com/gorails-screencasts/487-auto-submitting-forms. ⚠️ vidéo > 2 ans, à vérifier que Turbo 8 morphing ne change rien au pattern `form > input + Stimulus listener + requestSubmit` (a priori intact, mais confirmer en notes Obsidian).
    - **[How to use Stimulus JS 2.0's new Values and CSS Classes APIs](https://gorails.com/episodes/stimulus-js-2)** (Chris Oliver, 7 déc 2020, 10 min, Pro) — **backward** : couvre l'API `static values` (typage Number/String/Boolean + callback `xxxValueChanged`) utilisée mardi matin (Odin chap. 37, slideshow v2 réactif sandbox) et mardi aprem (`debounceMs: Number` dans `search_form_controller`). ⚠️ vidéo > 5 ans, flag fort : l'API Values **est restée stable** Stimulus 2 → 3, le code de l'épisode reste valide. Si Chris Oliver montre du Webpacker / Rails 6, transposer vers importmap Rails 8.
  - **3 fautes de relecture méta côté Claude** actées dans Blocages : `data-turbo-frame` redondant, signature `by_search`, form DANS la frame. + 2 nouvelles : (a) vulgarisation insuffisante 2x sur Stimulus/Tom Select (reset forcé), (b) faute pédagogique de nommage `submit` Stimulus en collision avec `form.submit()` DOM (renommé `scheduleSubmit`). Règles mémoire existantes `feedback-vulgarize-advanced-rails` + `feedback-explain-every-line-and-context` à appliquer plus strictement.

- **Mercredi 10 juin (démarrage de session, Claude app)** — **Refonte stratégique cleanup SUIVI hebdo + architecture `@import` au démarrage CLI.** Brainstorm structuré (skill `superpowers:brainstorming`) sur 3 problèmes liés : (1) SUIVI à 57 k chars chargés en `@import` auto → friction perception "doc trop lourd au démarrage CLI", (2) sem 23 non condensée violait la politique de verbosité, (3) pas de routine de condensation hebdo codifiée. **3 itérations de design** : option F (archive + condensation) → option B+C revisée → option finale "retirer SUIVI du @import auto + Read étape 0". **Livré** : (a) **cleanup matin sem 23** — copie verbatim vers `docs/planning/SUIVI/ARCHIVE/2026-06.md` créé + condensation sem 23 dans SUIVI-COURANT selon règle préservation (livraisons clés, décisions de design tranchées, concepts maîtrisés en live, friction/blocages, reports backlog) + suppression pavés techniques (vivent dans notes Obsidian par concept). 57k → 41k chars. (b) **Routine de condensation hebdo codifiée** dans `CLAUDE.md` projet étape 7 (lundi rolling, cible ≤ 8 k chars condensé par semaine, audit avant exécution). (c) **Refonte `@import` auto** : `@docs/planning/SUIVI/SUIVI-COURANT.md` retiré du `@import` auto dans CLAUDE.md projet + nouvelle **étape 0 obligatoire** dans la routine "Démarrage de session" (Read SUIVI avant tout, sans quoi étapes 5/6/7 inexécutables ; Read à la demande pour sessions non rituelles). **Bilan tokens** : `@imports` auto 22 k → ~12 k tokens (−45 %), sub-agents allégés du même montant. `phase-1.md` gardé en `@import` (vision long terme planning, perdre Read à la demande non viable). **Méta-pattern méthodo** : Claude a 3 fois sur-optimisé pour la métrique tokens avant que Chris recadre sur objectif réel ("Claude doit toujours avoir le contexte + Chris doit toujours accéder à ses notes + zéro bruit"). Reconnaissance d'erreur explicite + révision honnête à chaque tour. Règle 1 CLAUDE.md global (challenge sans flatterie, dire "je me suis trompé") appliquée.

- **Mercredi 10 juin (matin, Claude app)** — Odin chap. **38. Mailers** en lecture profonde + Rails Guides 8.1 Action Mailer Basics (sections 1-2, 6-7, 8.2, 9-10 selon reco Odin). Couvert : **mailer = hybride controller + model** (`rails g mailer User` génère mailer + views + tests + previews) ; **pattern moderne `Mailer.with(user: @user).action.deliver_later`** vs ancien `Mailer.action(@user)` (sérialisation GlobalID compatible Active Job + Solid Queue Rails 8 vs sérialisation fragile d'instance complète) ; **vues `app/views/<mailer>/<action>.html.erb` + `.text.erb`** (multipart MIME auto, anti-spam) **vs layout `app/views/layouts/mailer.html.erb`** (piège VIEW vs LAYOUT loupé en KC q2) ; **`deliver_later` (Active Job Solid Queue) vs `deliver_now` (cron/test/console)** ; **les 2 familles de callbacks** — `*_action` (variable `mail`, avant/après construction email : hydrater @vars, override SMTP settings runtime multi-tenant, `perform_deliveries = false` killswitch, headers custom) vs `*_deliver` (variable `message`, avant/après envoi SMTP : sandbox staging `message.to =`, `throw :abort` désabonnement, audit log `EmailDelivery.log`, `.touch(:delivered_at)` timestamp) ; **pipeline complet** schématisé (before_action → action+view → after_action → before_deliver → SMTP → after_deliver) ; **`attachments.inline[..]` vs `attachments[..]`** + cas N to F variants WebP Active Storage (`.processed` mandatory avant `.download`) ; **`*_url` mandatory dans vues** (jamais `*_path` car emails hors contexte HTTP) + config `default_url_options host:` ; **2 contraintes styling HTML emails** : pas de stylesheet externe (inline ou `<style>` tag, gem `premailer-rails`), support CSS limité Outlook = retour aux `<table>` (frameworks pro MJML, Foundation for Emails) ; **`.touch(:column)`** : 1 UPDATE SQL direct + skip validations + `before/after_save` + déclenche `after_touch` + `after_commit` (lien direct [[callbacks]] chap. 27 mardi 2 juin) ; **3 mécaniques d'abort** distinctes (`mail.perform_deliveries = false`, `throw :abort` dans before_deliver, `body = "..."` dans before_action) ; **Interceptors vs Observers** (global DRY mais opaque vs local explicite) ; **Mailer Preview** (rendu statique, design itératif) vs **Letter Opener** (intercepte deliver_now, flow réel). ⭐ **Lien retro chap. 27 Callbacks** : combo `after_save` + `deliver_later` + transaction rollback = job orphelin + `RecordNotFound` worker crash → **toujours `after_commit on: :create`** pour side-effects externes (mailer/job/webhook/broadcast). **Knowledge Check Odin (6) répondues par écrit** par Chris : 3 correctes (1, 5 partielle), 2 confusions importantes (4 : use cases `after_action`/`before_deliver` mélangés, "feedback" mal précisé pour after_deliver), 1 faute structurelle (2 : vue vs layout), 1 style obsolète (3 : ancien pattern `Mailer.action(arg)` au lieu de `with(...)`), 1 zappée (6 : contraintes styling). Toutes corrigées en chat. ⭐ **3 questions style entretien 90 sec rédigées par Claude (à la demande de Chris)** : (1) `deliver_later` + `after_save` vs `after_commit` dans transaction rollback (trace complète + fix + bonus `after_save_commit` alias) ; (2) Active Storage en inline dans mail récap N to F (code complet + 3 pièges `.inline` vs pièce jointe / performance `deliver_later` / `.processed` mandatory + trade-off inline vs URL externe) ; (3) Mailer Preview vs Letter Opener (duo complémentaire, Preview = itérer design / Letter Opener = vérifier flow + follow-up `assert_emails` + multi-states + délivrabilité réelle via webhooks). **Décision tranchée pédago** : pas de config Action Mailer dans Nine to Fine ce matin (4 raisons : viole ritual matin = lecture profonde sans code, YAGNI strict, premature config qui sera refaite sem 25, risque oubli contexte dans 5 jours). Setup repoussé à sem 25 (1er mailer follow notif). 2 notes Obsidian rédigées : [[mailers-fundamentals]] (Apprentissage Rails Mailers Jobs Realtime, 17 sections + 12 pièges + 6 KC + lien entretien) + [[../../../Entretiens/Rails/mailers]] (3 questions 90 sec format Q/Réponse/Pièges/Follow-up). Index Rails Obsidian mis à jour avec section "Action Mailer" dans Mailers, Jobs & Realtime (entrées détaillées) + sous-sections Active Job + Action Cable flaggées "à venir". 12 pièges gravés.

- **Mercredi 10 juin (avant-midi, Claude app)** — Tailwind docs section **Transforms** en lecture profonde (translate + rotate + scale + skew + transform-origin + intro 3D context perspective/transform-3d, ~6 pages doc v4 fetchées en parallèle). Couvert : **4 opérations 2D** (translate déplace, rotate tourne, scale agrandit, skew incline) + transform-origin pivot + 3D context. **Sémantiques différentes pour le même `N`** : `translate-N` = spacing scale (`--spacing × N`, default 4px), `rotate-N` = degrés, `scale-N` = pourcentage (`scale-110` = 110%), `skew-N` = degrés. **Translate** : axes X/Y/Z, fractions = % de la taille de l'élément (pattern centrage absolu `left-1/2 -translate-x-1/2`), `translate-full` 100%, `translate-px` 1px. **Rotate** : degrés défaut, arbitrary radians OK, 3D `rotate-x/y/z`. **Scale** : presets 0/50/75/90/95/100/105/110/125/150/200, **`-scale-x-100` = flip horizontal idiome icônes**, pas de layout shift (avantage perf + piège clipping/overlap). **⭐ LE mécanisme clé v4 — composition via CSS variables internes** : chaque utility écrit dans SA variable (`--tw-translate-x`, `--tw-rotate-z`, `--tw-scale-x`, etc.), une règle racine compose le `transform` final. Conséquence : on empile N transforms sans écrasement (en CSS pur, `transform: rotate()` puis `transform: scale()` s'écraseraient — la 2e tue la 1ère). Nouveau et fiable en v4 (vs v3 nécessitait classe `transform` explicite oubliée). **3D context** : 3 ingrédients (`perspective-*` parent + `transform-3d` parent + transform 3D sur l'enfant), sinon aucun effet visible. Analogie caméra qui regarde la scène. **Performance** : rendering pipeline du navigateur — transform + opacity = composite GPU only = 60 fps mobile ; width/height/margin/padding = layout + paint + composite = saccades 30 fps. JAMAIS animer width pour entrées/sorties d'éléments. **Skew rare en prod web sérieuse** (badges en biais, effets éditoriaux), à ranger comme "outil dispo". **Application N to F immédiate** : 5 patterns concrets — card lift index garments (`relative + hover:scale-105 + hover:z-10 + transition-transform`), pills filtre lift (`-translate-y-0.5 + shadow + transition-[transform,box-shadow]`), like button press (`active:scale-95`), flip d'icône expand/collapse dropdown (`rotate-180`), modal entrance (`opacity-0 + scale-95` → `opacity-100 + scale-100` avec `transition-all` toléré pour 2 props GPU). ⭐ **3 questions style entretien 90 sec rédigées par Claude (à la demande de Chris)** : (1) hover scale sur card + clipping/overlap + z-index (`relative + hover:z-10` mandatory + raison technique) ; (2) combiner rotate + scale au hover (mécanisme CSS variables Tailwind v4 vs CSS pur écrase) ; (3) modal entrance scale + opacity + pourquoi PAS width/height (rendering pipeline GPU vs layout reflow, `transition-all` exception légitime). **12 pièges gravés** dont scale layout shift + clipping + overlap z-index, `transition-all` code smell sauf exception modal, jamais animer w/h/margin/padding, sémantiques différentes N par utility, fractions = % de l'élément (pas du parent), 3D sans perspective parent = aucun effet, transform-origin default center, `-scale-x-100` flip idiome. Note Obsidian rédigée : [[transforms-fundamentals]] (Apprentissage Tailwind/Transforms/, 12 sections + 3 questions entretien intégrées + lien rétroactif [[transitions-and-animations-fundamentals]] et [[backgrounds-borders-effects-v4]]). Index Tailwind Obsidian mis à jour (section Transforms remplie + ordre de lecture étape 12 ✅).

- **Mercredi 10 juin (après-midi, Claude Code CLI)** — **3 chantiers livrés, 2 PR mergées + 3 notes Obsidian** (mode coach pédago strict). Mode coach pédago à RE-flagger 3 fois en session (squelettes nus pour by_search cas 1-3 + question "2 candidats" floue + bloc 12 `it` balancé sans coaching pour la migration RSpec) — règle mémoire `feedback-explain-every-line-and-context` existe depuis sem 22, à RE-appliquer plus strictement.

  - **Chantier 1 — 3 tests by_search Minitest (PR #95 mergée)** : 3 cas dans `test/queries/garment_filter_test.rb` (a) `"results filters by search with partial ILIKE match"` (valeur `shirt`, prouve matching partiel + insensible casse) ; (b) `"results filters by search and by color with AND"` (valeur `jean` + `blue`, prouve participation `by_search` au pipeline `inject`) ; (c) `"results escapes % wildcard in search to prevent matching everything"` (valeur `%`, prouve justification ligne `sanitize_sql_like`). Choix `refute_equal @chris_scope.pluck(:id).sort, result.pluck(:id).sort` plutôt que `assert_empty` pour robustesse vs évolution future des fixtures. 1 piège vécu : Chris avait écrit `garments(:blue_jean)` au lieu de `garments(:blue_jeans)` (typo). 13 runs, 0 failure côté Minitest. Solde la dette de PR #83.

  - **Chantier 2 — Design system typo Tailwind v4 (PR #95 mergée)** : 1ère application en prod du design system construit lundi 8 juin matin (cf. [[typography-fundamentals]]). 3 sous-étapes : (a) imports Google Fonts AVANT `@import "tailwindcss"` (piège ordre vu lundi — sinon `@font-face` arrivent trop tard) ; (b) ajout `--font-display: "Playfair Display", serif` + `--font-body: "Inter", system-ui, sans-serif` dans `@theme` existant ; (c) 5 tailles sémantiques bundlées `--text-h1/h2/h3/body/caption` avec sous-propriétés `--line-height/--letter-spacing/--font-weight` (échelle géométrique × 1.5, 12px → 48px). Application navbar : `text-lg font-bold` → `text-h2 font-display` sur Closet/Dressing, `text-sm font-medium` → `text-caption` sur user/logout. 1 typo vécue : `sasn-serif` (navigateur ignore silencieusement les valeurs CSS inconnues). ⭐ ⚠️ **LE PIÈGE MOBILE-FIRST flaggé par Chris en live** : valeurs canoniques pro calibrées **desktop-first** (text-h2 30px / text-caption 12px) → navbar cassée sur app mobile-first Nine to Fine (Closet/Dressing énorme, logout/username en dessous seuil accessibilité tactile Apple HIG 17px / Material 14px). Mémoire `project-nine-to-fine-mobile-first` créée dans `~/.claude/projects/.../memory/` pour toutes sessions futures. Note Obsidian : [[v4-design-system-and-mobile-first-pitfall]] avec 8 pièges. Échelle desktop laissée en l'état, refonte mobile-first reportée à fin d'aprem (chantier 4 si temps).

  - **Chantier 3 — Setup RSpec + factory_bot + migration garment_filter (PR à venir)** : Migration totale décidée par Chris après brainstorm 4 options (factories cohabitent avec fixtures vs migration totale). **Setup complet** : ajout `rspec-rails ~> 8.0` + `factory_bot_rails ~> 6.5` dans `:development, :test`, `bin/rails g rspec:install` (3 fichiers `.rspec`/`spec_helper.rb`/`rails_helper.rb`), `bundle binstubs rspec-core` (génère `bin/rspec` qui n'est pas auto-créé), config `config.include FactoryBot::Syntax::Methods` dans `rails_helper.rb`, suppression de `config.fixture_paths = [...]` (signal migration totale). **5 factories créées** dans `spec/factories/` : `user.rb` (sequence email + username + password fixe), `category.rb` (sequence name + position), `tag.rb` (1ère factory avec association `user` magique = équivalent `association :user`), `garment.rb` (2 associations user + category, valeur fixe `color { "black" }` car pas d'unicité), `tagging.rb` (polymorphique : `taggable { create(:garment) }` lazy explicite parce que factory_bot ne devine pas la classe). **1 fichier migré en live coach** : `test/queries/garment_filter_test.rb` → `spec/queries/garment_filter_spec.rb` (13 examples verts). Mapping syntaxe Minitest → RSpec capté en live : `assert_includes` → `expect().to include`, `refute_includes` → `expect().not_to include`, `assert_equal expected, actual` → `expect(actual).to eq(expected)` (⚠️ ordre inversé), `setup do` → `before do` ou `let`, fixtures `garments(:foo)` → factories `let!(:foo) { create(:garment, ...) }`. Concept `let` vs `let!` (lazy memoized vs eager — `let!` nécessaire quand record doit persister sans être nommé dans le spec, ex : "scope unchanged when params empty" qui ne nomme jamais `black_tshirt`). 13 fails sur 1er run cause **DB de test polluée par fixtures Minitest** résiduelles ("Username has already been taken") → fix `bin/rails db:test:prepare` (purge + recharge schema). Suppression de `test/queries/garment_filter_test.rb` après migration verte. **CI workflow adapté** : job `test` lance maintenant `bin/rails db:test:prepare && bin/rails test && bin/rspec` (séquence pendant la transition). 2 notes Obsidian rédigées : [[rspec-and-factory-bot-setup]] (Big Picture setup + 5 factories + let/let!) + [[minitest-to-rspec-migration-patterns]] (tableau mapping + workflow + estimation par fichier pour les 11 restants à migrer en autonomie jeudi matin). Index Rails Testing mis à jour avec les 2 nouvelles entrées.

  - **Chantier 4 — Refonte mobile-first des tokens typo (PR #98 mergée)** : tokens `--text-X` du `@theme` revus mobile-first par défaut (sans media queries qui ne sont pas supportées par `@theme` v4). Valeurs : `--text-h1` 48 → 32px, `--text-h2` 30 → 20px, `--text-h3` 20 → 18px, `--text-body` inchangé (16px = norme web universelle WCAG AA min), `--text-caption` 12 → 14px (au-dessus du seuil min Material Design d'accessibilité tactile). Sous-propriétés (line-height, letter-spacing) ajustées légèrement pour préserver lisibilité mobile. H1 des pages laissés intacts (responsive scale-up `md:text-*xl` conservé par décision Chris — refus de perdre travail responsive déjà fait). **Footer abandonné** après réflexion sur usage moderne : apps mobile-first (Instagram, X, TikTok, Spotify) n'ont pas de footer global dans le layout. Le footer est convention "site marketing web" pas "app". Si mentions légales B2C nécessaires plus tard, à créer dans `pages/home.html.erb` only (pas dans le layout). Décision actée comme finale, pas de report.

  - **Méta workflow** : Chris a établi le programme aprem clair en 4 séquences (commit/push/PR/merge/pull entre chaque). Routine end-of-day déclenchée par Chris avant chantier 4 pour éviter de tout cumuler en clôture (notes Obsidian + SUIVI MAJ chantiers 1-3 dans PR #97). Routine de clôture finale (chantier 4 + footer décision) dans PR à venir #99.

  - **⚠️ Méta erreurs Claude session (acté par Chris en clôture)** : (a) squelettes nus 3 fois (by_search cas 1-3 + 12 `it` RSpec en bloc) — règle mémoire `feedback-explain-every-line-and-context` existe sem 22, à RE-appliquer même sous pression de temps ; (b) calibrage typo desktop-first sur app mobile-first sans poser la question contexte → nouvelle memory `project-nine-to-fine-mobile-first` créée ; (c) typo `"tshi"` proposée pour matcher `Black T-shirt` mais ne matche pas à cause du tiret — faute de relecture (règle 6 globale "Relis-toi avant de proposer" à RE-appliquer) ; (d) confusion `test/queries` vs `spec/queries` dans rédaction (file path ambigu) ; (e) footer poussé par inertie sem 24 du SUIVI au lieu de questionner pertinence ; (f) proposition de refonte des h1 qui aurait fait perdre le travail responsive déjà mis en place. Patterns dominants : shortcut pédagogique sous pression de temps + récitation de pattern sans relecture du contexte affiché. Pas de nouvelle règle mémoire à créer (les existantes couvrent), application plus stricte à venir.

  - **Reportés vers JEUDI 11 juin** : (a) migration des 11 fichiers Minitest restants en autonomie (7 models + 3 controllers + 1 outfit_garment, ~1h45-2h30, pattern documenté dans [[minitest-to-rspec-migration-patterns]]) ; (b) suppression dossier `test/` après migration complète + retrait de `bin/rails test` du CI workflow.

  - **Reportés vers SAMEDI 13 juin (exos dirigés cumulés)** : exo dirigé soir lundi + mardi + mercredi non faits → reportés samedi. **Spec succincte exo Stimulus debounce** (pour retrouver demain/samedi) : repo `~/code/sandbox/stimulus/`, nouveau controller `debounced_search_controller.js` + bloc HTML standalone, debounce 300ms paramétrable via `static values`, propriété d'instance pure `this.timeout`, lifecycle `disconnect()` obligatoire avec `clearTimeout(this.timeout)`, cas limite pédagogique du timer fantôme à reproduire.

- **Clarté + vulgarisation insuffisante** côté Claude pendant le slot matin Turbo (2 reprises de Chris : "tes explications ne sont pas du tout claires ni vulgarisées", flaggée également sur question Tailwind Q2 "je ne comprends pas ce que tu me demandes"). Les règles mémoire `feedback-vulgarize-advanced-rails.md` et `feedback-concrete-verification-questions.md` **existent déjà** (sem 22 et sem 23), Claude a re-rechuté → pas de nouvelle règle à créer, à appliquer plus strictement. Pattern à surveiller : Tailwind v4 (concepts CSS-first + `@theme` + namespaces) tombe aussi dans la catégorie "avancé sous le capot" qui exige analogie concrète d'abord, jargon après.

- **Manque de relecture du code proposé côté Claude pendant l'aprem** (3 occurrences) : (1) `data-turbo-frame="garments_list"` redondant ajouté au form_with alors que le form était DANS la frame (Chris a flaggé "comment t'as pu te louper alors que tu avais le contexte" — vraie question légitime) ; (2) signature `by_search(value)` au lieu de `(relation, value)` sans relecture du pattern existant des autres méthodes `by_X` ; (3) form DANS la frame → focus perdu pendant le debounce, sous-estimation du trade-off UX. **Cause racine** : récitation de pattern sans relecture du code proposé dans le contexte du fichier que je viens d'afficher. Pas de nouvelle règle mémoire (la règle 6 CLAUDE.md global "Relis-toi avant de proposer" suffit), à appliquer plus strictement. Engagement pris : avant chaque bloc de code proposé, relecture mentale contre l'extrait du fichier que je viens d'afficher + chercher redondances/conflits + vérifier que je cite la doc et pas la mémoire sur les comportements par défaut framework.
