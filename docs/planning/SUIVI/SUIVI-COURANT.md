# Suivi vivant — Sem en cours + précédente

Journal de bord récent. À mettre à jour à la fin de chaque journée via la routine `/end-of-day` (à créer sem 22+).

**Politique de rolling** : ce fichier contient **la semaine en cours + la semaine précédente complète**. Début de mois (au plus tard le premier lundi du mois suivant), archivage de tout le mois précédent vers `ARCHIVE/{YYYY-MM}.md`.

**Politique de verbosité** : journal **condensé**. Les pavés techniques (pièges, concepts maîtrisés, choix de design, idiomes Rails 8, classes Tailwind) **vivent dans les notes Obsidian** (`~/Obsidian/DevJobReady/Apprentissage/`). Le Suivi les **référence par lien**, ne les duplique pas. Le backlog forward-looking (exos en attente, dettes, brainstorms reportés, chantiers coupables) vit dans `~/Obsidian/DevJobReady/to-do.md`.

**Règle de lecture au démarrage de session** :
- Mardi-vendredi : focus journée veille + sem en cours.
- Lundi (ou 1ère session de la sem) : lecture intégrale sem précédente + sem en cours qui démarre, avec récap accomplissements + blocages + ajustements de la sem précédente avant le programme du jour.

**Archives** : voir `docs/planning/SUIVI/ARCHIVE/{YYYY-MM}.md`.

---

### Sem 23 (1-7 juin 2026) — condensée

**Archive verbatim** : voir [`ARCHIVE/2026-06.md`](ARCHIVE/2026-06.md) (section sem 23, jour par jour).

**Livraisons clés**

- **CRUD Outfit + composition** (PR #53/#54/#55, lun 1er juin) : controller 7 actions + setter `garment_ids=` (jointure pure, pas `fields_for`) + `validates :garments, presence: true` + combobox Tom Select multi-select à pills via importmap + Stimulus. Notes : [[has-many-through-composition-and-presence-validation]], [[tom-select-importmap-stimulus]], [[erb-nested-anchor-and-loop-tag-pitfalls]].
- **Active Storage sur Garment** (PR #59 + tests #61, mar 2 juin) : `has_one_attached :photo` + variants WebP + validation `active_storage_validations` (`content_type` + `size` + `spoofing_protection: true`) + swap `mini_magick`→`ruby-vips`. Piège purge `""→DeleteOne` neutralisé. Notes : [[active-storage-fundamentals-and-variants]], [[active-storage-validations-and-pitfalls]], [[object-fit-and-aspect-ratio]], [[minitest-active-storage-validations]].
- **Outfit collage adaptatif** (PR #69, mer 3 juin) : partial unique `_collage` taille-agnostique + eager loading anti-N+1 (`includes(garments: [:category, { photo_attachment: :blob }])`) + mosaïque plafonnée à 4 tuiles + compteur `+N`. Notes : [[eager-loading-sort-by-vs-order-and-nested-attachments]], [[outfit-collage-adaptive-mosaic]].
- **Tags comma-separated** (PR #74, jeu 4 juin) : concern `Taggable` mutualisé Garment+Outfit + attribut virtuel `tag_names` + `after_save :sync_tags` (`find_or_create_by` + downcase) + gem `bullet` (2 N+1 préexistants corrigés). Notes : [[taggable-concern-and-tag_names-virtual-attribute]], [[minitest-taggable-concern]], [[bullet-n+1-detection-and-eager-loading-fixes]].
- **Exo sandbox `auth-from-scratch`** (lun 1er soir, `~/code/sandbox/rails/` commit `f0c3609`) : `has_secure_password` from scratch sans Devise. Note : [[auth-from-scratch-sandbox-exo]] + entretien : [[Entretiens/Rails/auth-from-scratch]].

**Odin Rails (lectures profondes matin)** : chap. 27 Callbacks (mar 2), chap. 30+31 APIs paire (mer 3), chap. 34+35 CSS+JS Bundling paire (jeu 4). Notes : [[callbacks]], [[apis-and-building-your-own]], [[working-with-external-apis]], [[bundling-vs-importmap-rails-8]].

**Tailwind (lectures profondes avant-midi)** : Theme + Functions/Directives (mar 2), Dark mode + Colors palette (mer 3), Backgrounds/Borders/Effects/Opacity (jeu 4). Notes : [[theme-variables-and-directives]], [[dark-mode]], [[colors-palette]], [[backgrounds-borders-effects-v4]].

**CI/CD méta** : PR #65 (routine reco vidéos fin de session activée), PR #66 (workflow auto-fix Dependabot bundler CHECKSUMS — testé en réel sur PR #63 jbuilder + #64 puma ; friction connue : `GITHUB_TOKEN` ne retrigger pas CI → close+reopen manuel), PR #68 (push filter CI `master`→`main`).

**Décisions de design tranchées (ne pas re-proposer)**

- **Position spatial outfit** (drag-drop canvas, hyp. B) : reporté backlog → composition simple V1.
- **Compteur `+N` mosaïque** : non-cliquable V1 (card-lien + pills de `show` couvrent déjà le besoin).
- **API JSON N to F V1** : pas d'API (Hotwire monolith, pas de SPA à servir). Option `/api/v1/` portfolio Phase 4 à trancher.
- **Dark mode V1** : non (photos vêtements = perception colorimétrique correcte requise).
- **Tom Select setup** : pin CDN sur build complete wrappé `+esm` (pas vendoring importmap qui 404).
- **Processeur images Rails 8** : `:vips` (cohérence Dockerfile libvips + `brew install vips` dev).
- **Intégration Anthropic Phase 4** : gem officielle `anthropic-sdk-ruby` + service object `app/services/ai/outfit_suggester.rb` + credentials chiffrées `credentials.yml.enc`.
- **Sélection garments par clic dans l'index** (remplace Tom Select à ~100 pièces) : reporté backlog `FEATURES_FUTURES.md`, post-emploi après index filtrable.

**Concepts maîtrisés en live (ne pas réexpliquer comme débutant)**

- `has_many through:` + setter virtuel `garment_ids=` (jointure pure, pas `accepts_nested_attributes_for` / `fields_for`).
- `validates :garments, presence: true` voit la collection en mémoire avant save (≠ DB).
- Eager loading nested attachments : `photo_attachment: :blob` (proxy), pas `:photo`.
- Concern polymorphique mutualisé (`Taggable` sur Garment+Outfit) avec `as: :taggable`.
- Garde `nil` (préserver) vs `[]` (vider) pour attribut virtuel `tag_names`.
- Minitest 6 (Rails 8.1) a retiré `Object#stub` → stub Ruby pur via `define_singleton_method`.
- `respond_to do |format|` + négociation Accept header + `as_json only:` whiteliste (pas `except:`).
- Active Storage : piège purge `""→DeleteOne` (file_field vide), garde `garment_params` retirant `:photo` blank.
- 3 vidéos GoRails visionnées en autonomie mer soir : [[preserving-file-uploads-on-edit]], [[gorails-importmaps-with-rails]], [[strict-loading-activerecord]].

**Friction / blocages capturés**

- Setup Tom Select / importmap : ~35 min trial-and-error (lun 1er) → règle mémoire `feedback-verify-external-setup`.
- Questions de vérification floues 2× (mar 2 matin) → règle mémoire `feedback-concrete-verification-questions`.
- **Ven 5 + sam 6 décrochés** (fatigue post grosse semaine prod) → 2 soirs sandbox manqués → cumul exos reportés sem 24 (samedi 13 juin rattrapage).

---

### Sem 24 (8-14 juin 2026) — condensée

**Archive verbatim** : voir [`ARCHIVE/2026-06.md`](ARCHIVE/2026-06.md) (section sem 24, jour par jour).

⭐ **Bilan global** : **Phase 1 techniquement bouclée vendredi 12 juin** (9 jours d'avance sur planning). Parcours **Odin Rails ✅** (chap. 1-41), **Phase 1 Tailwind v4 ✅** (étapes 1-14). **113 examples RSpec verts** dans la suite complète (migration totale Minitest → RSpec achevée). **Dette technique sem 22 soldée** (CRUD Garment sans tests le 29 mai → 15 examples + 4 IDOR sentinels). Phase 2 React démarre sem 26 (22 juin) inchangée — timeline emploi septembre intacte.

**Livraisons clés**

- **GarmentFilter Query Object back** (PR #79, lun 8 juin) : pipeline `inject` + dispatch dynamique `send("by_#{key}")` + 4 filtres AND combinables (color/category/tag/brand). OCP prouvé 2× en live (ajout `:brand` proactif sem 24 puis `:search` mardi, zéro modif `apply`). Note : [[query-objects-pipeline-inject-and-dynamic-dispatch]].
- **GarmentFilter tests Minitest** (PR #81, lun 8 juin) : 10 cas incl. ⭐ test propriété sécurité cross-user. Note : [[minitest-garment-filter-query-object]].
- **GarmentFilter front + tags cliquables** (PR #83, lun 8 juin) : constante `Garment::COLORS` + validation inclusion + normalisation données dev (12 valeurs polluées corrigées) + helper `pill_class` + index Turbo Frame `garments_list` + pattern `_top` pour échapper au frame. Notes : [[turbo-frames-in-production-and-content-missing-trap]], [[inclusion-validation-with-allow-blank]].
- **Recherche live ILIKE + Stimulus debounce** (PR #91, mar 9 juin) : 5e tamis `by_search` + `sanitize_sql_like` + Stimulus `search_form_controller` debounce 300ms via `static values`. ⭐ form HORS du frame pour préserver focus. Notes : [[garment-filter-search-extension-ilike-sanitize]], [[stimulus-debounce-and-turbo-frame-focus-preservation]].
- **Catégories Dress/Suit/Swimwear + color selector Tom Select** (PR #91, mar 9 juin) : seeds + constante `Garment::COLOR_HEX` (13 hex) + test invariant statique + Stimulus `color_select_controller.js` avec pastilles colorées + CSS centralisé `.ts-control`. Bridge `data-hex` → `data.hex` (Tom Select strip `data-`). Note : [[tom-select-render-customization-color-selector]].
- **Design system typo Playfair+Inter (desktop puis mobile-first)** (PR #95+98, mer 10 juin) : tokens `--font-display`/`--font-body` + 5 tailles sémantiques `--text-h1/h2/h3/body/caption` bundlées v4. ⭐ piège mobile-first flaggé en live → refonte tokens (h1 32px, h2 20px, caption 14px). Notes : [[v4-design-system-and-mobile-first-pitfall]], [[typography-fundamentals]].
- **RSpec setup + migration totale Minitest** (PR #96+102+106, mer-jeu-ven) : rspec-rails 8 + factory_bot 6.5 + 5 factories + migration 7 models specs (~68 examples) + 3 request specs controllers (30 examples) + cleanup `test/` complet + simplification CI (5 jobs → 4, 2-3 min gagnées par PR). ⭐ **Dette sécurité sem 22 soldée** : `garments_spec.rb` 15 examples + 4 IDOR cross-user sentinels (show/edit/update/destroy). Notes : [[rspec-and-factory-bot-setup]], [[minitest-to-rspec-migration-patterns]], [[factory-bot-advanced-patterns]], [[rspec-active-storage-testing]], [[rspec-taggable-concern-testing]], [[rspec-request-specs-fundamentals]], [[idor-sentinel-pattern-rspec]].
- **Anti-double-submit Stimulus + Tailwind Customization avancé v4** (PR #104, ven 12 juin) : `@custom-variant data-loading` + `@utility btn-base` + Stimulus `submit_button_controller` mutant `data-loading="true"/"false"`. Note : [[customization-advanced-v4]].
- **Investigation warning bullet POST /outfits create** (sam 13 juin matin) : safelist documenté livré. Cause = cascade `active_storage_validations` + `spoofing_protection: true` qui télécharge le blob de chaque Garment associé au save Outfit. Fix idiomatique impossible. Note enrichie : [[active-storage-validations-and-pitfalls]] § 7.

**Odin Rails (parcours TERMINÉ ✅)** : chap. 36 Turbo Frames+Streams (lun) [[turbo-frames-streams-and-format-negotiation]] · chap. 37 Stimulus + 3 exos sandbox (mar, repo `ChrisCroc/sandbox-stimulus` créé) [[stimulus-fundamentals]] · chap. 38 Mailers (mer) [[mailers-fundamentals]] · chap. 40 Advanced Topics 6 sections (jeu : [[advanced-routing-fundamentals]], [[controllers-without-models-and-rest-principles]], [[advanced-layouts-and-content-for]], [[metaprogramming-ruby-and-rails-first-pass]] ⚠️ first-pass, [[solid-design-principles-and-anti-patterns]], [[i18n-internationalization-fundamentals]]) · chap. 41 Websockets+Action Cable + Rails Guide 8.1 (ven) [[action-cable-fundamentals]].

**Tailwind v4 (Phase 1 TERMINÉE ✅)** : Typography (lun) [[typography-fundamentals]] · Transitions & Animation (mar) [[transitions-and-animations-fundamentals]] · Transforms (mer) [[transforms-fundamentals]] · Interactivity 19 utilities (jeu) [[interactivity-fundamentals]] · Customization avancé `@apply`/`@utility`/`@custom-variant` (ven) [[customization-advanced-v4]].

**Décisions de design tranchées (ne pas re-proposer)**

- **Filtres garments combinables AND** (vs un seul à la fois) : URL `?q[color]=red&q[category_id]=3` plat ANDé, objet `q` paramétrisable. Routes nested éliminées (combinatoire ingérable).
- **`GarmentFilter` PORO** (vs ActiveModel) : injecté avec scope + params, méthodes privées `by_X`, 4 filets de défense (scope `current_user.X` + `permit` controller + `slice(*FILTERS)` filter + SQL paramétrisé). Recherche par nom scopée `current_user.garments` mandatory (pas `Garment.where`).
- **`pg_search` reporté backlog** (`FEATURES_FUTURES.md`) + **sidebar filtres togglable** reportée backlog (Chris a qualifié le bandeau pills horizontal d'"horrible").
- **Multi-color par garment reporté backlog** : V1 mono-color. **Normalisation `brand` au save** reportée backlog : callback `before_save :normalize_brand` (`.strip.titleize`).
- **Form HORS du frame pour debounce** : préserver focus pendant les swaps. Alternative Turbo 8 morphing reportée.
- **`data-*` attribute over `.is-loading` classe** : sémantique état machine-lisible + a11y + convention Tailwind v4 `data-[state=open]:` natif. **Controller Stimulus sur `<form>` pas `<button>`** (turbo:submit-end bubble vers le haut). **`@utility btn-base` scope C** (forme + focus ring a11y, peinture en HTML) ouvert OCP futurs `btn-primary`/`btn-danger` via Rule of 3. **Pas de `pointer-events-none`** : Turbo pose déjà `disabled` natif + `pointer-events-none` bloque `cursor-wait`.
- **Migration totale Minitest → RSpec** (vs cohabitation) : factory_bot natif, `let`/`let!`, `type: :request`. Cleanup `test/` complet + `spec/fixtures/files` au défaut RSpec.
- **Footer abandonné** : apps mobile-first modernes n'en ont pas. Mentions légales B2C plus tard dans `pages/home.html.erb` only.
- **Pas de config Action Mailer pré-emptive** (YAGNI) : setup repoussé au 1er mailer (follow notif sem 25+).
- **Pas de setup i18n pré-emptive** (YAGNI + cargo cult + premature optimization, 3 anti-patterns SOLID Section 5) : scope 7 étapes documenté `to-do.md`, trigger explicite = "démo recruteur francophone Paris".
- **Skip tuto Odin "Basic Messaging App"** (vanilla JS pattern legacy 2018-2021, viole règle péda 9) → exo sandbox focused Solid Cable + Turbo Streams + vraie pratique sem 25 sur Likes/Comments broadcast N to F.
- **SuggestionsController dédié dès V1 + futur model `Suggestion` persisté** : ajouté `FEATURES_FUTURES.md` Phase 4 IA (pattern verbe→nom DHH).
- **Safelist bullet documenté pour POST /outfits create** : `Bullet.add_safelist type: :n_plus_one_query, class_name: "Garment", association: :photo_attachment` + `Bullet.stacktrace_includes = [ "app/" ]` permanent.
- **US18 "Make an outfit public" reportée sem 26+** (vs stub `is_public:boolean` lun 15) : brainstorm produit dédié requis sur sémantique "public" (feed/index/URL directe). Likes/Comments/Follow sem 25 testables via RSpec + console sans `is_public`.

**Concepts maîtrisés en live (ne pas réexpliquer)**

- **Turbo Frames** : `turbo_frame_tag @model` via `dom_id` + `data-turbo-frame="_top"` pour sortir + ⭐ piège "Content missing" si link interne ciblant page sans frame matchant.
- **Turbo Streams** : 8 actions + `target`/`targets` + templates `.turbo_stream.erb` + broadcast via `after_create_commit { broadcast_append_to }` + `turbo_stream_from @record`.
- **Action Cable + Solid Cable** : stack mentale Hotwire (Action Cable EN DESSOUS Turbo Streams = sucre `turbo_stream_from` génère auto `Turbo::StreamsChannel` signé). Auth via `env["warden"].user` (4 obstacles à `cookies.encrypted` direct). ⭐ Connection **permissive** + `return reject unless current_user` per-channel (vs `reject_unauthorized_connection` qui casse Turbo Streams sur pages publiques).
- **Stimulus avancé** : 5 lifecycle + `static values` typés + `XValueChanged` réactif + 3 catégories d'état (targets DOM / values HTML config / propriété JS `this.xxx` interne) + ⚠️ jamais shadow d'API DOM (`submit`/`click`/`focus`) + `disconnect()` cleanup obligatoire (timer fantôme).
- **Query Object pipeline** : `FILTERS.inject(scope) { |rel, k| next rel if blank; send("by_#{k}", rel, val) }` + dispatch dynamique safelisté + AR lazy.
- **AR sécurité + IDOR** : scope `current_user.X.find` → `RecordNotFound` → 404. Pattern IDOR sentinel : `sign_in user_a` + accès user_b → `expect(response).to have_http_status(:not_found)`. ⚠️ Rails `show_exceptions = :rescuable` en test env **mandatory** (vérifier `config/environments/test.rb`).
- **RSpec request specs** : `type: :request` + helpers HTTP + `response`/`flash` + `Devise::Test::IntegrationHelpers` + Rails 7+ redirection PATCH/DELETE en **303 See Other** (pas 302). Pattern AAA `context "with valid params"`/`"with invalid params"`. ⚠️ `do` manquant sur `context`/`describe` = bug sournois (Ruby n'erreure pas, examples se réattachent au parent → tests trop indulgents).
- **factory_bot** : `after(:build)` callback + bypass `Model.new` quand factory force un état + `let!` (eager) vs `let` (lazy) + association magique vs explicite + `expect { action }.to change(Class, :count).by(-1)`.
- **Active Storage testing** : `config.file_fixture_path` mandatory + `attach(io:, filename:, content_type:)` + `define_singleton_method(:byte_size)` hack pour stub > 10MB.
- **Tailwind v4 mécanisme central** : composition via CSS variables internes (`--tw-translate-x`, etc.) → empiler N transforms sans écrasement. ⚠️ Ordre `@import` Google Fonts **AVANT** `@import "tailwindcss"`. ⚠️ Typo dans `@apply` casse TOUTE la directive (terminal `bin/dev` le dit explicitement). `@custom-variant X (...)` + `@utility X { @apply ... }` + `@variant` ≠ `@custom-variant`. Utilities natives gagnent contre `@utility` custom en conflit (garantie v4).
- **Mailers pattern moderne** : `Mailer.with(params).action.deliver_later` + GlobalID sérialisation + 2 familles callbacks `*_action` (mail) vs `*_deliver` (message) + ⭐ `after_commit on: :create` **mandatory** (jamais `after_save` pour `deliver_later`, sinon job orphelin sur rollback transaction). Pareil pour broadcasts Action Cable.
- **SOLID 5 lettres** + 5 anti-patterns (God Object, Spaghetti, Cargo cult, Premature optimization, Magic numbers) + ⭐ **OCP central** dans concerns/extensions polymorphic (likeable scénario).
- **Metaprogramming first-pass** : 3 armes (`send` LE dispatch, `define_method` génération à la volée, `method_missing` + `respond_to_missing?` DSL) + 4 pièges sécurité (`send` user input = RCE). ⚠️ **Cours dédié sem 25 mer 17 Priorité 1** (`to-do.md`).
- **Bullet audit méthode** : (1) cartographie statique access pattern AVANT tout fix, (2) `Bullet.stacktrace_includes = [ "app/" ]` pour remonter à la cause, (3) reproduction propre 2 chemins, (4) diagnostic SQL `rails console`.

**Friction / blocages + méta-erreurs Claude (à appliquer plus strictement)**

- ⭐ **HEREDOC zsh+AZERTY de Chris ne marche JAMAIS** (flaggée 2× ven 12). Toujours commits `-m "1 ligne"` + body PR via `gh pr create --body-file /tmp/...md`. Plus jamais de `$(cat <<'EOF' ... EOF)`.
- **Bash sans commande à gauche** (`> log/bullet.log`, `: > file`) refusée par zsh de Chris. Toujours commande explicite (`truncate -s 0`, `rm`).
- **Numéros de ligne périmés après edits successifs** : utiliser ancres textuelles (`grep -n "system-test:"`) plutôt que numéros figés.
- **Préalables critiques flaggés trop discrètement** (Devise::Test::IntegrationHelpers, file_fixture_path, show_exceptions :rescuable). Encadrer ⚠️ **Avant de continuer** sur sections complexes mandatory.
- **Squelettes nus + récitation de pattern sans relecture du code affiché** (3 occurrences sem 24). Règles `feedback-explain-every-line-and-context` + globale 6 "Relis-toi avant de proposer" → **relire aussi les notes Obsidian récentes** avant de prescrire (anti-pattern `pointer-events-none` documenté hier que Claude a re-recommandé sans relire).
- **Recommandation aveugle sur warnings bullet** : vérifier l'access pattern réel (grep + lecture) AVANT tout fix, même si bullet recommande explicitement.
- **Vulgarisation insuffisante** récurrente sur Tailwind v4 + Turbo + Tom Select + Stimulus (concepts CSS-first + `@theme` + namespaces) → "avancé sous le capot" exige analogie concrète d'abord. Règle `feedback-vulgarize-advanced-rails` sem 22.
- **Trop d'info empilée d'un coup** sur lignes denses Action Cable (ven 12 "tu commences à m'embrouiller"). Sur lignes denses = **une seule idée par bloc**.
- **2h30 lecture pure mal calibré pour chapitres notationnels** (Tailwind Customization v4) : pivot pédago ven 12 juin → pour les chapitres **notationnels** (vs conceptuels Action Cable/Callbacks/SOLID), couper 1h lecture + 1h30 pratique immédiate. Pattern à valider sem suivantes.
- **3 nouvelles memory globales sem 24** : `project-nine-to-fine-mobile-first` (design system desktop-first sans poser la question contexte), `project-nine-to-fine-locale-english` (présomption FR alors que `:en`), `feedback-no-co-authored-by` (commits sans Co-Authored-By Claude ni email Anthropic).

**Reports vers sem 25**

- **Likes broadcast Turbo Streams** (lun 15) : Like model polymorphic + UI broadcast `after_create_commit { broadcast_append_to outfit }` + `turbo_stream_from @outfit` (pattern moderne d'entrée de jeu = consolidation Turbo Streams par production réelle).
- **Comments broadcast Turbo Streams** (mar 16) : Comment model polymorphic + même pattern broadcast.
- ⭐ **Journée consolidation hybride mer 17** : matin **cours Stimulus avancé** + 3 exos sandbox `ChrisCroc/sandbox-stimulus` (Outlets / `dispatch()` pub-sub / state machine via `XValueChanged`) → note Obsidian [[stimulus-advanced-patterns]] à créer ; avant-midi **cours Metaprog Priorité 1** + 3 exos sandbox `~/code/sandbox/rails/metaprogramming/` (clone `attr_accessor` / Builder DSL `method_missing` / refactor `GarmentFilter` `define_method` Q6 reportée) → note Obsidian [[metaprogramming-deep-dive]] à créer ; aprem suite Likes broadcasts sur N to F.
- **Follow model polymorphic + profil user public** (jeu 18) : `UsersController#show` + listing garments/outfits/follows + US04 (update profile).
- **Lancement recherche emploi + skill `job-tracker`** (ven 19 + sam 20-21) : 3 candidatures Rails + LinkedIn polish + GitHub README pinned. Skill Claude Code `job-tracker` ~1-2 j atelier.
- **4e IDOR sentinel `GET /outfits/:id/edit`** : à ajouter pour cohérence avec garments (non bloquant, scoping garanti par `set_outfit` partagé).
- **5 exos sandbox cumulés** (lun/mar/mer/jeu sem 24 + ven 12) : Stimulus debounce, Mailer `after_commit` + RSpec `have_enqueued_mail`, Tailwind Transforms modal+hover, Solid Cable + Turbo Streams focused. Plan = 1 nouveau/soir sem 25 + rattrapage progressif samedi 20. Spec complète dans `to-do.md`.
- **Personal to-do Obsidian** : Huntr Le Wagon 🔴, contentieux serrurier 🔴, relire Turbo Frames/Streams 🟡, décision SPA vs MPA NtF 🟡.

---

### Sem 25 (15-21 juin 2026)

⭐ **Démarrage lundi 15 juin** — Première session sem 25. Phase 1 techniquement bouclée vendredi 12 juin (9 jours d'avance). Sem 25 = **semaine consolidation dispersée** + features sociales N to F + lancement recherche emploi + skill `job-tracker`. Phase 2 React démarre sem 26 (22 juin) inchangée.

**Conditions d'alerte sem 25** (cf. `PHASES/phase-1.md`) : si Likes broadcast Turbo Streams patine > 1.5 j → simplifier en HTML simple, upgrade reportée sem 26-27. Si mer 17 hybride trop dense cognitivement → décaler 1 cours à jeudi matin en repoussant Follow d'½ journée. Si ven 19 lancement emploi patine → décaler 3 candidatures à samedi.

**Accompli**

- **Lundi 15 juin (démarrage de session, Claude Code CLI)** — Read SUIVI obligatoire (étape 0 CLAUDE.md). Annonce programme du jour (Like model polymorphic + UI broadcast Turbo Streams + LikesController + IDOR). Routine lundi : (1) `gh issue list` → mapping 9 issues ouvertes avec plan sem 25 (US20 lun / US21 mar / US22+US04 jeu / US18 reportée sem 26+ après brainstorm produit "public" / US05 + US23-24 IA Phase 4). **Décision tranchée sur US18** : option B retenue (coder Like model + UI sem 25 sans `is_public:boolean`, testable via RSpec + console ; US18 = brainstorm produit dédié sem 26+ vs stub aujourd'hui = premature optimization). (2) **Routine condensation hebdo sem 24 exécutée** (étape 7 CLAUDE.md) : archive verbatim sem 24 ajoutée à `ARCHIVE/2026-06.md` + sem 24 condensée écrite dans SUIVI-COURANT (~40 k → ~11 k chars, marge sur cible 8 k pour préserver décisions tranchées + concepts en live + reports backlog). 6 tâches TaskCreate posées pour la journée. **PR planning #110 mergée** (chore/planning-mon-jun-15-condensation). **CVE Dependabot intercalaire** : `bundler-audit` flaggé 3 CVE sur `net-imap 0.6.4` (transitive via `mail 2.9.0`, advisory-db update 14 juin) → branche dédiée `fix/net-imap-cve-0.6.4.1` + `bundle update net-imap --conservative` → 0 vuln + **PR #111 mergée**. Décision : pas attendre Dependabot weekly (transitive + Gemfile direct seul scanné = bump manuel mandatory).

- **Lundi 15 juin (matin, Claude Code CLI mode coach)** — **Task #2 ✅ Like model polymorphic + RSpec, PR #112 mergée**. 7 fichiers, +106/-1 lignes, **8 examples RSpec verts** (113 → 121 dans la suite complète). **Décisions design tranchées** : polymorphic dès V1 (vs FK directe `outfit_id`, cohérence OCP sem 24 [[solid-design-principles-and-anti-patterns]]) + index unique composite DB-level `[user_id, likeable_type, likeable_id]` + validation `uniqueness` scopée app-level + `dependent: :destroy` sur User+Outfit `has_many :likes` + `as: :likeable` côté Outfit + pas de `foreign_key: true` sur likeable (impossible polymorphic, trade-off accepté + `dependent: :destroy` Rails au lieu de FK DB) + hooks broadcast Turbo Streams **pas dans cette PR** (vue qui écoute mandatory en parallèle). **10 concepts gravés en live ligne par ligne** : BIGINT vs INTEGER (safe default Rails 5.1+), `references` (migration) vs `belongs_to` (model) — 2 mondes séparés à synchroniser + analogie clé+voiture, `null: false` (Postgres garde-fou) vs `validates presence: true` (Ruby user-friendly) defense-in-depth, `foreign_key: true` impossible polymorphic + trade-off, `polymorphic: true` 2 colonnes (`likeable_type` string + `likeable_id` BIGINT) + analogie casier de gym, index composite ordre des colonnes (Postgres scan left-to-right), `let` (lazy memoized) vs `let!` (eager), pattern paire scope (preuve par contraste sur N+1 tests par scope), `expect { ... }.to raise_error(...)` accolades = bloc différé / parenthèses = valeur immédiate + analogie pomme vs boîte fermée, `save(validate: false)` bypass app-level pour tester defense-in-depth DB-level. **3 pièges Ruby récurrents** flaggés en live : (a) **virgule dans `%i[]`** (récurrence sem 24 lundi 8 juin sur `GarmentFilter::FILTERS`) — bug silencieux sournois (`%i[a, b]` génère `[:"a,", :b]`, Ruby/Rails passent au load, plante au runtime SQL) ; (b) **`:` manquant sur `factory: outfit`** (1ère apparition) — `outfit` interprété comme variable/méthode locale → `NameError` au runtime ; (c) **`do` manquant sur `it`** (variante du piège vendredi 12 juin sur `context` dans `garments_spec.rb`) — `it "string"` sans bloc est valide RSpec (marqué `pending`), code après s'exécute au load-time hors test, tests "verts" qui ne testent rien. **Règle mémo entretien gravée** : "dès qu'on référence par nom dans Rails (validates/belongs_to/factory/...), c'est un symbole — toujours `:nom`". Reco Rubocop autofix pour lint CI (Chris a aussi demandé setup pre-commit hook lefthook → décision skip aujourd'hui, PR dédiée plus tard, reporté `to-do.md` Chris). **Pédago : Chris a flaggé 1× "tu ne vulgarises pas bien" sur test 4 DB-level (expect{}+raise_error+save(validate:false))** → reset complet en 3 sub-concepts (saut de douane / pomme dans boîte / viser précisément l'exception) avec analogies fortes → débloqué.

- **Lundi 15 juin (avant-midi, Claude Code CLI mode coach) — Task #3 INTERROMPUE** : tentative UI Likes broadcast Turbo Streams **stoppée par Chris suite à 2 méta erreurs Claude graves**. **Erreur 1** : recommandation de lancer `bin/rails solid_cable:install` + `bin/rails db:migrate` **sans vérifier l'état du repo** (`config/database.yml` était en multi-DB défaut Rails 8 jamais appliqué + `db/cable_schema.rb` déjà présent depuis `rails new` du 25 mai + `async` en dev suffit pour multi-onglet local sans aucune table SQL). **Chris a challengé** ("n'y a-t-il pas des modifs à faire avant ?") → bonne intuition qui a sauvé du blunder. Sub-step "ne JAMAIS faire `db:migrate` sans relire la migration" gravée ce matin, immédiatement oubliée par moi en début d'avant-midi. **Erreur 2** : après que Chris ait lancé `solid_cable:install`, conclusion **pseudo-certitude** "rien d'installé" sur la base d'un `git status` propre sans vérifier la sortie réelle de la commande (générateur idempotent — fichiers déjà présents → `identical` × 2 → 0 modif → `git status` clean). Récurrence exacte du pattern Dependabot début de session ("pas de probablement / surement, vérifie avant" — règle 2 globale + précédent flaggé par Chris). **Décisions tranchées avant interruption** : (1) **Setup Single DB cable+queue+cache reporté Phase 2 sem 28-29** (au moment du déploiement Kamal Hetzner), pattern enseigné à Chris dans le passé (3 generators InstallSolid* + copy schemas + database.yml unifié sur DATABASE_URL + db:migrate + cleanup schemas) — j'ignorais ce pattern, Chris me l'a appris. Aucune dette technique active aujourd'hui (Nine to Fine reste en multi-DB défaut Rails 8 brut, jamais appliqué, async en dev suffit). (2) Option C retenue (skip setup Solid Cable aujourd'hui, async dev suffit pour multi-onglet test) AVANT que Chris interrompe. **Chris a coupé court** : "tu es incompétent, je fais l'aprem en CLI seul". **Tasks #4-#6 (aprem + soir + fin de journée) seront faites par Chris en autonomie**, pas de coaching Claude cet aprem. **3 méta-erreurs Claude récurrentes ce matin+avant-midi** : (a) **pseudo-certitude après vérif superficielle** (Dependabot au démarrage de session + `git status` clean en avant-midi = 2× sur la même journée), (b) **pas de lecture de l'état du repo avant générateur** (database.yml + cable.yml + schemas + migrations existantes — règle "vérifier l'état avant de proposer une commande infra/setup"), (c) **ignorance de patterns Rails 8 prod** (Single DB Hetzner-friendly = pattern documenté GoRails/Drifting Ruby/Kamal blog que j'aurais dû connaître au lieu de recommander défaut multi-DB). **Engagement gravé pour la suite** : (i) avant TOUTE commande de générateur/setup infra, lire `config/database.yml` + `config/<service>.yml` + `db/migrate/` + schemas — pas juste les modèles/controllers ; (ii) ne pas conclure depuis 1 seule vérification ambiguë (git status clean ≠ rien lancé, c'est aussi "lancé idempotent") — demander la sortie de la commande ; (iii) flagger les sous-projets infra comme blockers à traiter en PR dédiée, pas mélanger dans une PR feature.

- **Lundi 15 juin (reprise aprem, Claude Code CLI mode coach assoupli)** — Chris a repris le coaching après l'interruption avancée. **Task #1 ✅ PR A Solid Cable + Queue + Cache Single DB unifié, PR #114 mergée** : 4 fichiers configs modifiés (`database.yml` pattern Le Wagon `DATABASE_URL` 4 named connections + `cable.yml` dev async→solid_cable + `development.rb` cache_store=solid_cache_store + queue_adapter=:solid_queue + `Procfile.dev` worker:bin/jobs), 3 migrations standard `InstallSolidCable`/`InstallSolidQueue`/`InstallSolidCache` (12 tables Solid* en dev + test), 2 schemas standalone supprimés (queue + cache), `cable_schema.rb` conservé (convention Le Wagon de Chris vs reco doc officielle Rails de supprimer). **Pipeline complet vérifié en console** : broadcast Action Cable → INSERT solid_cable_messages → auto-enqueue `SolidCable::TrimJob` pické par Solid Queue worker en 0.6ms → 3 process actifs (Supervisor + Worker + Dispatcher avec heartbeats) + Solid Cache round-trip via `solid_cache_entries` avec namespace `Rails.env`-préfixé. **Tâche #2 ✅ PR B UI Likes broadcast Turbo Streams, PR #115 mergée** : 11 fichiers (138+/3-), pipeline Likes broadcast Turbo Streams + Stimulus optimistic UI fonctionnel. **6 décisions design tranchées avant code** : (1) routes nested classiques vs polymorphic (anti cargo cult), (2) form_with + Stimulus vs button_to (consolidation Stimulus), (3) hooks broadcast dans Like model (after_create_commit) pour le compteur global + render turbo_stream replace `_like_button` depuis controller pour le bouton spécifique current_user (= 2 mécanismes pour 2 zones DOM différentes, 2 partials séparés `_likes_count` vs `_like_button`), (4) IDOR autorisé V1 (user peut liker tout outfit incl. les siens, US18 "public" reportée sem 26+), (5) counter_cache sur Outfit (dénormalisé likes_count colonne anti N+1), (6) optimistic UI Stimulus (flip immédiat icon ♡↔♥ + disable button au submit, confirm/rollback sur turbo:submit-end). **Note Obsidian rédigée FR** : `Apprentissage/Rails/Asset Pipeline & Hotwire/likes-broadcast-turbo-streams-and-optimistic-ui.md` (vulgarisation complète du pipeline + couplage data-attributes ERB/Stimulus + dom_id ↔ broadcast/turbo_stream.replace + Fat Model Thin View + @utility btn-like cohérent design system + flow optimistic UI + 3 questions style entretien 90 sec). **8 méta-erreurs Claude cumulées sur la journée** (Chris a explicitement compté) : (1) Dependabot pseudo-certitude, (2) git status clean conclusion solid_cable install, (3) convention `CreateSolidCableTables` au lieu de `InstallSolid*` (Chris a corrigé), (4) pattern Single DB pure proposé au lieu du pattern Le Wagon `DATABASE_URL` × 4 named connections, (5) reco supprimer `cable_schema.rb` (techniquement OK selon README solid_cable mais Chris a tranché de garder par convention Le Wagon), (6) design system Tailwind ignoré (classes brutes `text-sm`, `text-2xl` au lieu de tokens `text-caption` / `@utility btn-like` cohérent avec `btn-base`), (7) `dom_id` supposé dispo dans model contexte sans vérification (en réalité dispo seulement via `ActionView::RecordIdentifier.dom_id` au niveau module), (8) vulgarisation incorrecte de la convention `dom_id` (j'ai dit "outfit_42_likes_count" pendant 2h mais c'est `likes_count_outfit_42` — préfixe au début). **9e méta-erreur identifiée en diagnostic counter_cache** : option `counter_cache: :likes_count` perdue lors d'une édition (probablement copier-coller incomplet), counter_cache jamais enregistré (`Like.counter_cached_association_names: []`), broadcasts envoyés avec `likes_count = 0` en boucle → compteur ne se mettait pas à jour. Fix : ajouter `, counter_cache: :likes_count` à la ligne 3 du model Like + backfill via `Outfit.find_each { Outfit.reset_counters(o.id, :likes) }`. Pour la 3e fois de la journée j'ai supposé au lieu de vérifier (counter_cache polymorphic Rails 8 a été investigué dans le code source `activerecord-8.1.3` au lieu d'inspecter d'abord le contenu actuel du Like model). **Engagement renouvelé pour demain (mardi 16 juin)** : application stricte du protocole "si Le Wagon a un tuto ou Chris a appris quelque chose dans le bootcamp, demander la source AVANT toute proposition" + "lire l'état actuel du fichier AVANT de diagnostiquer un bug". **Bug Stimulus `confirm(event)` rollback ligne 16 non fixé** (`InputEvent.Detail.success` au lieu de `event.detail.success`) — to-do.md, à fix demain avec RSpec request spec. **Tâche #3 reportée mardi soir + #4 reco vidéos faite ce soir.**

- **Mardi 16 juin (matin, Claude Code CLI mode coach) — Likes soldés (dette lundi).** Fix bug Stimulus `confirm(event)` rollback (fait par Chris, déjà committé) + **request spec `LikesController`** (6 examples : create nominal / idempotent `find_or_create_by` / cross-user social autorisé / destroy own / **sentinel IDOR** destroy d'autrui / redirect anonyme) + **régression `counter_cache`** ajoutée à `like_spec.rb` (8 → 10 examples). Branche `feat/rspec-likes` mergée (commit `test:`). ⭐ Point de coaching : **l'IDOR des Likes ≠ celui des Garments** — liker l'outfit d'autrui est AUTORISÉ (social V1), le seul vrai sentinel IDOR est sur `destroy` (`current_user.likes.find` → `RecordNotFound` → 404). Le test counter_cache = régression directe contre la 9e méta-erreur de lundi (option `counter_cache:` perdue).

- **Mardi 16 juin (avant-midi + aprem, Claude Code CLI mode coach) — ⭐ feature Commentaires complète, branche `feat/comments` mergée.** Pipeline superpowers complet suivi à la demande de Chris : **brainstorming → writing-plans → exécution** coachée step-by-step. Design doc (`docs/superpowers/specs/2026-06-16-comments-design.md`) + plan (`docs/superpowers/plans/2026-06-16-comments.md`) committés. **Décisions design tranchées** : (1) ⭐ **FK-direct `Comment belongs_to :outfit`** (PAS polymorphic, contrairement à Like) — on commente une tenue pas un garment isolé ; une marketplace future (Vinted-like) gérerait ses propres commentaires produit ; migration FK→polymorphic bon marché → YAGNI assumé ; (2) **antéchronologique** (récent en haut) → `order(created_at: :desc)` dans l'association + `broadcast_prepend_to` ; (3) **create + destroy seulement** V1 (update inline backlog) ; (4) `body` presence + max 1000 ; (5) **counter_cache `comments_count`** sur Outfit + compteur affiché ; (6) ordre dans l'association (pas `default_scope`, pas scope nommé). **Temps réel** : hooks model `after_create_commit` (prepend commentaire + replace compteur) / `after_destroy_commit` (remove + replace compteur), `target:` via `ActionView::RecordIdentifier.dom_id`, page déjà abonnée via `turbo_stream_from @outfit`. Tests : `comment_spec.rb` (validations + counter_cache +1/−1 + ordre antéchrono) + `comments_spec.rb` request (create valide / blank 422 / outfit d'autrui autorisé / destroy own / **sentinel IDOR** / anonyme). Suite complète verte. PR body manuel, lint RuboCop autocorrigé (array bracket spacing). **4 notes Obsidian rédigées, séparées par thème** : [[comments-broadcast-turbo-streams-list]], [[params-expect-vs-require-permit-rails-8]], [[erb-output-vs-execution-and-partial-anatomy]], [[lambda-lazy-evaluation-scopes-and-callbacks]] + index Rails MAJ (nouvelle sous-section « Turbo Streams — Broadcasts »).

- **Concepts gravés en live (mardi 16)** : ⭐ **piège `current_user` dans un broadcast** (un partial broadcasté est rendu hors requête → pas de session → pattern `viewer_id` passé en local via `local_assigns`, `nil` en broadcast → pas de bouton Delete sur l'insertion live, revient au reload = trade-off V1 « Option A ») ; ⭐ **anti-doublon controller/broadcast** (le controller `create` **ne rend PAS** le commentaire — le `broadcast_prepend` l'ajoute pour tous, auteur inclus → le controller vide juste le form ; inverse des Likes où le controller rendait le bouton user-specific et le broadcast le compteur global) ; **`params.expect(comment: [ :body ])`** Rails 8 vs `require.permit` (vérifie la structure → 400 au lieu d'un 500) ; **`<%=` vs `<%`** (`<% render %>` exécute mais jette le HTML → form invisible ; ⭐ un test « 200 OK » ne prouve pas le contenu) ; **anatomie d'un partial** `_name.html.erb` (underscore + format + handler, les 3 obligatoires) ; **lambda `->`** = évaluation différée (scopes d'association + callbacks, valeur dynamique figée au boot sinon).

- **Backlog enrichi (`FEATURES_FUTURES.md`, mardi 16)** : (a) *Édition inline des commentaires* (`update`, priorité moyenne, évolution certaine) ; (b) *Bouton Delete en temps réel* (Option B : bouton toujours rendu masqué + `<meta current-user-id>` + Stimulus, basse) ; (c) *Gestion gracieuse `RecordNotFound`* (UX back-button sur ressource supprimée — ⚠️ trade-off central : un `rescue_from` global transformerait **tous** les sentinels IDOR de 404 en redirections, basse).

- **Friction / méta (mardi 16)** : ⭐ **Chris a recadré sur la vulgarisation en milieu d'aprem** — j'ai balancé les 4 partials avec le code sans coacher (« beaucoup de choses nébuleuses : `viewer_id` jamais setté nulle part, `dom_id` que je croyais inutilisable, `time_ago_in_words`, `comment.outfit`, `divide-y` »). Repris point par point en français pédagogique. Rappel `feedback-explain-every-line-and-context` + `feedback-vulgarize-advanced-rails`. Plusieurs typos de saisie attrapées en relecture coach (chemin partial singulier `comment/form`, `:comment` singulier dans `has_many`, partial sans underscore puis sans extension, `<%` au lieu de `<%=`, factory `create(:other_user)` inexistante, lint array brackets). **Exos du soir + reco vidéos faits par Chris sur l'app Claude** (hors CLI). **Travail de fin de session Claude** : mise à jour SUIVI + phase-1 + PROGRESSION + schéma CLAUDE.md (Like polymorphic / Comment FK) + 4 notes Obsidian.

- **Mercredi 17 juin (matin + avant-midi, Claude Code CLI mode coach) — ⭐ Cours Stimulus avancé + 7 exos sandbox.** Consolidation par la pratique dans `~/code/sandbox/stimulus/` (repo `ChrisCroc/sandbox-stimulus`, `stimulus-starter` officiel). **L'échelle des 7 patterns** codée du plus simple au plus complexe, Chris écrit 100% du code, Claude relit ligne par ligne : (1) controller de base target+action (`lamp`) ; (2) targets multiples `this.xTargets`+forEach (`hearts`) ; (3) Values `static values`+`this.xValue`+défauts par type (`counter`/`favorite`) ; (4) ⭐ **`xValueChanged` réactif** — affichage centralisé, se déclenche aussi après init, `reset()` = preuve (1 ligne, zéro affichage à recoder) ; (5) **`disconnect()`** + timer fantôme (`setInterval`/`clearInterval` + chaîne `destroy → element.remove() → Stimulus appelle disconnect auto`) (`clock`) ; (6) **`dispatch()`** + `detail`/`event` (JS pur) + ⭐ **piège bubbling → `@window`** (deux controllers cousins, l'événement remonte chez les ancêtres pas chez le voisin → échec silencieux) (`sender`/`receiver`) ; (7) **Outlets** — `static outlets`, `this.xOutlets` = instances (pas éléments), câblage `data-[hôte]-[outlet]-outlet="sélecteur"` + ⚠️ double exigence `data-controller`+classe sur la cible (`remote`/`bulb`). Réflexe n°0 enseigné : `connect()`+`console.log(this.element)` pour prouver la connexion avant toute logique. **2 techniques gardées en mémoire pour N to F** (dans la note) : cacher un bouton inutile via `valueChanged`+`.hidden` ; cliquer sur l'élément (+ ⚠️ piège a11y div/span non focusable, point d'entretien). ⭐ **Intuition juste de Chris** : la complexité réelle des controllers N to F (Tom Select) est de l'**intégration** (briques Stimulus + lib externe + DOM lourd + piège Turbo), pas un concept neuf → noté « à approfondir ». **Note Obsidian rédigée** : [[stimulus-advanced-patterns]] (français clair + code de chaque exo) + index Rails MAJ.

- **Friction / méta-erreurs Claude (mercredi 17 matin) — grave, à ne PAS répéter.** ⭐⭐ **Le cours a très mal démarré** : Claude a fait un « cours » abstrait (table des 3 patterns + trade-offs Outlets/dispatch + question sur dispatch non encore enseigné + `@window` sorti de nulle part **avant** d'avoir montré le cas de base). Chris a explosé plusieurs fois (« lecture dans le vide », « tu balances des exemples dans le vide », « tu ne guides en rien », « je le sors de mon cul ? », « brouillon, mal expliqué », « j'en peux plus de toi »). **Récurrence exacte du travers du 12 juin** (lecture pure mal calibrée pour un sujet pratique). **3 erreurs concrètes répétées** : (a) théorie/notation avant qu'un seul truc tourne ; (b) introduire un terme (`@window`) sans avoir montré le problème qu'il résout ; (c) **supposer l'état du sandbox sans regarder** (j'ai dit « écris dans le sandbox » alors que `src/index.js` contenait du HTML cassant le build webpack + j'ai pointé un `index.html` pollué par d'anciens exos — récurrence du « vérifier l'état avant d'affirmer », déjà flaggé lundi 15). **Méthode corrigée en cours de route, validée par Chris via AskUserQuestion** : « je code, tu guides, mais tu expliques le pourquoi du comment **comme si je ne connaissais rien**, et tu repars du début ». À partir de là, format = exemple complet/minimal qui TOURNE + explication ligne par ligne + Chris code une variante + Claude relit en lisant l'état réel des fichiers à chaque blocage. **Workspace remis à blanc** (index.html nettoyé, `src/index.js` réparé). Une fois ce format adopté → 7 barreaux enchaînés sans friction. **Leçon transverse** : pour un sujet **pratique** (vs conceptuel comme Action Cable/SOLID), JAMAIS de cadrage théorique en avance ; partir d'un truc qui tourne, vocabulaire à la demande quand un problème concret l'exige. Rappels `feedback-explain-every-line-and-context` + `feedback-vulgarize-advanced-rails` + « lire l'état du repo avant d'affirmer ».

- **Mercredi 17 juin (après-midi, Claude Code CLI mode coach) — ⭐ Cours dédié Metaprogrammation + 6 exos sandbox + capstone.** Résout le 2e chapitre « flou ». Tout dans `~/code/sandbox/rails/metaprogramming/` (Ruby pur, `ruby xx.rb`), Chris code tout, **1 fichier par exo** : (1) `define_method` (1 méthode + boucle générant N méthodes) ; (2) réimplémenter `attr_accessor` (rouvrir `class Class` car une classe = exemplaire de `Class` + `instance_variable_get/set` + appels multiples) ; (3) `send` (dispatch dynamique = le `send("by_#{key}")` de GarmentFilter) ; (4) `method_missing` + (5) `respond_to_missing?` (binôme obligatoire) ; (6) reconstruire `OpenStruct` soi-même (`@table` + `method_missing(name, value = nil)` détectant getter/setter via `end_with?`/`chomp`). **Capstone `GarmentFilter`** (lecture + explication complète de son vrai code : `relation` = requête lazy en construction, `inject` accumulateur, `slice(*FILTERS)` liste blanche, jointure `by_tag_id`, `ILIKE`+`sanitize_sql_like` 2 couches sécurité). ⭐ **Décision/jugement clé tranché : NE PAS refactorer les 3 `by_X` uniformes avec `define_method`** = cargo cult (perte lisibilité > gain DRY pour 3 méthodes d'1 ligne) ; le `send` du dispatch est au bon endroit → GarmentFilter reste tel quel. **Intuition de Chris validée** (« metaprog pas utile partout ») : on en écrit peu, on le LIT partout (Rails EST du metaprog). Note Obsidian rédigée [[metaprogramming-deep-dive]] (français clair + code de chaque exo, **remplace** [[metaprogramming-ruby-and-rails-first-pass]] marqué superseded) + index Rails MAJ.

- **Friction / méta-erreurs Claude (mercredi 17 aprem) — même travers que le matin, persistant.** Le cours metaprog a aussi déraillé plusieurs fois, **même cause racine** : (a) **concepts neufs imposés dans une variante que Chris doit écrire** (le splat `*` jamais montré tourner, glissé dans l'exo « Multiplier ») → « tu me balances des concepts nébuleux jamais vus » ; (b) **chaos de fichiers** (01 → 02 → 01b → retour 01, exos lancés puis abandonnés) → « tu me fais switcher en cours » → règle posée : **1 fichier par exo, fini avant de passer au suivant, jamais de retour en arrière** ; (c) **`@table` introduit sans l'expliquer** dans OpenStruct ; (d) **confusion getter vs setter** (« `.host` est le même dans les deux ! ») → a nécessité de re-décortiquer le `=` (deux méthodes `host` et `host=` distinctes). Insultes répétées (« gros bouffon », « tu te prends pour qui »). ⭐ **Question de fond légitime de Chris** : « à quoi sert le metaprog si Rails donne tout ? » → répondu honnêtement (raison : on n'en écrit pas ; tort : Rails EN EST, faut le lire/débugger), bon moment qui a réengagé. **Leçon (3e fois aujourd'hui)** : sujet **pratique** = jamais de concept neuf dans une tâche que Chris écrit (uniquement ce qui a été MONTRÉ tourner) ; 1 exemple complet expliqué ligne par ligne → il code → relecture. À surveiller strictement aux prochaines sessions React (Phase 2). **Backlog** : repratiquer `GarmentFilter` (mini Query Object from scratch) en sandbox un autre jour (ajouté `to-do.md`).

- **Jeudi 18 juin (Claude Code CLI, mode coach) — ⭐ feature Follow complète + profil minimal, PR #125 mergée.** Pipeline superpowers complet **brainstorming → writing-plans → exécution** coachée (design doc `docs/superpowers/specs/2026-06-18-follow-design.md` + plan `2026-06-18-follow.md`). 3e application du combo après Likes/Comments. **Livré** : model `Follow` self-join + counter_cache + RSpec model ; `FollowsController` create/destroy + request spec + sentinel ; profil nu `UsersController#show` (auth-gated) + bouton Follow/Unfollow Turbo Stream + lien navbar→profil ; utility Tailwind `page-shell` (refacto du fond dégradé répété sur garments/outfits index/new/edit/show + profil, **commit séparé**). 4 notes Obsidian : [[follow-self-referential-associations]], [[follow-button-turbo-stream-no-stimulus]], [[page-shell-utility-vs-custom-variant]], [[rspec-subject-and-is-expected]].

- **Décisions de design tranchées (jeudi 18, ne pas re-proposer)** :
  - ⭐ **Follow = self-join user→user, PAS polymorphic** (le « polymorphic » du PLANNING était une imprécision d'écriture). `belongs_to :follower/:followed, class_name: "User"` + counter_cache 2 colonnes (`following_count`/`followers_count`).
  - **Follow instantané (pas d'approbation)** V1 ; l'approbation (compte privé) = colonne `status` **additive** plus tard, pas une réécriture.
  - **Pas de broadcast Action Cable** : bouton user-specific → `turbo_stream.replace` controller (comme `_like_button`), **pas de Stimulus optimistic** non plus. 3e niveau « controller-only » (vs broadcast Likes/Comments + Stimulus Like).
  - ⭐⭐ **Découplage US18** : on ne construit que la **primitive invariante** (relation Follow + profil nu nom/compteurs/bouton). Tout ce qui dépend du public/privé (outfits publics, gating par follow, avatar, nav par tag, profil riche) = **brief US18** ajouté `FEATURES_FUTURES.md` (priorité haute), brainstorm produit sem 26+. Principe acté avec Chris : **100 % du code du jour survit à la décision US18** (le gating est une règle de lecture, il ne change pas le stockage des follows).
  - **Profil = auth-gated** : tout le site exige la connexion → profil visible par tout user **connecté** (pas anonyme) → **pas de `skip_before_action`** sur `users#show`.
  - ⭐ **US04 = Option B tranchée** : page profil dédiée `UsersController#edit/update`, PAS le champ username dans Devise. Voir handoff ci-dessous.

- **Concepts gravés en live (jeudi 18)** : ⭐ **piège `nil == nil`** sur `not_self_follow` (avec `build`, les FK sont nil → `follower_id == followed_id` = faux positif self-follow → comparer les **objets** `follower == followed`, sémantique `==` d'AR) ; ⭐ **`@user.reload`** mandatory dans le controller (counter_cache bouge en SQL → objet mémoire périmé → ancien compteur ré-affiché sinon) ; **route `resource :follow` singulière** → IDOR **structurel** (`find_by(...)&.destroy` = no-op gracieux, **pas un 404** contrairement aux Likes) ; **`@utility` vs `@custom-variant`** (QUOI vs QUAND appliquer) ; **sucre RSpec `subject`/`is_expected`/`it {}`** (à connaître pour lire, mais le projet écrit la forme explicite). `foreign_key: { to_table: :users }`, `dependent: :destroy` des 2 côtés, `"follower".pluralize(n)` (mot seul, pas le nombre).

- **Friction / méta-erreurs Claude (jeudi 18)** : (a) ⭐ **forme RSpec condensée (`subject`/`is_expected`) imposée sans la flagger** comme un style différent de celui du projet → Chris a recadré (« tu fais de la refacto de style sans prévenir et sans expliquer »), réécriture en forme explicite alignée sur `like_spec`. Récurrence du travers « changer d'approche sans prévenir ». (b) **Mauvaise hypothèse d'auth** : `skip_before_action :authenticate_user!` posé en supposant « profil public = anonyme » alors que tout le site est auth-gated → Chris a corrigé. (c) **Bon réflexe maintenu** : 9 tests rouges pour **un seul typo** (`not_sef_follow`) → fichier réel **lu** avant diagnostic → trouvé en 1 coup ; idem avant chaque générateur (schema.rb, vues, ApplicationController vérifiés). (d) **Challenge utile** : avant US04, signalé que Devise permet déjà `:username` mais que sa **vue n'a pas le champ** → évité du code redondant, design B retenu.

- **Jeudi 18 juin (session bonus aprem, Claude Code CLI mode coach) — ⭐ US04 livrée, PR #127 mergée.** Édition du profil (username) en TDD coach (Chris code, Claude relit) : pipeline routes → request spec rouge → controller → vue → lien show. **Option B tranchée** : page profil dédiée `UsersController#edit/update`, **PAS** le champ Devise (la vue `registrations/edit` n'a pas de champ username → US04 pas « déjà fait » ; sépare account/sécurité Devise de profil/présentation ; pose la fondation que US18 enrichira). **Livré** : routes `%i[show edit update]` ; `UsersController` avec `set_user` partagé (show+edit+update) + `require_self` + `params.expect(user: [ :username ])` ; vue `users/edit.html.erb` (design system `page-shell`/`btn-base`/tokens) ; lien « Edit profile » own-only sur show ; 5 request specs (render edit, update OK, re-render invalide 422, **2 sentinels IDOR**). 2 commits (`feat:` US04 + `fix:` typos `_follow_button` `Unfllow`/`hover-`/`font ` + comment `outfits/show`). Tweak design : `mt-2` consolidé dans `@utility btn-base` (+ retiré le doublon `comments/_form`, décision assumée par Chris après vérif visuelle des 5 boutons). Note Obsidian : [[profile-edit-idor-redirect-and-render-resolution]].

- **Décisions/concepts US04 (ne pas re-proposer)** :
  - ⭐ **L'IDOR a 3 formes dans le projet** : (1) **404** via scope `current_user.X.find` (Garments/Likes — ressource privée) ; (2) **no-op gracieux** (Follow destroy, route singulière) ; (3) **redirect + alert** via `require_self` (Users edit/update — ressource **visible** par tous les connectés via `show`, mais **éditable** par le seul propriétaire). La forme dépend de la visibilité de la ressource.
  - ⭐ **Sécurité = controller, pas masquage UI** : le `if current_user == @user` qui cache le lien « Edit profile » est **cosmétique** ; la vraie barrière est `require_self` (un intrus tapant `/users/2/edit` à la main est renvoyé par le before_action, pas par l'absence de lien).
  - ⭐ **Résolution du render** (découverte en live sur 2 rouges) : action **vide** → render **implicite** → template manquant lève `ActionController::UnknownFormat` → **406** ; `render :edit` **explicite** → template manquant lève `ActionView::MissingTemplate` → 500. Même cause (vue absente), 2 chemins de code.
  - `form_with model: @user` persisté → infère **PATCH** + `user_path` automatiquement (record nouveau → POST `users_path`).

- ~~⭐ **PROCHAINE SESSION (vendredi 19) — lancement recherche emploi**~~ **❌ PÉRIMÉ** — ven 19 + sam 20 non travaillés, Chris part en vacances. Recherche emploi reportée à la semaine vacances sem 26 (voir ci-dessous). Skill `job-tracker` reportée au retour. **US18 toujours reporté** (brainstorm produit : public/privé outfits, gating par follow, avatar, nav par tag, profil riche — brief `FEATURES_FUTURES.md`). **US04 ✅ done.**

---

### Sem 26 (22-28 juin 2026) — ⚠️ semaine transition vacances (régime réduit)

⭐ **Démarrage mardi 23 juin** — Chris n'a rien fait depuis ven 19 et part en vacances cette semaine (retour dim soir 28). Veut travailler en **régime fortement réduit (3-4h/j)**, pas rien faire. Session de cadrage stratégique (pas de code de feature côté Claude).

**Décision stratégique tranchée (23 juin) — ne pas re-litiger** :
- ⭐ **Découpler recherche emploi et apprentissage React.** La recherche emploi (LinkedIn + README pinned + 3 candidatures Rails) est **le vrai livrable borné fin sept** ET la tâche la plus compatible régime réduit (profil/admin, bursty) → **faite cette semaine**. L'apprentissage React (Phase 2) est **décalé d'1 sem** pour le faire à plein régime, pas « construire sur du flou ».
- **Phase 2 React → sem 27 (lundi 29 juin)**, plein régime. Toutes les phases glissent +1 sem (fin de plan ~4 oct). Cible emploi fin sept préservée (job search continu + **Next.js Phase 4 optionnel = amortisseur de re-convergence**). Docs planning MAJ : `PLANNING.md` (table + note), `phase-1.md` (clôture sem 25 + section sem 26), `phase-2.md` (header + table +1 sem), `phase-3/4.md` (headers +1 sem, dates internes à recaler en début de phase).
- **Skill `job-tracker` reportée au retour** (atelier 1-2 j, demande du focus continu, pas pour des vacances).
- **Apprentissage de la semaine = consolidation passive GoRails** (Rails = stack qui décroche le job ; angle forward **Kamal/déploiement** = dé-risque le jalon deploy de Phase 2). React/Scrimba autorisé **uniquement en amorçage léger** (pas de notes Obsidian de conso, pas « j'ai fait la semaine 1 ») — sinon piège « construire sur du flou ».

**Polish NtF sélectionné depuis `to-do.md`** (vacances-compatible, ~1h30 cumulé) :
- Layout SEO/a11y (`feat/layout-seo-polish`) — `<html lang>` + `<title>`/meta dynamiques + body class scoping. ~30-45 min, zéro test, recruiter-visible.
- `_field` partial DRY sur `garments/_form.html.erb`. ~30-45 min.
- (optionnel léger : 4e sentinel IDOR `GET /outfits/:id/edit`).

**5 nouveaux items NtF relevés par Chris (23 juin) → ajoutés `to-do.md`** (à évaluer faisables cette semaine, pas forcément) : (a) ⚠️ **warning bullet régression depuis Follow PR #125** à corriger ; (b) **filtre recherche sur Outfit** (porter `GarmentFilter` sur les outfits) ; (c) **preview photo à la création (new)** garment + outfit (dispo seulement à l'edit) ; (d) **sidebar filtres togglable** (élève l'entrée existante `FEATURES_FUTURES.md` en near-term) ; (e) **toggle select/déselect d'un même filtre** (hidden field true/false, clic sur filtre actif = retire au lieu de forcer Clear all).

**Accompli**
- **Mardi 23 juin (Claude Code CLI)** — Read SUIVI obligatoire (étape 0). **Journée = écriture/réécriture des programmes planning** (semaine vacances sem 26 + décalage +1 sem des semaines à venir — docs MAJ : PLANNING + phase-1/2/3/4 + SUIVI + to-do) + décision découplage emploi/React + **3 vidéos GoRails**. ⭐ **0 code aujourd'hui.** Programme actif (emploi + polish NtF) dès mer 24. _(à compléter en fin de journée)_
