# Suivi vivant — Sem en cours + précédente

Journal de bord récent. À mettre à jour à la fin de chaque journée via la routine `/end-of-day` (à créer sem 22+).

**Politique de rolling** : ce fichier contient **la semaine en cours + la semaine précédente complète**. Début de mois (au plus tard le premier lundi du mois suivant), archivage de tout le mois précédent vers `ARCHIVE/{YYYY-MM}.md`.

**Politique de verbosité** : journal **condensé**. Les pavés techniques (pièges, concepts maîtrisés, choix de design, idiomes Rails 8, classes Tailwind) **vivent dans les notes Obsidian** (`~/Obsidian/DevJobReady/Apprentissage/`). Le Suivi les **référence par lien**, ne les duplique pas. Le backlog forward-looking (exos en attente, dettes, brainstorms reportés, chantiers coupables) vit dans `~/Obsidian/DevJobReady/to-do.md`.

**Règle de lecture au démarrage de session** :
- Mardi-vendredi : focus journée veille + sem en cours.
- Lundi (ou 1ère session de la sem) : lecture intégrale sem précédente + sem en cours qui démarre, avec récap accomplissements + blocages + ajustements de la sem précédente avant le programme du jour.

**Archives** : voir `docs/planning/SUIVI/ARCHIVE/{YYYY-MM}.md`.

---

### Sem 23 (1-7 juin 2026) — condensée

**Archive verbatim** : voir [`ARCHIVE/2026-06.md`](ARCHIVE/2026-06.md) (section sem 23, jour par jour).

**Livraisons clés**

- **CRUD Outfit + composition** (PR #53/#54/#55, lun 1er juin) : controller 7 actions + setter `garment_ids=` (jointure pure, pas `fields_for`) + `validates :garments, presence: true` + combobox Tom Select multi-select à pills via importmap + Stimulus. Notes : [[has-many-through-composition-and-presence-validation]], [[tom-select-importmap-stimulus]], [[erb-nested-anchor-and-loop-tag-pitfalls]].
- **Active Storage sur Garment** (PR #59 + tests #61, mar 2 juin) : `has_one_attached :photo` + variants WebP + validation `active_storage_validations` (`content_type` + `size` + `spoofing_protection: true`) + swap `mini_magick`→`ruby-vips`. Piège purge `""→DeleteOne` neutralisé. Notes : [[active-storage-fundamentals-and-variants]], [[active-storage-validations-and-pitfalls]], [[object-fit-and-aspect-ratio]], [[minitest-active-storage-validations]].
- **Outfit collage adaptatif** (PR #69, mer 3 juin) : partial unique `_collage` taille-agnostique + eager loading anti-N+1 (`includes(garments: [:category, { photo_attachment: :blob }])`) + mosaïque plafonnée à 4 tuiles + compteur `+N`. Notes : [[eager-loading-sort-by-vs-order-and-nested-attachments]], [[outfit-collage-adaptive-mosaic]].
- **Tags comma-separated** (PR #74, jeu 4 juin) : concern `Taggable` mutualisé Garment+Outfit + attribut virtuel `tag_names` + `after_save :sync_tags` (`find_or_create_by` + downcase) + gem `bullet` (2 N+1 préexistants corrigés). Notes : [[taggable-concern-and-tag_names-virtual-attribute]], [[minitest-taggable-concern]], [[bullet-n+1-detection-and-eager-loading-fixes]].
- **Exo sandbox `auth-from-scratch`** (lun 1er soir, `~/code/sandbox/rails/` commit `f0c3609`) : `has_secure_password` from scratch sans Devise. Note : [[auth-from-scratch-sandbox-exo]] + entretien : [[Entretiens/Rails/auth-from-scratch]].

**Odin Rails (lectures profondes matin)** : chap. 27 Callbacks (mar 2), chap. 30+31 APIs paire (mer 3), chap. 34+35 CSS+JS Bundling paire (jeu 4). Notes : [[callbacks]], [[apis-and-building-your-own]], [[working-with-external-apis]], [[bundling-vs-importmap-rails-8]].

**Tailwind (lectures profondes avant-midi)** : Theme + Functions/Directives (mar 2), Dark mode + Colors palette (mer 3), Backgrounds/Borders/Effects/Opacity (jeu 4). Notes : [[theme-variables-and-directives]], [[dark-mode]], [[colors-palette]], [[backgrounds-borders-effects-v4]].

**CI/CD méta** : PR #65 (routine reco vidéos fin de session activée), PR #66 (workflow auto-fix Dependabot bundler CHECKSUMS — testé en réel sur PR #63 jbuilder + #64 puma ; friction connue : `GITHUB_TOKEN` ne retrigger pas CI → close+reopen manuel), PR #68 (push filter CI `master`→`main`).

**Décisions de design tranchées (ne pas re-proposer)**

- **Position spatial outfit** (drag-drop canvas, hyp. B) : reporté backlog → composition simple V1.
- **Compteur `+N` mosaïque** : non-cliquable V1 (card-lien + pills de `show` couvrent déjà le besoin).
- **API JSON N to F V1** : pas d'API (Hotwire monolith, pas de SPA à servir). Option `/api/v1/` portfolio Phase 4 à trancher.
- **Dark mode V1** : non (photos vêtements = perception colorimétrique correcte requise).
- **Tom Select setup** : pin CDN sur build complete wrappé `+esm` (pas vendoring importmap qui 404).
- **Processeur images Rails 8** : `:vips` (cohérence Dockerfile libvips + `brew install vips` dev).
- **Intégration Anthropic Phase 4** : gem officielle `anthropic-sdk-ruby` + service object `app/services/ai/outfit_suggester.rb` + credentials chiffrées `credentials.yml.enc`.
- **Sélection garments par clic dans l'index** (remplace Tom Select à ~100 pièces) : reporté backlog `FEATURES_FUTURES.md`, post-emploi après index filtrable.

**Concepts maîtrisés en live (ne pas réexpliquer comme débutant)**

- `has_many through:` + setter virtuel `garment_ids=` (jointure pure, pas `accepts_nested_attributes_for` / `fields_for`).
- `validates :garments, presence: true` voit la collection en mémoire avant save (≠ DB).
- Eager loading nested attachments : `photo_attachment: :blob` (proxy), pas `:photo`.
- Concern polymorphique mutualisé (`Taggable` sur Garment+Outfit) avec `as: :taggable`.
- Garde `nil` (préserver) vs `[]` (vider) pour attribut virtuel `tag_names`.
- Minitest 6 (Rails 8.1) a retiré `Object#stub` → stub Ruby pur via `define_singleton_method`.
- `respond_to do |format|` + négociation Accept header + `as_json only:` whiteliste (pas `except:`).
- Active Storage : piège purge `""→DeleteOne` (file_field vide), garde `garment_params` retirant `:photo` blank.
- 3 vidéos GoRails visionnées en autonomie mer soir : [[preserving-file-uploads-on-edit]], [[gorails-importmaps-with-rails]], [[strict-loading-activerecord]].

**Friction / blocages capturés**

- Setup Tom Select / importmap : ~35 min trial-and-error (lun 1er) → règle mémoire `feedback-verify-external-setup`.
- Questions de vérification floues 2× (mar 2 matin) → règle mémoire `feedback-concrete-verification-questions`.
- **Ven 5 + sam 6 décrochés** (fatigue post grosse semaine prod) → 2 soirs sandbox manqués → cumul exos reportés sem 24 (samedi 13 juin rattrapage).

---

### Sem 24 (8-14 juin 2026)

**Accompli**

- **Lundi 8 juin (démarrage, Claude app)** — Récap sem 23 + diagnostic du retard (ven/sam non faits + 2 soirs d'exos sandbox manqués). Programme du jour annoncé : Odin Turbo + Tailwind Typography + (aprem en CLI) filtres `link_to` + tags cliquables. **Kanban GitHub réaligné** : 15 issues closes en mode coach (`gh issue close X -c "..."` exécutés par Chris) → US01/02/03/06/07/08/09/10/11/12/13/14/15/16/17. Restent ouvertes : US04/05 (sem 25 profil), US18-21 (sem 25 social), US22-24 (Phase 4 IA). **3 notes Obsidian GoRails rétroactives** rédigées par Claude (vidéos regardées jeudi 4 juin soir) : [[preserving-file-uploads-on-edit]], [[gorails-importmaps-with-rails]], [[strict-loading-activerecord]] (⚠️ vidéo > 2 ans, API Rails 8 vérifiée inchangée). Index Rails Obsidian mis à jour avec les 3 entrées. **`personal-to-do.md` créé** dans la vault Obsidian (racine `DevJobReady/`) : checkbox + jour + priorité 🔴/🟡/🟢 ; 4 items posés pour samedi 13 juin (Huntr 🔴, relire Turbo Frames/Streams 🟡, décider SPA/MPA NtF 🟡, contentieux serrurier 🔴).

- **Lundi 8 juin (matin, Claude app)** — Odin chap. **36 Turbo** en lecture profonde + coaching Claude (vulgarisation prioritaire, Frames + Streams + format negotiation = avancé sous-le-capot). Couvert : modèle mental Hotwire (Drive + Frames + Streams + Stimulus + Native), **Turbo Frames** (PIP scope, `turbo_frame_tag @model` idiomatique via `dom_id`, `src` eager + `loading="lazy"`, `data-turbo-frame="_top"` pour sortir, ciblage externe, `aria-busy`), **Turbo Streams** (8 actions append/prepend/replace/update/remove/before/after/refresh, `target` singulier vs `targets` CSS selector pluriel, helpers Rails `turbo_stream.replace(..., partial:)`, templates `.turbo_stream.erb`, broadcast Action Cable via `after_create_commit { broadcast_append_to }` + `turbo_stream_from @record`), **négociation de format** (`respond_to` + `Accept:` header : Turbo n'est pas magique, il ajoute juste `text/vnd.turbo-stream.html` en tête de l'`Accept`). Bonus hors Odin : **morphing Turbo 8** (`<meta name="turbo-refresh-method" content="morph">` + `data-turbo-permanent` + `broadcasts_refreshes`). **3 questions de vérification entretien détaillées répondues par Claude** : (1) scoping frame avec lien interne ("Modifier" reste dans le frame = ✅ ; "Retour à index" reste dans le frame = ❌ frame vidé → fix `data: { turbo_frame: "_top" }`) ; (2) `update` vs `replace` avec Stimulus `data-controller` attaché (update conserve l'instance, replace fait disconnect+connect — choix prod = replace + partial self-contained) ; (3) piège `406 Not Acceptable` si on supprime `format.html` quand un client envoie `Accept: text/html` (Turbo OFF, JS désactivé, crawler) + pire : `create!` exécuté avant `respond_to` → record en DB + erreur 406 affichée. Knowledge Check Odin (4 Q : SPA, Hotwire, Frame, Stream) répondu par écrit. **Application directe Nine to Fine** : filtres `link_to` aprem sem 24 (frame `garments_list`), likes/comments sem 25 (stream replace + broadcast). Note Obsidian : [[turbo-frames-streams-and-format-negotiation]]. Index Rails Obsidian mis à jour.

- **Lundi 8 juin (avant-midi, Claude app)** — Tailwind docs **Typography** en lecture profonde + coaching Claude (vulgarisation forcée — 2 reprises de Chris sur clarté pendant Turbo le matin, règle `feedback-vulgarize-advanced-rails` re-flaggée pour Tailwind v4 aussi). Couvert : **Font family** (`font-sans/serif/mono` + `ui-sans-serif`/`ui-serif` qui pointent vers la police système native, customization via `@theme --font-display/--font-body`, namespace `--font-X` génère utility `font-X`, ordre critique des `@import` Google Fonts AVANT `@import "tailwindcss"`, feature settings OpenType). **Font size** (13 tailles `text-xs` à `text-9xl`, **rupture à `text-5xl`** : line-height par défaut couplé proportionnel sur les petites/moyennes tailles, forcé à 1.0 sur les ≥48px, shorthand `text-base/7` en `--spacing` units, bundling v4 `--text-h1--line-height/--letter-spacing/--font-weight`). **Font weight** (100→900, **piège faux gras synthétique** si la font ne charge pas le poids, variable fonts `font-[450]`). **Line height** (formule `calc(--spacing × n)`, unitless vs unit héritage, `leading-*` ≠ `space-y-*`). **Letter spacing** (em-based pour scale avec font-size, conventions pro : `-tracking-tight` sur gros titres, `tracking-wider` sur majuscules). **Text decoration** (5 propriétés combinables : line, color, style, thickness, offset + pattern lien moderne). **Q1 coaching** : diagnostic du chevauchement `text-5xl + leading-7` (ratio 28/48 = 0.58 = télescope vertical) + fix (retirer `leading-7`, garder défaut). 11 pièges gravés. **Proposition design system Nine to Fine actée** dans la note (à valider en CLI cet aprem) : Playfair Display (display) + Inter (body), échelle 5 tailles sémantiques bundlées v4 (`--text-h1/h2/h3/body/caption`). Note Obsidian : [[typography-fundamentals]]. Index Tailwind Obsidian mis à jour.

- **Lundi 8 juin (aprem, Claude Code CLI)** — **`GarmentFilter` Query Object livré (back complet, smoke test 5 cas OK), PR #79 mergée**. Mode coach pédago strict (Chris code, Claude guide ligne par ligne avec analogies). Spec verbale en chat (pas de doc spec séparée).

  - **Pivot design en début de session** : abandon du "un seul filtre à la fois" (spec sem 23) → **filtres combinables AND** (couleur ET catégorie ET tag ET brand). Argument Chris : Zalando/Uniqlo le font, c'est UX standard. Conséquence : **option B (routes nested) éliminée** (combinatoire `/categories/X/tags/Y/garments` ingérable) → **C choisi** (objet `q` paramétrisable `?q[color]=red&q[category_id]=3`).

  - **Design choisi** (récap des 9 décisions chat) : URL `?q[...]` plat ANDé, pipeline `inject` + dispatch dynamique via `send("by_#{key}")`, PORO (pas ActiveModel), sécurité par scope d'entrée `current_user.garments`, tri APRÈS filter au controller, strong params (`permit`) + slice défensif (3 filets de défense en profondeur), tests Minitest planifiés (9 cas), emplacement `app/queries/garment_filter.rb`. Périmètre V1 : `GarmentFilter` uniquement, pas d'`OutfitFilter` (YAGNI).

  - **Concepts/idiomes Ruby couverts** : `%i[...]` symbol array literal (et **piège virgule** silencieux : `tag_id,` devient `:"tag_id,"`), `.freeze` sur constantes (vs piège `FILTERS << :evil`), variable d'instance `@` vs locale (portée controller→vue), `||` nil-coalescing, `Hash#slice` + `*` splat, `inject` (accumulator pattern), blocks `{ |a, b| ... }`, `send` dispatch dynamique, string interpolation `"#{key}"`, safe navigation `&.`, early return `return X if cond`. Côté AR : `.where(col: val)` SQL paramétrisé, `.joins(:tags)` via `has_many through:` polymorphique (2 INNER JOIN générés), `.where(tags: { id: val })` syntaxe hash imbriqué, **lazy evaluation** (SQL exécuté à l'itération, pas au `.new`).

  - **Open/Closed Principle prouvé en live** : Chris a ajouté proactivement un 4ème filtre `brand` (`FILTERS` + `by_brand` + `:brand` au `permit`) → **zéro modif sur `apply`/`results`**. Le pattern marche.

  - **Smoke test 5 URLs** (logs SQL vérifiés) : `/garments` nu (identité) ✅ ; `?q[color]=red` (`WHERE color`) ✅ ; `?q[tag_id]=1` (`INNER JOIN taggings → tags` + `WHERE tags.id`) ✅ ; `?q[category_id]=1` (`WHERE category_id`) ✅ ; combo `color+category_id` (AND combiné dans un seul `WHERE`) ✅. Eager loading préservé via scope avant filter (`includes` reste actif, pas de N+1).

  - **Bugs traversés** : (1) `NameError uninitialized constant GarmentFilter` au 1er run = Zeitwerk ne re-scanne pas `app/queries/` créé après boot → restart serveur. (2) Virgule dans `%i[color category_id tag_id, brand]` → `:"tag_id,"` au lieu de `:tag_id` → filtre tag silencieusement cassé. (3) `private` indenté à 0 espaces (style RuboCop attend 2).

  - **Defense en profondeur** : 4 filets (scope `current_user.X` + `permit` controller + `slice(*FILTERS)` filter + `.where(col: val)` SQL paramétrisé). Une URL `?q[evil]=hack&q[user_id]=99999&foo=bar` est nettoyée en cascade : `foo` viré par `params[:q]` (hors scope), `user_id`+`evil` virés par `permit`.

  - **Reports résolus en fin d'aprem** : **Étape 3 tests Minitest livrée, PR #81 mergée** — 10 cas dans `test/queries/garment_filter_test.rb` (identité, chaque filtre seul incluant le JOIN polymorphique tag, combos AND 2 et 4 filtres, edge cases `slice` + `.blank?`, ⭐ **test de propriété de sécurité cross-user**). Fixture `black_jacket` (user=john, color=black) ajoutée à `garments.yml` pour rendre le cas 10 non-trivial (sans elle, l'isolation aurait passé par hasard). Bug vécu pédagogique : typo `result_empty` vs `results_empty` (Minitest `NameError: undefined local variable`). 10 runs, 19 assertions, 0 failure. Notes : [[minitest-garment-filter-query-object]].

  - **Reports résolus en fin d'aprem (front) — PR #83 mergée** : story 3+4 livrée en bloc. Mode coach pédago strict.

    - **Refactor model `Garment`** : constante `COLORS = %w[black white grey beige brown red orange yellow green blue purple pink].freeze` + `validates :color, presence: true, inclusion: { in: COLORS, allow_blank: true }` (`allow_blank` pour ne pas doubler le message d'erreur avec `presence`). Test Minitest associé.

    - **Normalisation des données dev** (avant validation `inclusion` activée) : 12 valeurs polluées détectées via `Garment.where.not(color: Garment::COLORS).pluck(:color).uniq` (français : `noir`/`blanc`/`rouge`/`vert`/`gris`, féminins/pluriels `noires`/`blanche`, composés `light grey & purple`/`blue denim`/`light blue`, typo `blanc ` avec espace en trop). Script `update_all` exécuté en `rails runner` avec mapping explicite. Cas tranché par Chris : `light grey & purple` → `grey` (V1 mono-color, multi-color reporté backlog). Vérification finale : 0 violation restante.

    - **Controller** : `GarmentsController#index` expose `@filter_params` (déjà permit-é, safe pour la vue, **convention "la vue ne touche jamais à `params` brut"**) + `@available_colors` (= constante model), `@available_categories` (= `Category.order(:position)`), `@available_brands` (= `current_user.garments.where.not(brand: [ nil, "" ]).distinct.pluck(:brand).sort`), `@available_tags` (= `current_user.tags.order(:name)`).

    - **Helper** : `pill_class(active)` dans `garments_helper.rb` (DRY des classes Tailwind actif/inactif, fond zinc-900+champagne si actif, fond blanc translucide si inactif).

    - **Vue index refondue** : `turbo_frame_tag "garments_list"` enveloppe **bandeau filtres + grille**, en-tête (h1 + boutons Add/Create) **hors** du frame (statique). Bandeau = 4 lignes (Color / Category / Brand / Tag) chacune avec pill "All" + pills par valeur. Propagation des filtres actifs via `current.merge(...)` (ajout/remplacement d'une dimension) et `current.except(:dim)` (suppression d'une dimension). `current = @filter_params.to_h.with_indifferent_access` au début de la frame pour permettre `current[:color]` (symbol) sur un hash à clés strings (piège classique Rails). Cast `.to_i` sur `current[:category_id]` et `current[:tag_id]` pour comparer correctement string-from-URL ↔ integer-from-AR. État vide adapté au contexte (filtres trop stricts vs pas encore de garments).

    - **Pattern Turbo Frame `_top`** : 2 `link_to` à l'intérieur du frame `garments_list` pointant vers des pages **sans** frame matchant doivent avoir `data: { turbo_frame: "_top" }` pour échapper au frame : (1) `link_to garment` qui wrap chaque card (→ show garment), (2) `link_to "Add your first piece..."` dans l'état vide (→ new garment). **Sans `_top`, Turbo affiche "Content missing" à la place de la grille** (bug latent : ne se déclenche qu'au premier clic post-rendering du frame).

    - **Partial `shared/_tags`** : pills tag deviennent `link_to tag.name, garments_path(q: { tag_id: tag.id }), class: "... hover:bg-zinc-200 ... transition"` (style cliquable + hover discret). Bug existant corrigé au passage : `flew-wrap` (typo) → `flex-wrap`.

    - **Bugs traversés en session** : (1) `NameError uninitialized constant GarmentFilter` (initial back, Zeitwerk + restart serveur) — vu sem 23 ; (2) typo `result_empty` vs `results_empty` (tests Minitest) ; (3) Chris a oublié de virer la virgule dans `%i[]` (vu back) ; (4) **"Content missing" Turbo** (le bug du jour qui a fait perdre du temps) = link_to dans le frame ciblant une page sans frame ; (5) virgule manquante entre `data: { turbo_frame: "_top" }` et `class:` dans `link_to` (Ruby `SyntaxError`) ; (6) RuboCop `Layout/EmptyLinesAroundModuleBody` sur le helper (autocorrect `bundle exec rubocop -a`).

    - **Reports actés au backlog `FEATURES_FUTURES.md`** : (a) **multi-color par garment** (suite de la discussion "light grey & purple") ; (b) **normalisation `brand` au save** (callback `before_save :normalize_brand` avec `.strip.titleize`, pour éviter la pollution future "Uniqlo"/"uniqlo"/"UNIQLO ").

  - **Reportés mardi 9 juin** : (a) **application design system typo** (Playfair Display + Inter + `@theme --text-h1/h2/h3/body/caption`) — déjà au programme polish navbar/footer mardi ; (b) **exo dirigé du soir** (non fait) ; (c) **reco 2 vidéos fin de journée** (non faite).

  - Notes Obsidian : [[query-objects-pipeline-inject-and-dynamic-dispatch]] (back, PR #79) + [[minitest-garment-filter-query-object]] (tests, PR #81) + [[turbo-frames-in-production-and-content-missing-trap]] (front, PR #83, nouvelle) + [[inclusion-validation-with-allow-blank]] (model, PR #83, nouvelle). Index Rails Obsidian mis à jour (sections "Active Record / Queries" + "Active Record / Validations" + "Asset Pipeline & Hotwire / Turbo Frames + Streams" + "Testing").

- **Lundi 8 juin (soir)** — **Exo dirigé sandbox non fait** (reporté samedi 13 juin rattrapage). **2 vidéos GoRails visionnées en autonomie par Chris** (accès Pro), notes Obsidian rédigées le 9 juin matin :
  - **[Stimulus JS Framework Introduction](https://gorails.com/episodes/stimulus-js-framework-introduction)** (Chris Oliver, 23 jan 2018, 18 min, Free) — **forward-looking** : préparation Odin chap. 37 Stimulus du mardi 9 juin matin. ⚠️ **Vintage lourde** : démontre Stimulus 0.9.0 (pré-1.0), syntaxe targets `data-target="X.name"` morte depuis v2 (remplacée par `data-X-target="name"` + déclaration obligatoire `static targets = [...]`). Concepts stables (3 piliers controllers/actions/targets + lifecycle `initialize`/`connect`/`disconnect` + philosophie "modeste" opposée React) OK pour le modèle mental. Note Obsidian : [[gorails-stimulus-introduction]].
  - **[Adding Turbo Frame Test Helpers](https://gorails.com/episodes/adding-turbo-frame-test-helpers)** (Chris Oliver, 16 juin 2025, 11 min, Pro) — **backward-looking** : consolide le `turbo_frame_tag "garments_list"` de la PR #83 mergée le matin/aprem même. **Forward-looking** aussi : prépare le setup RSpec mercredi 10 juin + tests rétroactifs `GarmentsController` jeudi 11 juin. Pattern helper Minitest `assert_turbo_frame(id, &block)` qui wrap `assert_select "turbo-frame#X"` (intention explicite + parser Nokogiri vs regex fragile) + variante system test Capybara + matcher RSpec `have_turbo_frame`. ⭐ **Test du piège `Content missing`** (system spec qui clique une card depuis le frame et vérifie qu'on sort vers la show sans erreur Turbo) — celui qui aurait attrapé le bug `_top` du lundi avant prod. À intégrer aux specs system jeudi/vendredi. Note Obsidian : [[gorails-turbo-frame-test-helpers]].

- **Mardi 9 juin (matin, Claude app)** — Odin chap. **37. Stimulus** en lecture profonde + handbook officiel (Origin / Introduction / Hello Stimulus) + reference complète (Controllers / Actions / Targets / Values / CSS Classes / Lifecycle Callbacks) + 3 exos sandbox dans `~/code/sandbox/stimulus/` (repo `ChrisCroc/sandbox-stimulus` créé en cours de session). Couvert : **philosophie "modeste"** Stimulus (vs React/Redux, sparkle sur HTML serveur, slogan "framework for the HTML you already have"), **4 piliers** Controllers + Actions + Targets + Values, **lifecycle** complet (initialize 1× / connect N× / disconnect N× + XTargetConnected/Disconnected + XValueChanged), naming conventions strictes (camelCase JS / kebab-case HTML / snake_case fichiers), registration auto via importmap, cross-controller communication (`dispatch` pub/sub + Outlets v3). **Exos sandbox** : (1) `hello_controller` (1er wiring, validation `data-controller` + `data-target` + `data-action`) ; (2) `slideshow_controller` v1 impératif (`this.index` + appel manuel `showCurrentSlide()`) ; (3) `slideshow_controller` v2 réactif (`static values = { index: Number }` + `indexValueChanged()` callback auto — la marche d'escalier vers le pattern réactif) ; (4) `content_loader_controller` (`fetch` + `setInterval` polling + `clearInterval` cleanup obligatoire dans `disconnect()`). **Pièges vécus en live** : interpolation Ruby `#{}` vs JS backticks `${}` (vu sur `hello_controller`) ; type minuscule `string` vs `String` qui raise `ReferenceError` (vu sur `content_loader_controller`). **3 questions style entretien junior Rails 90 sec répondues par écrit** : (1) `initialize()` vs `connect()` avec instanciation Chart.js + impact Turbo Drive ; (2) pattern impératif vs réactif avec compteur de panier (3 arguments concrets pour Values) ; (3) bug du timer fantôme `setInterval` sans cleanup (200 req/s en prod, code du fix). Knowledge Check Odin (4 Q) répondu par écrit. **Méta sandbox** : repo `ChrisCroc/sandbox-stimulus` créé (public, init commit du template `hotwired/stimulus-starter` + 3 exos). Convention commits sandbox `exo(<concept>): <desc>` actée. Note Obsidian : [[stimulus-fundamentals]]. Index Rails Obsidian mis à jour.

- **Mardi 9 juin (avant-midi, Claude app)** — Tailwind docs **Transitions & Animation** en lecture profonde (transition-property + duration + timing-function + animation, 4 pages). Couvert : distinction **transitions** (état A→B, déclenché par hover/focus/classe JS) vs **animations** (keyframes cycliques). **Famille `transition`** : utility défaut (liste longue propre color/opacity/shadow/transform), utilities ciblées (`transition-colors`, `transition-opacity`, etc.), **`transition-all` = code smell** (perf catastrophique sur listes + animations parasites + FOUC). `duration-*` (échelle 0-1000ms, défaut 150ms sweet spot, > 500ms = perception latence). `ease-*` (linear/in/out/in-out, défaut `cubic-bezier(0.4, 0, 0.2, 1)` Material), custom cubic-bezier `[bracket]` + `@theme --ease-X`. `delay-*` (tooltips, staggers). **5 animations built-in** (`animate-spin`/`ping`/`pulse`/`bounce`/`none`) avec patterns (spinner submit, badge ping, skeleton pulse). **Custom animations** : `--animate-X` + `@keyframes X` dans `@theme` (pas de fade-in/slide-up natif, à déclarer). ⭐ **Accessibilité non-négociable** : `motion-safe:` et `motion-reduce:` sur toutes animations infinies et transforms (WCAG 2.3.3 — risque nausée/épilepsie). **3 questions style entretien répondues par écrit** : (1) `transition-all` vs `transition` en code review (3 arguments concrets perf + parasites + FOUC + version corrigée) ; (2) spinner accessible avec `motion-safe:animate-spin` + texte alternatif "Loading…" ; (3) custom `animate-fade-in` Nine to Fine via `@theme` + `@keyframes` + intégration ERB + bonus cascade `stagger` avec `animation-delay`. 10 pièges gravés. Application N to F directe sem 24+ : 8 cas (pill filtre hover, card hover, submit spinner, skeleton loader, fade-in cards index, like button press, modal backdrop, toast slide-in). Note Obsidian : [[transitions-and-animations-fundamentals]]. Index Tailwind Obsidian mis à jour.

- **Mardi 9 juin (méta CI/CD)** — **2 PRs Dependabot bundler mergées** : PR #85 `image_processing 2.0.1 → 2.0.2` + PR #86 `bootsnap 1.24.5 → 1.24.6`. Workflow auto-fix lockfile CHECKSUMS (PR #66 sem 23) avait bien tourné dès l'ouverture des PRs (8 juin 19:36) mais la CI principale n'avait pas retriggered sur les fix commits (sécurité anti-loop GitHub sur push depuis `GITHUB_TOKEN`). Solution = close+reopen manuel pour forcer la CI sur HEAD (= le commit auto-fix). 5/5 checks SUCCESS sur les 2 PRs après reopen. Merge squash + delete-branch sur PR #85 OK direct. PR #86 a échoué la 1ère fois au merge (`mergeStateStatus: BEHIND` — main avait avancé suite au merge #85, branch protection rule exige "branche à jour"). `gh pr update-branch 86` (merge main into branch) → nouvelle CI re-déclenchée → 5/5 SUCCESS → merge squash OK. main HEAD final : `774d3bc`. Pull local + `bundle install` requis pour synchroniser les nouvelles versions de gems en dev. **Dette technique notée** : friction récurrente close+reopen+update-branch manuel à chaque Dependabot bundler. 3 solutions long terme à évaluer (PAT au lieu de `GITHUB_TOKEN` dans auto-fix workflow ; `workflow_dispatch`/`repository_dispatch` re-trigger ; Auto-merge GitHub native).

- **Mardi 9 juin (après-midi, Claude Code CLI)** — **Recherche live par nom sur l'index garments, PR à venir (en cours par Chris)**. Mode coach pédago strict. Empile 3 technos Hotwire en une seule feature : back (5e tamis `GarmentFilter#by_search`), front (controller Stimulus debounce), Turbo Frame (rafraîchissement grille). Spec verbale en chat (pas de doc séparée).
  - **Back** : `:search` ajouté à `FILTERS` + méthode `by_search(relation, value)` avec `ILIKE` + `ActiveRecord::Base.sanitize_sql_like` + paramètre lié `?`. Open/Closed Principle **prouvé une 2e fois en live** (2 lignes ajoutées : constante + méthode, zéro touche `apply`/`results`/`initialize`/`slice`). Le piège du `%` décortiqué : sans échappement, taper `%` produit `WHERE name ILIKE '%%%'` = match toute la garde-robe (aberration UX, pas faille de sécurité). **2 mécanismes anti-attaque distincts** : paramètre lié `?` (anti-injection SQL) + `sanitize_sql_like` (anti-aberration UX). Strong params controller : `:search` ajouté au `permit` (défense en profondeur préservée). Détail + 4 questions style entretien 90 sec → [[garment-filter-search-extension-ilike-sanitize]].
  - **Front Stimulus + Turbo Frame** : controller `search_form_controller.js` avec **debounce 300ms paramétrable via `static values`** + nettoyage timer dans `disconnect()` (anti timer fantôme — exactement le pattern `content_loader` du matin). ⭐ **Leçon UX majeure** : **form HORS de la frame** pour préserver le focus pendant le debounce (form DANS = focus perdu à chaque swap). Trade-off documenté + alternative Turbo 8 morphing reportée. ⭐ **Concept central débloqué** — les 3 catégories de "trucs" qu'un controller peut stocker : `targets` (DOM, déclaré pour scan Stimulus), `values` (HTML config, déclaré pour cast + accesseur), **propriété JS classique** `this.xxx` (état interne pur, JS dynamique, aucune déclaration — comme `this.timeout` ici ou `this.select` dans Tom Select). Tableau de décision réutilisable. **`requestSubmit` vs `submit` DOM** : avec Turbo, toujours `requestSubmit` (déclenche l'événement `submit` que Turbo écoute). Hidden inputs pour préserver les filtres actifs en GET (form GET sérialise tous ses champs visibles + cachés). **Convention de nommage Stimulus actée** : `scheduleSubmit`/`triggerX`/`toggleY`, jamais shadow d'API DOM (`submit`/`click`/`focus`). 12 pièges gravés + 4 questions style entretien → [[stimulus-debounce-and-turbo-frame-focus-preservation]].
  - **2 bugs vécus en live et corrigés** : (1) signature `by_search(value)` au lieu de `(relation, value)` + `@scope` au lieu de `relation` → `ArgumentError` au runtime + casse la pile de tamis sur la dimension search (ma faute de relecture, règle 6 CLAUDE.md global re-violée — actée pour la suite : relire le code proposé dans le contexte du fichier que je viens d'afficher) ; (2) form DANS la frame → focus perdu à chaque debounce (ma faute d'archi initiale, j'avais sous-estimé le coût UX). Corrections faites par Chris après diagnostic, tout marche en manuel sur localhost après corrections.
  - **Découverte isolation user en live** : Chris a tapé `Garment.where("name ILIKE ?", "%re%").pluck(:name)` en console → retour `["chemise légère blanche"]` (qui matche "re" dans "légère"), mais cette pièce n'apparaît pas dans son index. Explication : la console cherche dans **toute** la table `garments`, l'index est scopé `current_user.garments`. La chemise appartient à un autre user (seed/ancien compte). C'est l'isolation user qui marche correctement — preuve en live de la propriété de sécurité cross-user testée explicitement dans `garment_filter_test.rb` (cas 10, PR #81).
  - **Backlog `FEATURES_FUTURES.md` mis à jour** : (a) **Full-text search `pg_search`** (entrée existante enrichie avec contexte aujourd'hui + rejet explicite de la search multi-colonnes naïve pour ambiguïté UX) ; (b) **Sidebar filtres togglable** (nouvelle entrée, priorité moyenne, post-emploi — Chris a qualifié le bandeau de pills horizontal d'"horrible", veut basculer vers une sidebar type Zalando/Asos/Uniqlo togglable type drawer responsive). Stack Stimulus avancé (multi-targets + values + localStorage) + Tailwind transitions/responsive.
  - **Reste à faire en fin d'aprem** : (a) tests Minitest pour `by_search` (3 cas à ajouter dans `garment_filter_test.rb`) ; (b) polish navbar/footer avec design system typo (Playfair + Inter) ; (c) brainstorm UX sur 4 points (selector couleurs comme catégories, ajout catégories dresses/suits/swimwear, preview photo en `new`, suggestions tags existants).
  - **Reports** : exo dirigé soir sandbox + reco 2 vidéos fin de journée à caler en fin de session.

- **Mardi 9 juin (après-midi partie 2, Claude Code CLI)** — **Brainstorm UX 4 sub-projects + livraison 2 sur 4, PR #91 mergée**. Brainstorming structuré via skill `superpowers:brainstorming`. Mode coach pédago strict, vulgarisation forcée à plusieurs reprises (Chris a re-flaggé "tu n'as pas vulgarisé" à 2 occasions sur Stimulus + Tom Select → reset complet et reprise ligne par ligne).
  - **Sub-project #1 ✅ ajout catégories** Dress/Suit/Swimwear via `db/seeds.rb`. Trivial (5 min) + piège typo vécu (`poistion: 8` au lieu de `position: 8` → `RecordInvalid` au seed).
  - **Sub-project #2 ✅ color selector Tom Select** (spec + plan rédigés via skill `writing-plans`, 4 tâches TDD livrées). Constante `Garment::COLOR_HEX` (13 entrées Tailwind v4 shade-500 + cream HTML) + test invariant Minitest `COLORS.sort == COLOR_HEX.keys.sort`. Stimulus controller dédié `color_select_controller.js` (séparé de `select_controller.js` Outfit). `f.select` à 4 arguments avec 3-tuples `[label, value, { "data-hex": ... }]` pour injecter data-attributes par option. Discipline TDD Red→Green explicite (porte verrouillée → clé → porte ouverte). **Stack 4 couches** : Rails ERB + Stimulus + Tom Select + CSS centralisé `.ts-control` dans `application.css`. ⭐ **Le bridge data-attributes → JS data object** : Tom Select v2 strippe le préfixe `data-`, donc `data-hex` HTML → `data.hex` JS (vu en live via `console.log`). ⭐ **2 pièges majeurs vécus** : (a) `#` manquant dans `COLOR_HEX` → CSS invalide → pastilles toutes grises ; (b) classes Tailwind sur le `<select>` source → Tom Select les copie sur `.ts-wrapper` → double encadré avec `.ts-control` centralisé. Fix = pas de classes sur `<select>`, tout via `.ts-control`. 4 typos Ruby/Tailwind aussi vécues (`c.capitalized` au lieu de `c.capitalize`, virgule au lieu de `:` dans hash, `fovus:` au lieu de `focus:`). Détail + 4 Q/R entretien → [[tom-select-render-customization-color-selector]].
  - **Sub-projects #3 (photo preview new) + #4 (tag autocomplete) REPORTÉS** : option A choisie dans le brainstorming priorisation (quick wins d'abord + specs pour le reste un autre jour). Specs photo preview + tag autocomplete à brainstormer plus tard.
  - **Backlog `FEATURES_FUTURES.md` enrichi** au début de l'aprem partie 1 : `pg_search` + sidebar filtres togglable. À noter pour la sem 25 (avec features sociales).
  - **Specs + plans rédigés** : `docs/superpowers/specs/2026-06-09-color-selector-tom-select-design.md` + `docs/superpowers/plans/2026-06-09-color-selector-tom-select.md`. Commités sur la branche `feat/categories-colorpicker` avant l'implémentation.
  - **Lint CI échec rattrapé** : Rubocop `Layout/SpaceInsideHashLiteralBraces` sur `db/seeds.rb:20` (Swimwear hash sans espace avant `}`). Fix via `bundle exec rubocop -a` + commit `style:` + re-push → lint vert → merge. Friction CI à noter : prendre le réflexe `bundle exec rubocop` localement avant chaque commit, OU installer un pre-commit hook.
  - **Reports VERS MERCREDI 10 juin** : (a) **tests Minitest `by_search`** (3 cas dans `garment_filter_test.rb`, ~15-20 min — match partiel ILIKE, combo avec color, échappement du `%`) ; (b) **polish navbar/footer typo design system Playfair+Inter** (~1h-1h30) ; (c) **cleanup SUIVI hebdo** différé à demain matin au démarrage de session — condenser sem 23 vers `~/Obsidian/DevJobReady/Suivi/2026-W23.md` + résumé highlight dans SUIVI-COURANT + codifier la routine rolling lundi dans `CLAUDE.md` projet étape 7 démarrage de session (cible : SUIVI-COURANT 54 k → ~25 k chars).
  - **Reports VERS SAMEDI 13 juin (exos dirigés cumulés)** : exo dirigé soir mardi 9 juin **confirmé non fait** → reporté samedi. **Spec succincte exo Stimulus debounce** (pour retrouver demain/samedi) : repo `~/code/sandbox/stimulus/`, nouveau controller `debounced_search_controller.js` + bloc HTML standalone (input + ul vide), comportement = debounce 300ms paramétrable via `static values = { debounceMs: Number }`, target `input` + `results`, propriété d'instance pure `this.timeout` (pas `values`, pas `targets`), méthode `scheduleAppend(event)` (jamais `submit` — collision API DOM, leçon mardi aprem), lifecycle `disconnect()` obligatoire avec `clearTimeout(this.timeout)`. Cas limite pédagogique à reproduire : coder d'abord SANS `clearTimeout` dans `disconnect()`, puis dans la console JS `document.querySelector('[data-controller]').removeAttribute('data-controller')` + 1 dernière frappe juste avant → `<li>` apparaît après déconnexion = preuve du timer fantôme, fix → silence radio. Question entretien 90 sec en clôture : "Tu codes un autocomplete Stimulus qui hit ton API toutes les frappes. Tu mets quel debounce ? Justifie. Et pourquoi le `clearTimeout(this.timeout)` dans `disconnect()` est non-négociable en prod ?". Commit cible : `exo(debounce): debounced search with values, target lifecycle and timer cleanup`.
  - **Reco 2 vidéos fin de journée** ✅ sélectionnées par Claude Code CLI (WebFetch GoRails vérifiée — search épisode KO 520, fallback par series Stimulus JS + Hotwire) et **visionnées en autonomie par Chris ce soir** (accès Pro). Notes Obsidian à rédiger demain matin en rétroactif comme pour les 3 vidéos lundi soir sem 24.
    - **[Auto-submitting Forms & Custom Turbo Stream Actions](https://gorails.com/episodes/custom-turbo-stream-actions-and-auto-submitting-forms)** (Chris Oliver, 27 mars 2023, 10 min, Pro) — **backward** : couvre le pattern auto-submit form on input change + Turbo Streams pour rafraîchir la liste = squelette exact du `search_form_controller` + `turbo_frame_tag "garments_list"` codé mardi aprem. Source code dispo : https://github.com/gorails-screencasts/487-auto-submitting-forms. ⚠️ vidéo > 2 ans, à vérifier que Turbo 8 morphing ne change rien au pattern `form > input + Stimulus listener + requestSubmit` (a priori intact, mais confirmer en notes Obsidian).
    - **[How to use Stimulus JS 2.0's new Values and CSS Classes APIs](https://gorails.com/episodes/stimulus-js-2)** (Chris Oliver, 7 déc 2020, 10 min, Pro) — **backward** : couvre l'API `static values` (typage Number/String/Boolean + callback `xxxValueChanged`) utilisée mardi matin (Odin chap. 37, slideshow v2 réactif sandbox) et mardi aprem (`debounceMs: Number` dans `search_form_controller`). ⚠️ vidéo > 5 ans, flag fort : l'API Values **est restée stable** Stimulus 2 → 3, le code de l'épisode reste valide. Si Chris Oliver montre du Webpacker / Rails 6, transposer vers importmap Rails 8.
  - **3 fautes de relecture méta côté Claude** actées dans Blocages : `data-turbo-frame` redondant, signature `by_search`, form DANS la frame. + 2 nouvelles : (a) vulgarisation insuffisante 2x sur Stimulus/Tom Select (reset forcé), (b) faute pédagogique de nommage `submit` Stimulus en collision avec `form.submit()` DOM (renommé `scheduleSubmit`). Règles mémoire existantes `feedback-vulgarize-advanced-rails` + `feedback-explain-every-line-and-context` à appliquer plus strictement.

- **Mercredi 10 juin (démarrage de session, Claude app)** — **Refonte stratégique cleanup SUIVI hebdo + architecture `@import` au démarrage CLI.** Brainstorm structuré (skill `superpowers:brainstorming`) sur 3 problèmes liés : (1) SUIVI à 57 k chars chargés en `@import` auto → friction perception "doc trop lourd au démarrage CLI", (2) sem 23 non condensée violait la politique de verbosité, (3) pas de routine de condensation hebdo codifiée. **3 itérations de design** : option F (archive + condensation) → option B+C revisée → option finale "retirer SUIVI du @import auto + Read étape 0". **Livré** : (a) **cleanup matin sem 23** — copie verbatim vers `docs/planning/SUIVI/ARCHIVE/2026-06.md` créé + condensation sem 23 dans SUIVI-COURANT selon règle préservation (livraisons clés, décisions de design tranchées, concepts maîtrisés en live, friction/blocages, reports backlog) + suppression pavés techniques (vivent dans notes Obsidian par concept). 57k → 41k chars. (b) **Routine de condensation hebdo codifiée** dans `CLAUDE.md` projet étape 7 (lundi rolling, cible ≤ 8 k chars condensé par semaine, audit avant exécution). (c) **Refonte `@import` auto** : `@docs/planning/SUIVI/SUIVI-COURANT.md` retiré du `@import` auto dans CLAUDE.md projet + nouvelle **étape 0 obligatoire** dans la routine "Démarrage de session" (Read SUIVI avant tout, sans quoi étapes 5/6/7 inexécutables ; Read à la demande pour sessions non rituelles). **Bilan tokens** : `@imports` auto 22 k → ~12 k tokens (−45 %), sub-agents allégés du même montant. `phase-1.md` gardé en `@import` (vision long terme planning, perdre Read à la demande non viable). **Méta-pattern méthodo** : Claude a 3 fois sur-optimisé pour la métrique tokens avant que Chris recadre sur objectif réel ("Claude doit toujours avoir le contexte + Chris doit toujours accéder à ses notes + zéro bruit"). Reconnaissance d'erreur explicite + révision honnête à chaque tour. Règle 1 CLAUDE.md global (challenge sans flatterie, dire "je me suis trompé") appliquée.

- **Mercredi 10 juin (matin, Claude app)** — Odin chap. **38. Mailers** en lecture profonde + Rails Guides 8.1 Action Mailer Basics (sections 1-2, 6-7, 8.2, 9-10 selon reco Odin). Couvert : **mailer = hybride controller + model** (`rails g mailer User` génère mailer + views + tests + previews) ; **pattern moderne `Mailer.with(user: @user).action.deliver_later`** vs ancien `Mailer.action(@user)` (sérialisation GlobalID compatible Active Job + Solid Queue Rails 8 vs sérialisation fragile d'instance complète) ; **vues `app/views/<mailer>/<action>.html.erb` + `.text.erb`** (multipart MIME auto, anti-spam) **vs layout `app/views/layouts/mailer.html.erb`** (piège VIEW vs LAYOUT loupé en KC q2) ; **`deliver_later` (Active Job Solid Queue) vs `deliver_now` (cron/test/console)** ; **les 2 familles de callbacks** — `*_action` (variable `mail`, avant/après construction email : hydrater @vars, override SMTP settings runtime multi-tenant, `perform_deliveries = false` killswitch, headers custom) vs `*_deliver` (variable `message`, avant/après envoi SMTP : sandbox staging `message.to =`, `throw :abort` désabonnement, audit log `EmailDelivery.log`, `.touch(:delivered_at)` timestamp) ; **pipeline complet** schématisé (before_action → action+view → after_action → before_deliver → SMTP → after_deliver) ; **`attachments.inline[..]` vs `attachments[..]`** + cas N to F variants WebP Active Storage (`.processed` mandatory avant `.download`) ; **`*_url` mandatory dans vues** (jamais `*_path` car emails hors contexte HTTP) + config `default_url_options host:` ; **2 contraintes styling HTML emails** : pas de stylesheet externe (inline ou `<style>` tag, gem `premailer-rails`), support CSS limité Outlook = retour aux `<table>` (frameworks pro MJML, Foundation for Emails) ; **`.touch(:column)`** : 1 UPDATE SQL direct + skip validations + `before/after_save` + déclenche `after_touch` + `after_commit` (lien direct [[callbacks]] chap. 27 mardi 2 juin) ; **3 mécaniques d'abort** distinctes (`mail.perform_deliveries = false`, `throw :abort` dans before_deliver, `body = "..."` dans before_action) ; **Interceptors vs Observers** (global DRY mais opaque vs local explicite) ; **Mailer Preview** (rendu statique, design itératif) vs **Letter Opener** (intercepte deliver_now, flow réel). ⭐ **Lien retro chap. 27 Callbacks** : combo `after_save` + `deliver_later` + transaction rollback = job orphelin + `RecordNotFound` worker crash → **toujours `after_commit on: :create`** pour side-effects externes (mailer/job/webhook/broadcast). **Knowledge Check Odin (6) répondues par écrit** par Chris : 3 correctes (1, 5 partielle), 2 confusions importantes (4 : use cases `after_action`/`before_deliver` mélangés, "feedback" mal précisé pour after_deliver), 1 faute structurelle (2 : vue vs layout), 1 style obsolète (3 : ancien pattern `Mailer.action(arg)` au lieu de `with(...)`), 1 zappée (6 : contraintes styling). Toutes corrigées en chat. ⭐ **3 questions style entretien 90 sec rédigées par Claude (à la demande de Chris)** : (1) `deliver_later` + `after_save` vs `after_commit` dans transaction rollback (trace complète + fix + bonus `after_save_commit` alias) ; (2) Active Storage en inline dans mail récap N to F (code complet + 3 pièges `.inline` vs pièce jointe / performance `deliver_later` / `.processed` mandatory + trade-off inline vs URL externe) ; (3) Mailer Preview vs Letter Opener (duo complémentaire, Preview = itérer design / Letter Opener = vérifier flow + follow-up `assert_emails` + multi-states + délivrabilité réelle via webhooks). **Décision tranchée pédago** : pas de config Action Mailer dans Nine to Fine ce matin (4 raisons : viole ritual matin = lecture profonde sans code, YAGNI strict, premature config qui sera refaite sem 25, risque oubli contexte dans 5 jours). Setup repoussé à sem 25 (1er mailer follow notif). 2 notes Obsidian rédigées : [[mailers-fundamentals]] (Apprentissage Rails Mailers Jobs Realtime, 17 sections + 12 pièges + 6 KC + lien entretien) + [[../../../Entretiens/Rails/mailers]] (3 questions 90 sec format Q/Réponse/Pièges/Follow-up). Index Rails Obsidian mis à jour avec section "Action Mailer" dans Mailers, Jobs & Realtime (entrées détaillées) + sous-sections Active Job + Action Cable flaggées "à venir". 12 pièges gravés.

- **Mercredi 10 juin (avant-midi, Claude app)** — Tailwind docs section **Transforms** en lecture profonde (translate + rotate + scale + skew + transform-origin + intro 3D context perspective/transform-3d, ~6 pages doc v4 fetchées en parallèle). Couvert : **4 opérations 2D** (translate déplace, rotate tourne, scale agrandit, skew incline) + transform-origin pivot + 3D context. **Sémantiques différentes pour le même `N`** : `translate-N` = spacing scale (`--spacing × N`, default 4px), `rotate-N` = degrés, `scale-N` = pourcentage (`scale-110` = 110%), `skew-N` = degrés. **Translate** : axes X/Y/Z, fractions = % de la taille de l'élément (pattern centrage absolu `left-1/2 -translate-x-1/2`), `translate-full` 100%, `translate-px` 1px. **Rotate** : degrés défaut, arbitrary radians OK, 3D `rotate-x/y/z`. **Scale** : presets 0/50/75/90/95/100/105/110/125/150/200, **`-scale-x-100` = flip horizontal idiome icônes**, pas de layout shift (avantage perf + piège clipping/overlap). **⭐ LE mécanisme clé v4 — composition via CSS variables internes** : chaque utility écrit dans SA variable (`--tw-translate-x`, `--tw-rotate-z`, `--tw-scale-x`, etc.), une règle racine compose le `transform` final. Conséquence : on empile N transforms sans écrasement (en CSS pur, `transform: rotate()` puis `transform: scale()` s'écraseraient — la 2e tue la 1ère). Nouveau et fiable en v4 (vs v3 nécessitait classe `transform` explicite oubliée). **3D context** : 3 ingrédients (`perspective-*` parent + `transform-3d` parent + transform 3D sur l'enfant), sinon aucun effet visible. Analogie caméra qui regarde la scène. **Performance** : rendering pipeline du navigateur — transform + opacity = composite GPU only = 60 fps mobile ; width/height/margin/padding = layout + paint + composite = saccades 30 fps. JAMAIS animer width pour entrées/sorties d'éléments. **Skew rare en prod web sérieuse** (badges en biais, effets éditoriaux), à ranger comme "outil dispo". **Application N to F immédiate** : 5 patterns concrets — card lift index garments (`relative + hover:scale-105 + hover:z-10 + transition-transform`), pills filtre lift (`-translate-y-0.5 + shadow + transition-[transform,box-shadow]`), like button press (`active:scale-95`), flip d'icône expand/collapse dropdown (`rotate-180`), modal entrance (`opacity-0 + scale-95` → `opacity-100 + scale-100` avec `transition-all` toléré pour 2 props GPU). ⭐ **3 questions style entretien 90 sec rédigées par Claude (à la demande de Chris)** : (1) hover scale sur card + clipping/overlap + z-index (`relative + hover:z-10` mandatory + raison technique) ; (2) combiner rotate + scale au hover (mécanisme CSS variables Tailwind v4 vs CSS pur écrase) ; (3) modal entrance scale + opacity + pourquoi PAS width/height (rendering pipeline GPU vs layout reflow, `transition-all` exception légitime). **12 pièges gravés** dont scale layout shift + clipping + overlap z-index, `transition-all` code smell sauf exception modal, jamais animer w/h/margin/padding, sémantiques différentes N par utility, fractions = % de l'élément (pas du parent), 3D sans perspective parent = aucun effet, transform-origin default center, `-scale-x-100` flip idiome. Note Obsidian rédigée : [[transforms-fundamentals]] (Apprentissage Tailwind/Transforms/, 12 sections + 3 questions entretien intégrées + lien rétroactif [[transitions-and-animations-fundamentals]] et [[backgrounds-borders-effects-v4]]). Index Tailwind Obsidian mis à jour (section Transforms remplie + ordre de lecture étape 12 ✅).

- **Mercredi 10 juin (après-midi, Claude Code CLI)** — **3 chantiers livrés, 2 PR mergées + 3 notes Obsidian** (mode coach pédago strict). Mode coach pédago à RE-flagger 3 fois en session (squelettes nus pour by_search cas 1-3 + question "2 candidats" floue + bloc 12 `it` balancé sans coaching pour la migration RSpec) — règle mémoire `feedback-explain-every-line-and-context` existe depuis sem 22, à RE-appliquer plus strictement.

  - **Chantier 1 — 3 tests by_search Minitest (PR #95 mergée)** : 3 cas dans `test/queries/garment_filter_test.rb` (a) `"results filters by search with partial ILIKE match"` (valeur `shirt`, prouve matching partiel + insensible casse) ; (b) `"results filters by search and by color with AND"` (valeur `jean` + `blue`, prouve participation `by_search` au pipeline `inject`) ; (c) `"results escapes % wildcard in search to prevent matching everything"` (valeur `%`, prouve justification ligne `sanitize_sql_like`). Choix `refute_equal @chris_scope.pluck(:id).sort, result.pluck(:id).sort` plutôt que `assert_empty` pour robustesse vs évolution future des fixtures. 1 piège vécu : Chris avait écrit `garments(:blue_jean)` au lieu de `garments(:blue_jeans)` (typo). 13 runs, 0 failure côté Minitest. Solde la dette de PR #83.

  - **Chantier 2 — Design system typo Tailwind v4 (PR #95 mergée)** : 1ère application en prod du design system construit lundi 8 juin matin (cf. [[typography-fundamentals]]). 3 sous-étapes : (a) imports Google Fonts AVANT `@import "tailwindcss"` (piège ordre vu lundi — sinon `@font-face` arrivent trop tard) ; (b) ajout `--font-display: "Playfair Display", serif` + `--font-body: "Inter", system-ui, sans-serif` dans `@theme` existant ; (c) 5 tailles sémantiques bundlées `--text-h1/h2/h3/body/caption` avec sous-propriétés `--line-height/--letter-spacing/--font-weight` (échelle géométrique × 1.5, 12px → 48px). Application navbar : `text-lg font-bold` → `text-h2 font-display` sur Closet/Dressing, `text-sm font-medium` → `text-caption` sur user/logout. 1 typo vécue : `sasn-serif` (navigateur ignore silencieusement les valeurs CSS inconnues). ⭐ ⚠️ **LE PIÈGE MOBILE-FIRST flaggé par Chris en live** : valeurs canoniques pro calibrées **desktop-first** (text-h2 30px / text-caption 12px) → navbar cassée sur app mobile-first Nine to Fine (Closet/Dressing énorme, logout/username en dessous seuil accessibilité tactile Apple HIG 17px / Material 14px). Mémoire `project-nine-to-fine-mobile-first` créée dans `~/.claude/projects/.../memory/` pour toutes sessions futures. Note Obsidian : [[v4-design-system-and-mobile-first-pitfall]] avec 8 pièges. Échelle desktop laissée en l'état, refonte mobile-first reportée à fin d'aprem (chantier 4 si temps).

  - **Chantier 3 — Setup RSpec + factory_bot + migration garment_filter (PR à venir)** : Migration totale décidée par Chris après brainstorm 4 options (factories cohabitent avec fixtures vs migration totale). **Setup complet** : ajout `rspec-rails ~> 8.0` + `factory_bot_rails ~> 6.5` dans `:development, :test`, `bin/rails g rspec:install` (3 fichiers `.rspec`/`spec_helper.rb`/`rails_helper.rb`), `bundle binstubs rspec-core` (génère `bin/rspec` qui n'est pas auto-créé), config `config.include FactoryBot::Syntax::Methods` dans `rails_helper.rb`, suppression de `config.fixture_paths = [...]` (signal migration totale). **5 factories créées** dans `spec/factories/` : `user.rb` (sequence email + username + password fixe), `category.rb` (sequence name + position), `tag.rb` (1ère factory avec association `user` magique = équivalent `association :user`), `garment.rb` (2 associations user + category, valeur fixe `color { "black" }` car pas d'unicité), `tagging.rb` (polymorphique : `taggable { create(:garment) }` lazy explicite parce que factory_bot ne devine pas la classe). **1 fichier migré en live coach** : `test/queries/garment_filter_test.rb` → `spec/queries/garment_filter_spec.rb` (13 examples verts). Mapping syntaxe Minitest → RSpec capté en live : `assert_includes` → `expect().to include`, `refute_includes` → `expect().not_to include`, `assert_equal expected, actual` → `expect(actual).to eq(expected)` (⚠️ ordre inversé), `setup do` → `before do` ou `let`, fixtures `garments(:foo)` → factories `let!(:foo) { create(:garment, ...) }`. Concept `let` vs `let!` (lazy memoized vs eager — `let!` nécessaire quand record doit persister sans être nommé dans le spec, ex : "scope unchanged when params empty" qui ne nomme jamais `black_tshirt`). 13 fails sur 1er run cause **DB de test polluée par fixtures Minitest** résiduelles ("Username has already been taken") → fix `bin/rails db:test:prepare` (purge + recharge schema). Suppression de `test/queries/garment_filter_test.rb` après migration verte. **CI workflow adapté** : job `test` lance maintenant `bin/rails db:test:prepare && bin/rails test && bin/rspec` (séquence pendant la transition). 2 notes Obsidian rédigées : [[rspec-and-factory-bot-setup]] (Big Picture setup + 5 factories + let/let!) + [[minitest-to-rspec-migration-patterns]] (tableau mapping + workflow + estimation par fichier pour les 11 restants à migrer en autonomie jeudi matin). Index Rails Testing mis à jour avec les 2 nouvelles entrées.

  - **Chantier 4 — Refonte mobile-first des tokens typo (PR #98 mergée)** : tokens `--text-X` du `@theme` revus mobile-first par défaut (sans media queries qui ne sont pas supportées par `@theme` v4). Valeurs : `--text-h1` 48 → 32px, `--text-h2` 30 → 20px, `--text-h3` 20 → 18px, `--text-body` inchangé (16px = norme web universelle WCAG AA min), `--text-caption` 12 → 14px (au-dessus du seuil min Material Design d'accessibilité tactile). Sous-propriétés (line-height, letter-spacing) ajustées légèrement pour préserver lisibilité mobile. H1 des pages laissés intacts (responsive scale-up `md:text-*xl` conservé par décision Chris — refus de perdre travail responsive déjà fait). **Footer abandonné** après réflexion sur usage moderne : apps mobile-first (Instagram, X, TikTok, Spotify) n'ont pas de footer global dans le layout. Le footer est convention "site marketing web" pas "app". Si mentions légales B2C nécessaires plus tard, à créer dans `pages/home.html.erb` only (pas dans le layout). Décision actée comme finale, pas de report.

  - **Méta workflow** : Chris a établi le programme aprem clair en 4 séquences (commit/push/PR/merge/pull entre chaque). Routine end-of-day déclenchée par Chris avant chantier 4 pour éviter de tout cumuler en clôture (notes Obsidian + SUIVI MAJ chantiers 1-3 dans PR #97). Routine de clôture finale (chantier 4 + footer décision) dans PR à venir #99.

  - **⚠️ Méta erreurs Claude session (acté par Chris en clôture)** : (a) squelettes nus 3 fois (by_search cas 1-3 + 12 `it` RSpec en bloc) — règle mémoire `feedback-explain-every-line-and-context` existe sem 22, à RE-appliquer même sous pression de temps ; (b) calibrage typo desktop-first sur app mobile-first sans poser la question contexte → nouvelle memory `project-nine-to-fine-mobile-first` créée ; (c) typo `"tshi"` proposée pour matcher `Black T-shirt` mais ne matche pas à cause du tiret — faute de relecture (règle 6 globale "Relis-toi avant de proposer" à RE-appliquer) ; (d) confusion `test/queries` vs `spec/queries` dans rédaction (file path ambigu) ; (e) footer poussé par inertie sem 24 du SUIVI au lieu de questionner pertinence ; (f) proposition de refonte des h1 qui aurait fait perdre le travail responsive déjà mis en place. Patterns dominants : shortcut pédagogique sous pression de temps + récitation de pattern sans relecture du contexte affiché. Pas de nouvelle règle mémoire à créer (les existantes couvrent), application plus stricte à venir.

  - **Reportés vers JEUDI 11 juin** : (a) migration des 11 fichiers Minitest restants en autonomie (7 models + 3 controllers + 1 outfit_garment, ~1h45-2h30, pattern documenté dans [[minitest-to-rspec-migration-patterns]]) ; (b) suppression dossier `test/` après migration complète + retrait de `bin/rails test` du CI workflow.

  - **Reportés vers SAMEDI 13 juin (exos dirigés cumulés)** : exo dirigé soir lundi + mardi + mercredi non faits → reportés samedi. **Spec succincte exo Stimulus debounce** (pour retrouver demain/samedi) : repo `~/code/sandbox/stimulus/`, nouveau controller `debounced_search_controller.js` + bloc HTML standalone, debounce 300ms paramétrable via `static values`, propriété d'instance pure `this.timeout`, lifecycle `disconnect()` obligatoire avec `clearTimeout(this.timeout)`, cas limite pédagogique du timer fantôme à reproduire.

- **Mercredi 10 juin (soir, Claude Code CLI)** — Exos dirigés du soir reportés samedi 13 juin (3e soir cumulé : lundi + mardi + mercredi). Reco 2 vidéos sélectionnées par Claude Code CLI (WebFetch GoRails vérifié), à visionner en autonomie ce soir, notes Obsidian à rédiger demain matin en rétroactif (pattern habituel).

  - **Exos sandbox** (spec complète dans `~/Obsidian/DevJobReady/to-do.md` section "Exos sandbox en attente / Sem 24 — cumulés samedi 13 juin") :
    - **Rails Mailer + `after_commit` + RSpec `have_enqueued_mail`** dans `~/code/sandbox/rails/` (repo `ChrisCroc/sandbox-rails`) : model `Subscription` minimaliste + mailer pattern moderne `Mailer.with(subscription:).welcome.deliver_later` + reproduction live du bug job orphelin (`after_save` au lieu de `after_commit on: :create` + transaction avec rollback → job en queue + `RecordNotFound` worker) + tests RSpec `have_enqueued_mail` 2 cas (enqueue sur create ✅, pas d'enqueue sur rollback ✅). Liens : [[mailers-fundamentals]] + [[callbacks]] (chap. 27 Odin, lien retro `after_commit` mandatory side-effects externes) + [[rspec-and-factory-bot-setup]]. Commit cible : `exo(mailer): demonstrate after_save vs after_commit + rspec have_enqueued_mail`. Question entretien 90 sec intégrée à la spec.
    - **Tailwind Transforms modal + card hover** dans `~/code/sandbox/tailwind/transforms-modal-and-hover/` : 1 fichier `index.html` standalone via CDN, 4 cards hover scale-105 + z-10 + shadow + transition-transform, 2 modals comparatifs (modal A = scale-95→100 + opacity-0→100 GPU composite vs modal B = width 0→100% layout reflow) avec DevTools Performance pour démontrer la différence visuelle FPS. Bonus `motion-safe:` (WCAG 2.3.3). Lien : [[transforms-fundamentals]]. Commit cible : `exo(transforms): modal scale-opacity vs width-animate + card hover scale-z10`. Question entretien 90 sec intégrée à la spec.

  - **Reco 2 vidéos fin de journée** ✅ sélectionnées par Claude Code CLI (WebFetch GoRails vérifié titre + URL + date), à visionner en autonomie ce soir, notes Obsidian rédigées demain matin en rétroactif :
    - **[How to Embed Images with Inline Email Attachments in rails](https://gorails.com/episodes/how-to-embed-images-with-inline-email-attachments-in-rails)** (Chris Oliver, 6 avril 2026, 8 min, GoRails Pro) — **backward** : couvre exactement `attachments.inline[..]` + Content-ID documenté ce matin dans [[mailers-fundamentals]] section 9, qui s'applique directement au cas N to F mailer notification avec photo de garment Active Storage (variant WebP `.processed` mandatory). Vidéo RÉCENTE (2 mois, dans la zone safe Rails 8), pas de flag âge.
    - **[How to write Controller & Integration Tests in Rails](https://gorails.com/episodes/rails-integration-tests)** (Chris Oliver, 2 oct 2019, 14 min, GoRails Pro) — **forward** : prépare la dette technique RSpec controllers `GarmentsController` (7 actions + test IDOR cross-user) prévue jeudi 11 + vendredi 12 juin. ⚠️ **Vintage 6.5 ans (Rails 6.0 époque, syntaxe Minitest très probable)** : extraire le PATTERN universel de test controller (setup 2 users, `get/post/patch/delete`, `assert_redirected_to`, `sign_in` helper Devise, IDOR avec un 2nd user), puis transposer en RSpec request specs via le tableau de mapping de [[minitest-to-rspec-migration-patterns]] section 2 (`assert_X` → `expect().to ...`, `setup` → `before`/`let`, `assert_redirected_to` → `expect(response).to redirect_to`, etc.). Le PATTERN est universel, seule la syntaxe diffère.

- **Clarté + vulgarisation insuffisante** côté Claude pendant le slot matin Turbo (2 reprises de Chris : "tes explications ne sont pas du tout claires ni vulgarisées", flaggée également sur question Tailwind Q2 "je ne comprends pas ce que tu me demandes"). Les règles mémoire `feedback-vulgarize-advanced-rails.md` et `feedback-concrete-verification-questions.md` **existent déjà** (sem 22 et sem 23), Claude a re-rechuté → pas de nouvelle règle à créer, à appliquer plus strictement. Pattern à surveiller : Tailwind v4 (concepts CSS-first + `@theme` + namespaces) tombe aussi dans la catégorie "avancé sous le capot" qui exige analogie concrète d'abord, jargon après.

- **Manque de relecture du code proposé côté Claude pendant l'aprem** (3 occurrences) : (1) `data-turbo-frame="garments_list"` redondant ajouté au form_with alors que le form était DANS la frame (Chris a flaggé "comment t'as pu te louper alors que tu avais le contexte" — vraie question légitime) ; (2) signature `by_search(value)` au lieu de `(relation, value)` sans relecture du pattern existant des autres méthodes `by_X` ; (3) form DANS la frame → focus perdu pendant le debounce, sous-estimation du trade-off UX. **Cause racine** : récitation de pattern sans relecture du code proposé dans le contexte du fichier que je viens d'afficher. Pas de nouvelle règle mémoire (la règle 6 CLAUDE.md global "Relis-toi avant de proposer" suffit), à appliquer plus strictement. Engagement pris : avant chaque bloc de code proposé, relecture mentale contre l'extrait du fichier que je viens d'afficher + chercher redondances/conflits + vérifier que je cite la doc et pas la mémoire sur les comportements par défaut framework.

- **Jeudi 11 juin (matin, Claude app)** — Odin chap. **40 Advanced Topics** lecture profonde **6 sections** couvertes + **6 notes Obsidian** rédigées (5 solides + 1 first-pass) + 1 nouveau dossier vault `Apprentissage/Rails/Routing/` créé + 4 entrées MAJ index Rails + 1 entrée FEATURES_FUTURES.md + 3 entrées to-do.md (quick fixes UX/SEO + cours dédié metaprog + setup i18n complet) + 1 nouvelle memory globale `project-nine-to-fine-locale-english`. **Section 1 Advanced Routing** ([[advanced-routing-fundamentals]]) : singular `resource :profile` (6 routes sans `:id`, ressource implicite liée à `current_user`, distinction sémantique singular vs pluriel) + nested 1 niveau max (likes/comments sous outfit, jamais 2 niveaux) + member/collection règle discriminante "besoin d'`:id` ?" + `only:` minimiser surface + redirects/wildcards (`redirect("/x")` défaut 301 caché, glob `*path` toujours en dernier). 4 Q répondues (Q1 singular ✅, Q2 nested ✅ avec compléments controller idiomatique + button_to vs link_to, Q3 favorite ❌ confusion member/collection corrigée, Q4 listing favoris ambiguïté URL). 12 pièges gravés. Application sem 25 routes.rb complet posé. **Section 2 Controllers sans model + REST** ([[controllers-without-models-and-rest-principles]]) : casse le mythe 1-controller-1-model (convention par défaut pas obligation) ; controllers sans model (PagesController déjà dans N to F, Devise SessionsController/PasswordsController/RegistrationsController exemples historiques, DashboardsController, SearchController, HealthChecksController) ; generator léger `rails g controller` vs full kit `rails g resource` ; namespace `Admin::`/`Api::V1::` + héritage `BaseController` partagé portant `before_action :require_admin` (DRY + impossible à oublier) ; distinction `namespace` (URL + module + helper) vs `scope` (juste URL) ; pattern API versionnée `Api::V1::` dès la 1ère action ; **pattern "verbe → nom"** (Skinny Controllers Fat Resources DHH-Basecamp). ⭐ **Heuristique gravée** : "Si une action mérite plus tard de devenir un model, alors elle mérite déjà aujourd'hui son propre controller." **Q4 répondue** : SuggestionsController Phase 4 IA (B ✅) + complément sur les 5 besoins post-V1 qui feront émerger le model `Suggestion` (tracking historique, feedback user, économie API Anthropic via cache, audit debug hallucinations, analytics portfolio "% suggestions acceptées finissant en outfit publié"). **Entrée FEATURES_FUTURES.md ajoutée** : `SuggestionsController dédié dès V1 + futur model Suggestion persisté (pattern "verbe → nom")` en Priorité moyenne. 8 pièges gravés + matrice de décision complète (RESTful existant ? → besoin `:id` ? → one-shot ou évolutif ? → transactionnel ou listing ?). **Section 3 Advanced Layouts** ([[advanced-layouts-and-content-for]]) : `yield :name` + `content_for` accumule vs `provide` court-circuite (règle pro : `provide` pour `page_title`/`meta_description`, `content_for` pour `scripts`/`body_class` accumulables) + `content_for?(:name)` check de présence dans layout (marche pour les 2, même buffer interne, schéma mental "2 façons d'écrire / 3 façons de lire") ; nested layouts pattern 2 préféré (layouts complets + partial `_head.html.erb` commun, vs pattern 1 nested via `content_for :page_chrome` plus élégant mais confus 6 mois après) ; dynamic styling (body class scoping `<body class="<%= controller_name %> <%= action_name %>">` pattern GitHub/Basecamp/Shopify). ⭐ **Piège bloc `do...end` qui capture espaces** : `provide :title do %>\n  <%= @x.name %>\n<% end %>` → buffer reçoit `"\n  Veste\n"` → `<title>` rend `   Veste    — Nine to Fine` cassé visible. Fix : forme à 1 ligne `provide :title, @x.name`. **Q5 répondue** (4 changements diagnostiqués correctement par Chris + précision sur piège bloc + clarification `content_for?` marche pour `provide` ET `content_for` car même buffer). **Quick fixes UX/SEO ajoutés à `to-do.md` Dettes techniques** (locale EN détectée → tous textes EN après correction inline) : `<html lang="<%= I18n.locale %>">`, `<title>` dynamique avec ternaire fallback, meta description fallback, body class scoping, 9 vues `provide :page_title, "..."` listées une par une. 12 pièges gravés. **Section 4 Metaprogramming** ([[metaprogramming-ruby-and-rails-first-pass]]) ⚠️ **1er passage à approfondir** — Chris a flaggé "encore flou" après décortication détaillée. **Cours dédié programmé `to-do.md` Priorité 1** (1h-1h30 : 3 armes en Ruby pur avant Rails + lecture source Rails `attr_accessor`/`belongs_to`/route helpers + `instance_variable_get/set` + `class_eval` vs `instance_eval` + 3 exos sandbox `~/code/sandbox/rails/metaprogramming/` (refactor `GarmentFilter` define_method Q6 + clone `attr_accessor` maison + Builder DSL `method_missing`) + question entretien `send(params[:method])` code review). Note 1er passage couvre solidement : ⭐ **décortication ligne par ligne `GarmentFilter`** avec analogie centrale **chaîne de production usine** (5 postes sur tapis roulant) + bloc par bloc (FILTERS constante gelée whitelist anti-pollution, initialize `.to_h.with_indifferent_access.slice(*FILTERS)` 3 transformations enchaînées, apply `FILTERS.inject(@scope)` moteur du tapis + `next relation if blank?` skip + `send("by_#{key}", relation, value)` aiguillage dynamique, méthodes privées `by_X` ouvriers spécialisés) + exécution réelle pas à pas sur URL `?q[color]=black&q[brand]=Uniqlo` (5 tours + SQL final) + schéma mental ASCII + 3 super-pouvoirs combinés (`inject` Ruby standard, `send` LE metaprog, `ActiveRecord::Relation` lazy). Quick reference 3 armes + 4 pièges sécurité majeurs. **Section 5 Design Patterns + SOLID** ([[solid-design-principles-and-anti-patterns]]) : 5 lettres décortiquées avec exemples N to F concrets. **S** SRP (GarmentFilter 1 responsabilité, Garment.rb ~70 lignes à surveiller) ; ⭐ **O** OCP **prouvé 2× en live** dans `GarmentFilter` (PR #79 ajout `:brand` proactif + PR #83 ajout `:search` zéro modif `apply`) ; **L** LSP (pas de hiérarchie à risque N to F, composition partout) ; **I** ISP (`Taggable` concern interface fine) ; **D** DIP (`GarmentFilter.new(scope, params)` injection, ⚠️ Phase 4 `Ai::OutfitSuggester.new(outfit, client: Anthropic.new)` mandatory pour mocker tests). 5 anti-patterns (God Object, Spaghetti, Cargo cult, Premature optimization que Chris a évité ce matin avec Q6 `define_method`, Magic numbers). **Q7 répondue** (concern `Likeable` B vs méthodes directes Outfit A) : Chris a coché **ISP + SRP corrects** mais **manqué OCP** qui est LE principe central → scénario "Garment likeable dans 3 mois" rend OCP central (zéro modif Outfit avec B vs duplication 4 méthodes ou refactor avec A). LSP non applicable (pas d'héritage), DIP tiré par les cheveux. ⭐ **Réponse 90 sec entretien clé en main** : 5 principes × 5 exemples N to F en 5 phrases. **Section 6 i18n** ([[i18n-internationalization-fundamentals]]) : vocabulaire + 3 axes + config `default_locale`/`available_locales` + YAML hiérarchiques + helper `t()` + ⭐ **lazy lookup `t('.title')`** + interpolation `%{name}` mandatory vs concaténation + pluriels CLDR par locale + 3 stratégies set_locale (URL param/subdomain/user preference) + `default_url_options` propagation auto helpers + ⭐ **sécurité whitelist mandatory** (anti path traversal yml). **Q8 répondue** : 3 implémentations `set_locale`, **C seule sécurisée** (whitelist via `I18n.available_locales.include?(requested)`). ⭐⭐ **DÉCISION PÉDAGOGIQUE MAJEURE TRANCHÉE jeudi (méta)** : pas de setup i18n pre-emptive sur Nine to Fine = YAGNI + cargo cult + premature optimization (les 3 anti-patterns Section 5 vus 30 min plus tôt, méta cohérent). J'avais proposé un setup minimum, **Chris a challengé** ("est-ce vraiment judicieux ou mieux vaut attendre 1 journée dédiée ?") → bon réflexe, j'ai reconnu honnêtement et arrière. **Scope complet 7 étapes documenté `to-do.md` Dettes techniques** avec **trigger explicite** : "le jour où tu veux vraiment servir une 2e langue, typiquement démo recruteur francophone Paris". **État i18n détecté** : `I18n.locale => :en` + 17 locales `available_locales` viennent de Devise auto-mergé (pas de `rails-i18n`/`devise-i18n` installés). **Détection locale EN puis correction inline FR→EN** dans note Obsidian `advanced-layouts-and-content-for` + `to-do.md` (9 textes corrigés : "My garments" au lieu de "Mes garments", "Add a garment" au lieu de "Ajouter un garment", "Organize your wardrobe and compose outfits with Nine to Fine." au lieu de version FR, etc.). **Nouvelle memory globale `project-nine-to-fine-locale-english`** créée pour ne plus présumer FR sur N to F (cf. memory inverse `project-nine-to-fine-mobile-first` même logique). **Knowledge Check Odin (7 Q) + 3 questions style entretien 90 sec** : à faire en rétro plus tard (règle péda 9), non urgents.

- **Jeudi 11 juin (avant-midi, Claude app)** — Tailwind docs section **Interactivity** en lecture profonde (19 utilities, 3 pages doc v4 principales fetchées en parallèle : cursor + scroll-snap-type + accent-color), 1 note Obsidian solide [[interactivity-fundamentals]]. **8 catégories** structurées : pointeur souris (`cursor-*` 36 valeurs + `pointer-events-*` 2 valeurs), sélection texte (`select-*` 4 valeurs), tactile (`touch-action-*` mobile-first), scroll behavior (`scroll-smooth/auto`), scroll snap (`snap-x/y/both/mandatory/proximity/start/center/end/normal/always` + scroll-padding/margin pour offset header fixe), scrollbar (`scrollbar-width/color/gutter-*` niche), formulaires (`accent-*`/`caret-*`/`color-scheme-*`/`appearance-*`/`field-sizing-*`), perf (`will-change-*`/`resize-*`). ⭐ **Quick wins N to F identifiés** : (a) `touch-manipulation` sur tous boutons mobiles = **anti delay 300ms iOS Safari** (gain UX énorme perçu) ; (b) combo `disabled + cursor-not-allowed + opacity-50` boutons désactivés (sémantique HTML + visuel Tailwind) ; (c) `accent-zinc-900` futurs checkboxes filtres avancés (héritable sur form parent) ; (d) `motion-safe:scroll-smooth` anchor links (respecte `prefers-reduced-motion` WCAG 2.3.3) ; (e) `select-all` sur futurs code snippets doc (1 clic = tout copié). ⚠️ **À ne PAS faire** : (a) `will-change-transform` partout (explosion mémoire GPU mobile = anti-pattern **Premature Optimization** vu Section 5 matin) — uniquement en `hover:will-change-transform` activé au moment de l'animation OU via JS Stimulus avec reset après ; (b) `select-none` sur contenu copiable (token, code de confirmation) = anti-pattern UX majeur ; (c) `pointer-events-none` au lieu de `disabled` HTML (screen readers ne savent pas). **Patterns scroll-snap** : carousel horizontal `snap-x snap-mandatory + snap-center` + full-page sections verticales `snap-y snap-mandatory + snap-start` + `scroll-pt-20` pour offset header fixe (compense le header pour que snap n'écrase pas contenu). **Accent-color** : 1 classe vs 50 lignes CSS `appearance-none` reconstruction manuelle ; héritable sur `<form>` parent ; opacité `/50` support Firefox uniquement (Chrome/Safari ignorent). **Will-change** : réserve mémoire GPU **en permanence** (pas juste pendant animation), à utiliser comme **outil de dernier recours** quand on mesure un dropped frame réel. ⭐ **3 questions style entretien 90 sec rédigées par Claude** : (1) **300ms delay iOS Safari** diagnostic + fix `touch-manipulation` + trade-off perte double-tap zoom (acceptable sur SaaS interactive, pas sur site contenu lecture) ; (2) **`select-none` sur code de confirmation `8XK29`** code review : anti-pattern UX majeur + propose `<code class="select-all">` (1 clic = tout sélectionné) ; (3) **`will-change-transform` sur grille 200 cards** réaction code review : explication mémoire GPU pré-promote layer permanente → 200 layers = explosion mobile ; bon pattern `hover:will-change-transform` activé au moment du hover + Stimulus reset après ; règle générale will-change = outil de dernier recours quand dropped frame mesuré. 12 pièges gravés + synthèse décision rapide 10 cas (boutons mobiles, désactivés, draggable, carousel, sections scrolly, checkbox custom, anchor links, animation lourde, empêcher sélection, permettre copie 1 clic). Application liens rétroactifs [[transforms-fundamentals]] (will-change-transform compagnon perf transforms 2D/3D) + [[transitions-and-animations-fundamentals]] (motion-safe patterns) + [[typography-fundamentals]] (touch-manipulation sur tous boutons mobiles polish UI design system).

- **Jeudi 11 juin (méta cohérence pédagogique)** — 4 méta-décisions tranchées en session, toutes alignées avec les anti-patterns Section 5 :
  - **Décision 1** : Q6 refactor `GarmentFilter` avec `define_method` **reportée au cours dédié metaprogramming** (intuition : ne pas refactorer, gain faible + perte lisibilité réelle). Évite **Premature Optimization** d'aujourd'hui.
  - **Décision 2** : Section 4 metaprogramming Chris a flaggé "flou" → **cours dédié 1h-1h30** plutôt que digérer en passant. Évite cargo cult de retenir des patterns sans les comprendre.
  - **Décision 3** : Pas de setup config Action Mailer mercredi 10 juin (déjà acté hier, méta cohérent avec aujourd'hui).
  - **Décision 4 (la majeure)** : Pas de setup i18n minimum pré-emptive Nine to Fine = **YAGNI + cargo cult + premature optimization** (3 anti-patterns vus 30 min plus tôt). Scope complet 7 étapes documenté avec trigger explicite. Chris a challengé ma proposition initiale ("est-ce vraiment judicieux ?") = **bon réflexe pédagogique** appliquant directement la doctrine SOLID + anti-patterns.

- **Jeudi 11 juin (avant-midi clôture, Claude Code CLI)** — Routine end-of-morning exécutée en autonome (Chris a accordé autorisation explicite pour modifier documents + git ops). **Livré** : (a) note Obsidian Tailwind Interactivity + MAJ index Tailwind ; (b) MAJ planning files (PROGRESSION.md, PHASES/phase-1.md jeudi statut, SUIVI-COURANT.md cette entrée) ; (c) commit + push branche `chore/morning-routine-thu-jun-11` + PR vers main pour merge par Chris ; (d) vérification working tree clean pour démarrage aprem CLI sereine. **Aprem CLI (en autonomie Chris)** programme : migration 11 fichiers Minitest restants en RSpec + démarrage RSpec `GarmentsController` rétroactif (7 actions + IDOR — dette technique sem 22 soldée par l'introduction RSpec mercredi). 11 fichiers : 7 models specs + 3 controllers specs + 1 outfit_garment spec, ~1h45-2h30 estimation, pattern documenté dans [[minitest-to-rspec-migration-patterns]]. Après migration complète : suppression dossier `test/` + retrait `bin/rails test` du CI workflow.

- **Jeudi 11 juin (aprem, Claude Code CLI)** — **7 specs models migrés/créés (Minitest → RSpec) en mode coach pédago strict, ~3h de coaching intense ligne par ligne**, PR #102 mergée + PR #103 routine end-of-day. **Décalage vs plan ex-ante mercredi** : ~1h45-2h30 estimées pour les 11 fichiers (7 models + 3 controllers + 1 outfit_garment), réalisé ~3h pour 7 models seuls (mode coach = lent par design, explications ligne par ligne + vulgarisation + détails sécurité). Les 3 controllers + démarrage GarmentsController rétroactif reportés vendredi 12 juin aprem.

  - **Specs livrés en ordre dépendances montantes** (1 model par 1 model, du plus simple au plus dense, suppression Minitest source au fur et à mesure) :
    1. **`category_spec.rb` 6 examples** — validations basiques (presence name + position) + uniqueness case-insensitive (PostgreSQL functional index `LOWER(name) UNIQUE`) + #destroy `restrict_with_error` + ⭐ **DB-level uniqueness constraint** test avec `save(validate: false)` + `raise_error(ActiveRecord::RecordNotUnique)` (vérifier que la DB protège même si on bypass les validations Rails). Pédagogie sur l'**ordre `expect(actual).to eq(expected)` inversé vs Minitest** + matchers dynamiques `be_valid` → `valid?`.
    2. **`tag_spec.rb` 8 examples** — validations + ⭐ **unicité scopée user case-insensitive** (paire de tests "même user → invalide" + "autre user → valide" doit TOUJOURS aller en duo pour prouver le scope) + 2 tests polymorphic associations via `source_type: "Garment"`/`"Outfit"` (vérifier que `tag.tagged_garments` ne ramène pas les outfits et vice-versa). Inline `Outfit.create!(name:, user:, garments: [create(:garment, user:)])` pour les 2 tests polymorphic (factory `outfit` pas encore créée à ce stade).
    3. **`user_spec.rb` 12 examples ⭐ CRÉÉ FROM SCRATCH** — Minitest source `test/models/user_test.rb` ÉTAIT VIDE (juste la classe + commentaire défaut Rails) = dette technique sem 22 (User généré 26 mai avec Devise, jamais testé). 3 options proposées en chat (skip / créer / décaler) → Chris choisit "combler la dette". **Devise `:validatable` module** injecte AUTO 5 validations : email présence + format `is invalid` + uniqueness case-insensitive ⚠️ **incohérence DB case-sensitive** (dette potentielle à blinder en prod via index `LOWER(email)`) + password présence + length 6..128. Tests username (presence + uniqueness + length min 3). 3 cascade `dependent: :destroy` (Garment + Outfit + Tag) testées avec ⭐ **matcher `change(Class, :count).by(-1)`** + règle d'or `expect { action }` (accolades pour actions) vs `expect(value)` (parenthèses pour valeurs).
    4. **`garment_spec.rb` 21 examples** — le plus dense. 6 validations basiques + color `inclusion: { in: COLORS, allow_blank: true }` (subtilité du `allow_blank` qui évite le double message d'erreur sur nil) + ⭐ **test d'invariant statique `COLOR_HEX.keys.sort == COLORS.sort`** (Ruby pur sans DB ni factory, protège contre dérive entre 2 constantes liées) + 2 associations (outfits via outfit_garments + tags via taggings) + ⭐ **Active Storage section 4 tests** : valide attachée + valide sans + rejet non-image + rejet > 10MB avec **`define_singleton_method(:byte_size) { 11.megabytes }`** hack pour stub sans uploader 11MB (metaprogramming léger, à refactor en `allow/receive` plus tard) + ⭐ **Taggable concern section 7 tests denses** avec tableau sémantique clé `@tag_names` `nil → return → préserve` vs `"" → [] → vide` vs `"summer, casual" → [...] → sync` + `Garment.find(id)` (reset ivars) vs `garment.reload` (juste colonnes AR) — distinction cruciale pour tester le cas prod controller#update partial.
    5. **`outfit_spec.rb` 10 examples** — création de la **factory `outfit.rb`** au passage avec ⭐ **callback `after(:build)`** auto-créant un garment (`outfit.garments << create(:garment, user: outfit.user) if outfit.garments.empty? && outfit.user.present?`) pour satisfaire `validates :garments, presence: true`. ⭐ **Piège vécu en live** : la garde `.empty?` ne distingue pas "non spécifié" de "explicitement vide" → test "is invalid without at least one garment" avec `garments: []` passait la factory et redevenait valide → **fix bypass via `Outfit.new(name:, user:)` direct** (la factory est un outil, pas un dogme — pattern pro). Alternative `transient :skip_default_garment` proposée pour usage 100% factory. 1 smoke test Taggable concern (concern partagé Garment + Outfit testé exhaustivement sur Garment + smoke sur Outfit = principe DRY appliqué aux tests).
    6. **`tagging_spec.rb` 6 examples** — validations belongs_to polymorphic + ⭐ **uniqueness sur FK `:tag_id`** scopée `[:taggable_type, :taggable_id]` (clé d'erreur `errors[:tag_id]` avec `_id` parce que validation déclarée sur le FK, pas l'association) + paire scope (même taggable → invalide / autre taggable → valide).
    7. **`outfit_garment_spec.rb` 5 examples** — création **factory `outfit_garment.rb`** simple + validations belongs_to + uniqueness sur FK `:garment_id` scopée `:outfit_id` (test #4 prouve d'un coup que `Outfit.create!(..., garments: [garment])` crée auto la jointure ET que l'unicité est active).

  - **Total ~68 examples RSpec verts** (tous tests Minitest models supprimés au fur et à mesure).

  - **Factories ajoutées** (2) :
    - `spec/factories/outfit.rb` avec `after(:build)` callback + garde double. Side-effect documenté (un garment persisté par `build(:outfit)` à cause du `create` interne).
    - `spec/factories/outfit_garment.rb` simple (2 associations implicites).

  - **Config ajoutée** : `config.file_fixture_path = Rails.root.join("test/fixtures/files")` dans `spec/rails_helper.rb`. Chris a perdu ~5 min sur 3 fails `ArgumentError: directory 'spec/fixtures/files' does not contain 'valid.jpg'` parce que j'avais flaggé le préalable trop discrètement (noyé dans le flow). Mea culpa explicite + engagement pédago : encadrer les préalables critiques avec `⚠️ Avant de continuer` sur les sections complexes (Taggable section 7, request specs controllers vendredi).

  - **CI workflow fix méta** : après push initial PR #102, 5 jobs CI → `lint` fail (16 violations Rubocop dans le code Chris tapé : `Layout/EmptyLinesAroundBlockBody` × 11 + `Layout/SpaceInsideArrayLiteralBrackets` × 4 + `Layout/TrailingWhitespace` × 1) + `test` fail (17 fails `RecordInvalid: Username has already been taken` sur les specs ET sur `garment_filter_spec.rb` mergé mercredi). **Diagnostic** : workflow CI lançait `bin/rails db:test:prepare && bin/rails test && bin/rspec` → `bin/rails test` charge fixtures Minitest (`test/fixtures/users.yml` → user `chris`) AVANT RSpec qui fait `create(:user, username: "chris")` → conflit unicité. **Fix appliqué** dans `.github/workflows/ci.yml` : inverser l'ordre + ajouter 2e `db:test:prepare` entre les 2 runs : `bin/rspec && bin/rails db:test:prepare && bin/rails test`. RSpec rollback ses transactions à chaque example → DB propre, puis Minitest charge ses fixtures pour son propre run. Belt-and-suspenders. Commit 2 PR #102 : `fix(ci): swap rspec/minitest order to avoid fixture conflicts + rubocop autofix`. CI 5/5 vert après push.

  - **Méta erreurs Claude session capturées (mea culpa explicites)** : (a) **factory `outfit` avec piège anticipé non flaggé** — la garde `.empty?` de l'`after(:build)` ne distingue pas "non spécifié" vs "explicitement vide" → test fail prévisible en live, j'aurais dû mentionner ce piège en concevant la factory ; (b) **préalable `file_fixture_path` noyé dans le flow** — section Active Storage du spec dépendait d'une config dans `rails_helper.rb` que j'ai présentée trop discrètement, 3 fails inutiles + 5 min perdues ; (c) **commit message initial pollué par `Co-Authored-By: Claude` + email Anthropic** — Chris a flaggé "je ne veux pas voir de Co-Authored-By Claude ni l'adresse de Anthropic" → nouvelle memory `feedback-no-co-authored-by` créée immédiatement, règle étendue à toutes surfaces git/GitHub visibles (commits + PR body) par cohérence portfolio.

  - **3 nouvelles notes Obsidian rédigées** dans `Apprentissage/Rails/Testing/` :
    1. [[factory-bot-advanced-patterns]] — callbacks `after(:build)`/`after(:create)`/`after(:stub)` + bypass `Model.new` + transient attributes + override priority + bloc lazy mandatory + polymorphic auto + 7 pièges.
    2. [[rspec-active-storage-testing]] — config `file_fixture_path` + pattern `attach(io:, filename:, content_type:)` + visualisation 3 tables (model + blobs + attachments) + hack `define_singleton_method(:byte_size)` + alternative pro `allow/receive` + pourquoi `not_to be_empty` plutôt que message exact pour gems tierces + 7 pièges.
    3. [[rspec-taggable-concern-testing]] — tableau sémantique `nil/""/valeur` + 7 tests décortiqués ligne par ligne + ⭐ distinction `Garment.find(id)` (reset ivars) vs `garment.reload` (juste colonnes AR) + smoke test pattern pour concern partagé + 6 pièges dont typo classique `expect(garment).to be_empty`.
  - Note [[minitest-to-rspec-migration-patterns]] de mercredi **enrichie section 8** (9 sous-sections "Patterns vus EN LIVE jeudi 11 juin") + section 9 statut migration models complet + section 10 voir-aussi. Index Rails section Testing MAJ avec 3 entrées.

  - **Reports VEN 12 juin aprem** : (1) migration 3 specs controllers Minitest restants (`garments_controller_test.rb`, `outfits_controller_test.rb`, `pages_controller_test.rb`) — concept **nouveau** : RSpec request specs + `get/post/patch/delete` + `sign_in` Devise helper ; (2) démarrage RSpec `GarmentsController` rétroactif avec test **IDOR cross-user** (sécurité — la dette technique sem 22 vendredi 29 mai) ; (3) audit `bullet` complet si temps (n'a pas tourné sur les nouveaux specs RSpec, possibles N+1 non détectés).

  - **Conversation Chris fin d'aprem** : "si je fais controllers spec + IDOR aujourd'hui mon aprem de demain est libre ?" → trade-off honnête présenté en chat (POUR continuer = aprem demain libre ; CONTRE = fatigue cognitive sur concept neuf request specs + Devise + IDOR mérite cerveau frais). **Recommandation Claude** : pause maintenant, ne pas bâcler IDOR sécurité (skill valorisée en entretien). Chris penche pause, à confirmer après vérification clean.

- **Vendredi 12 juin (matin, Claude app)** — Odin chap. **41 Websockets and Action Cable ✅** lu en lecture profonde + Rails Guide 8.1 Action Cable Overview sections 1-7 (sections 8-11 scaling/deployment/testing reportées sem 25 quand on code dessus). **Dernier chapitre du parcours Odin Rails — consolidation post-bootcamp terminée ✅** (chap. 1-41 couverts, règle péda 9 respectée : projets Odin remplacés par sandbox + Nine to Fine). Note Obsidian rédigée : [[action-cable-fundamentals]] (16 sections + 12 pièges + 5 KC répondues + 3 questions entretien 90 sec). Index Rails Obsidian section "Action Cable / WebSockets" remplacée (à venir → entrée pleine). PROGRESSION.md MAJ (chap. 41 ✅ + parcours Odin Rails terminé). Concepts couverts : pourquoi WebSocket (HTTP unilatéral + ancêtres ratés polling/long-polling) ; 6 mots de vocabulaire (Connection / Consumer / Channel / Subscription / Stream / Broadcasting) ; ⭐ **stack mentale Hotwire** (Action Cable EN DESSOUS de Turbo Streams = sucre Turbo `turbo_stream_from @outfit` génère auto channel générique `Turbo::StreamsChannel` signé → tu n'écris JAMAIS de Channel custom pour les Turbo Streams broadcasts) ; **Server-side Connection auth via Warden** (`env["warden"].user`) vs `cookies.encrypted[:user_id]` (4 obstacles : nom cookie variable, structure Warden interne fragile `warden.user.user.key`, sérialisation Marshal/JSON, session middleware absent dans pipeline WebSocket) ; **variante from-scratch** cohérente avec [[auth-from-scratch-sandbox-exo]] (`User.find_by(id: cookies.encrypted[:user_id])` quand pas de Devise) ; **Channel pattern `ApplicationCable::Channel`** (couche d'isolation pro entre gem et ton code, exception namespace vs autres `Application*`, Zeitwerk naming `outfit_channel.rb` strict snake_case + suffixe `_channel` mandatory + singulier comme models pas pluriel comme controllers, generator crée serveur+client en 1 commande) ; **Streams** (`stream_from "string"` libre vs `stream_for model_instance` via `dom_id` — 95% cas pratique) + match `broadcast_to model` vs `ActionCable.server.broadcast "string"` ; **Client-side Consumer + Subscriptions** (5 lifecycle callbacks `initialized`/`connected`/`disconnected`/`rejected`/`received(data)` — LE callback central ; `perform("nom_method", payload)` = RPC client→serveur, désérialisation JSON → hash Ruby avec STRING keys côté serveur, ⚠️ `data[:foo]` retourne `nil` silencieux) ; ⚠️ **piège `this`/`bind` JS vanilla** (analogie lettre+enveloppe vide : `this` dépend de comment la fonction est APPELÉE pas où définie ≠ Ruby `self` figé ; `addEventListener("e", this.method)` perd `this` parce que c'est `window` qui poste la lettre → toujours `bind(this)` dans `initialized()` ; Stimulus fait le bind pour toi mais Action Cable JS vanilla non — c'est plus bas niveau) ; **Solid Cable Rails 8** (4 adapters : `async` dev only, `solid_cable` prod défaut DB-backed Rails 8, `redis` legacy, `postgresql` niche LISTEN/NOTIFY ; cohérent stack `solid_queue` + `solid_cache` Nine to Fine = 3 pieds tabouret Rails 8 sans dépendance externe, 1 Postgres suffit Kamal Hetzner ; setup `bin/rails solid_cable:install` mandatory) ; ⭐ **Connection permissive vs reject** (pattern pro mix public/privé — `reject_unauthorized_connection` ferme la WebSocket entière → casse `turbo_stream_from` sur pages publiques même si auth pas requise côté controller, **symptôme silencieux** = HTML s'affiche correctement mais zéro broadcast reçu ; solution = Connection permissive `self.current_user = env["warden"].user` sans reject + `return reject unless current_user` per-channel selon besoin). **Coaching mot par mot** sur 4 lignes denses demandé par Chris (mémoire `feedback-explain-every-line-and-context` appliquée strictement) : (a) `if verified_user = User.find_by(id: cookies.encrypted[:user_id])` — assignation dans condition + parens RuboCop convention + 4 niveaux Symbol/String décortiqués (forme moderne `id:` vs classique `:user_id` selon position dans hash literal ou accès `[]`) ; (b) `def appear(data); current_user.appear(on: data["appearing_on"])` — ⚠️ **piège Symbols vs Strings dans la même ligne** : `on:` (Symbol hash Ruby pur) vs `["appearing_on"]` (String parce que JSON désérialisé) → règle gravée : hash que TOI tu écris en Ruby = Symbols, hash que TU REÇOIS depuis JSON/WebSocket/API/HTTP = Strings ; (c) `class ChatChannel < ApplicationCable::Channel` — héritage `<` + résolution namespace `::` + 6 mots Ruby/Rails (class/CamelCase/`<`/module/`::`/Channel) + pattern Application* universel Rails + exception Action Cable double-classe namespacée ; (d) `this.update = this.update.bind(this)` — analogie lettre+enveloppe vide + bug silencieux `addEventListener` sans bind où `this` devient `window` → `this.documentIsActive` = `undefined` silent. ⭐⭐ **Décision pédagogique majeure tranchée** : skip tuto Odin "Basic Messaging App" — Chris a proposé de faire l'exo en remplacement du slot Tailwind avant-midi. Claude a challengé fort avec 3 arguments factuels après WebFetch du tuto : (1) **vanilla JS pattern legacy 2018-2021** (`consumer.subscriptions.create` + `received(data)` + `insertAdjacentHTML` DOM manipulation) = pattern que Chris n'utilisera PAS en prod sur Hotwire monolith Rails 8 ; (2) **viole règle péda 9** Chris l'a posée lui-même sem 22 (projets Odin remplacés par sandbox + Nine to Fine) ; (3) **2 meilleurs créneaux** existent : sandbox focused samedi 13 juin (1h max, pattern moderne Turbo Streams + Solid Cable + RSpec `have_broadcasted_to`) + vraie pratique sem 25 sur Nine to Fine (likes + comments broadcast). Tuto Odin = 60-90 min annoncées MAIS 2-2h30 réelles (install Devise + débug intercouches + JS DOM events + débug WebSocket cors/cable.yml en dev) pour projet jetable. Chris a accepté la recommandation après lecture des arguments. **Méta pédago** : Chris a respecté son propre cadre (la règle péda 9 qu'il a écrite il y a 3 semaines), Claude a rappelé sans flatter ni condescendre. Application directe sem 25 (Connection permissive + Solid Cable install + likes/comments broadcast `after_create_commit` + `broadcast_append_to outfit, target: "likes"` + `turbo_stream_from @outfit`) et Phase 4 (AI suggestions progress via job + broadcast). Liens stratégiques rétroactifs gravés : [[turbo-frames-streams-and-format-negotiation]] (lundi 8 juin sem 24 — le sucre Turbo au-dessus) + [[callbacks]] (chap. 27 — `after_commit on: :create` mandatory pour broadcasts sinon job/broadcast orphelin si transaction rollback) + [[solid-design-principles-and-anti-patterns]] (chap. 40 Section 5 — DIP appliqué : préférer abstraction stable `env["warden"].user` vs internals fragiles `session["warden.user.user.key"]`).

- **Vendredi 12 juin (méta clôture matin)** — Claude a aussi commis 1 erreur pédago en cours de matin : **trop d'info empilée d'un coup** sur la ligne `def appear(data); current_user.appear(on: data["appearing_on"])` (tableau Symbols vs Strings + ZOOM + mini-check camelCase → snake_case dans une seule réponse). Chris a flaggé "dis moi tu commences à m'embrouiller" → reset immédiat, retour à l'essentiel en 3 phrases, demande explicite "où précisément ça coince ?" avec 4 options (A/B/C/D). Bonne récupération mais pattern à graver : **sur les lignes denses, une seule idée par bloc**, pas tout l'arsenal pédagogique en une fois. Lien règle 12 globale "Réponses droit au but, concision maximale" + mémoire `feedback-vulgarize-advanced-rails` (analogie concrète d'abord, jargon après). Pas de nouvelle règle à créer, à appliquer plus strictement.

- **Vendredi 12 juin (avant-midi + clôture matinée groupée, Claude Code CLI)** — Tailwind **Customization avancé v4 ✅** lu en lecture profonde (docs "Adding custom styles" + "Functions and Directives") + ⭐ **application directe Nine to Fine, PR #104 mergée** (Chris a groupé dans la même PR le matin Action Cable + l'avant-midi Tailwind data-loading). Note Obsidian rédigée [[customization-advanced-v4]] (12 sections, 12 pièges gravés, 3 questions style entretien 90 sec, application directe PR #104 + futurs N to F sem 25+). Concepts couverts : paradigm shift v3 → v4 CSS-first (`tailwind.config.js` → `@theme` + plugins JS → `@utility`/`@custom-variant` + Node-less binaire standalone via `tailwindcss-rails`) ; les 5 directives clés en escalier (`@apply` v3 base / `@theme` tokens / `@variant X { }` utilise variant dans règle CSS / `@utility X { }` crée utility custom avec paramétrisation `--value()` 8 modes / `@custom-variant X (...)` crée variant nommée shorthand vs bloc `@slot`) ; ordre canonique fichier CSS Tailwind v4 (imports → `@theme` → `@custom-variant` → `@utility` → CSS composants) ; ⚠️ `@variant` ≠ `@custom-variant` distinction critique ; garantie v4 explicite que **utilities natives gagnent contre `@utility` custom en conflit** (vs `@layer components` cascade dépend de l'ordre dans class string) ; fonctions v4 (`--value`, `--modifier`, `--default`, `--spacing`, `--alpha`) ; migration v3 → v4 (`@config` + `@plugin` compat legacy, `theme()` function dépréciée, retrait `corePlugins`/`safelist`/`separator`).

  - ⭐⭐ **Méta-feedback Chris en milieu d'avant-midi (majeur)** : *"je trouve ça inutile sans pratique c'est juste lire de la notation qui est aussi vite oubliée si on ne la mets pas en pratique"*. Claude reconnu honnêtement : 2h30 lecture pure mal calibré pour un chapitre de **doc de référence notationnelle** (`@utility` paramétrisé + `--value()` 8 modes + arbitrary variants = oubli garanti sous 48h sans manipulation). **Distinction pédagogique posée** : chapitres **conceptuels** (Action Cable, Callbacks, SOLID) → lecture profonde matin fonctionne ; chapitres **notationnels** (Tailwind Customization avancé, ActiveSupport core_ext, regex CSS) → couper créneau en 1h lecture + 1h30 pratique immédiate sur projet. **Décision en live** : Chris a choisi de pivoter vers l'exo pratique, Claude a proposé spec immédiate. Règle 1bis ajoutée au PLANNING.md à valider plus tard si pattern se confirme. ⭐ **Pattern à intégrer dans le ritual quotidien** pour la suite des phases (React, TS, Next).

  - **Exo PR #104 livré en CLI mode coach pédago strict** (~1h15 + ~30 min cleanup/commit/PR) : 3 fichiers modifiés/créés. (a) `app/assets/tailwind/application.css` += `@custom-variant data-loading (&[data-loading="true"]);` + `@utility btn-base { @apply ... }` (scope C : forme + focus ring universel non-négociable a11y, peinture en HTML pour rester ouvert OCP à futurs `btn-primary`/`btn-danger` via Rule of 3) ; ordre canonique respecté (entre `@theme` et bloc Tom Select). (b) `app/javascript/controllers/submit_button_controller.js` créé : controller Stimulus minimaliste (`static targets = ["button"]` + 2 méthodes `start`/`end` mutant `this.buttonTarget.dataset.loading` à `"true"`/`"false"`, pas de `connect`/`disconnect` car state commandé par events submit/turbo:submit-end). (c) `app/views/garments/_form.html.erb` += `data-controller="submit-button"` + `data-action="submit->submit-button#start turbo:submit-end->submit-button#end"` sur form + classes `data-loading:cursor-wait data-loading:opacity-50` (sans `pointer-events-none`, cf. piège vécu) sur `f.submit` + `data-submit-button-target="button"`. Submit class simplifiée de 14 utilities à 7 (le reste absorbé dans `btn-base`).

  - **Décisions de design tranchées en live** : (1) `data-*` attribute over `.is-loading` classe (sémantique HTML état machine-lisible + convention Tailwind v4 cohérente avec `data-[state=open]:` natifs + composabilité multi-états `idle`/`pending`/`success`/`error` = 1 mutation au lieu de juggler 4 classes + tests E2E + screen readers s'attendent à trouver l'état dans `data-*`/`aria-*`) ; (2) controller Stimulus sur `<form>` pas sur `<button>` (turbo:submit-end dispatché par form, events DOM bubble vers le haut pas vers le bas, controller sur button enfant ne recevrait jamais l'événement) ; (3) **scope C pour `btn-base`** (forme + focus ring a11y mandatory, peinture en HTML — vs scope B complet ou A nu) parce qu'1 seul bouton stylé en prod aujourd'hui = premature design d'aller direct scope B, pattern d'évolution naturel via Rule of 3 ; (4) pas de `pointer-events-none` (Turbo pose déjà `disabled` natif auto pendant requête depuis Turbo 7+, anti-double-submit garanti sans aucun code) ; (5) `= "false"` au `end()` vs supprimer attribut (debug DevTools + convention state machine).

  - **3 pièges majeurs vécus en live** : (1) ⭐ **Typo `jsutify-center` dans `@apply` casse TOUTE la directive Tailwind v4** (pas juste la classe inconnue) → `@utility btn-base` rendu = vide → bouton "carré" sans aucune mise en forme (perte de `rounded-md`/`py-2 px-5`/`font-semibold`/`transition`/`cursor-pointer`/etc.) ; règle gravée = **toujours surveiller terminal `bin/dev`** qui dit explicitement `Cannot apply unknown utility class: jsutify-center` ; 90 % des "Tailwind marche pas" viennent de typos signalées dans le terminal. (2) ⭐ **`pointer-events-none` bloque `cursor-wait`** car browser ignore les events de souris sur l'élément → cursor passe à travers comme si transparent → `cursor-wait` jamais appliqué visuellement (cursor affiché = celui du fond de page) ; **anti-pattern documenté hier dans [[interactivity-fundamentals]]** que Claude a recommandé dans la spec ce matin sans relire la note → ⭐ **méta majeur** = relire les notes Obsidian avant de coder, sinon les pièges gravés sont oubliés ; Turbo pose déjà `disabled` natif → solution = retirer `pointer-events-none`, pas de redondance. (3) **Stimulus target manquant** (`data-submit-button-target="button"` oublié initialement sur submit) → `this.buttonTarget` undefined → mutation silencieuse + erreur console JS verbose `Missing target element "button" for "submit-button" controller` ; règle gravée = **console JS ouverte en debug Stimulus** (verbose sur targets manquants, controllers non chargés, actions mal formatées) ; même méta que terminal Tailwind = les frameworks modernes te disent ce qui ne va pas dans leur output, à condition de regarder.

  - ⭐ **Phase 1 Tailwind bouclée** ✅ — toutes étapes de l'index Tailwind couvertes (étape 14 ✅ dans ordre lecture). PROGRESSION.md MAJ : `**Phase 1 Tailwind bouclée** ✅` + transition Phase 2 React fondamentaux sem 26 (22 juin). Index Tailwind Obsidian MAJ : nouvelle entrée `[[customization-advanced-v4]]` dans section Customization + section Core concepts "à venir" → "couvert dans customization-advanced-v4" + ordre lecture étape 14 ✅. Application directe N to F future actée : promouvoir `btn-base` en `btn-primary`/`btn-secondary`/`btn-danger`/`btn-ghost` dès Rule of 3 (sem 25+ avec features sociales — likes/comments/follows) ; autres `@custom-variant` candidats `is-owner`/`has-photo`/`is-current-page` ; Phase 4 IA pattern Stimulus loading généralisable au submit `Ai::OutfitSuggester` longues tâches (broadcast progress).

  - **Aprem ✅ — PR #106 mergée** : 3 chantiers cumulés terminés (migration controllers + GarmentsController rétroactif + cleanup CI/test) en mode coach pédago strict ~3h. Chris a choisi option A (tout enchaîner aprem) après brainstorm trade-off cumul cognitif vs aprem libre samedi.

  - **Chantier 1 — Migration 3 controllers Minitest → RSpec request specs** : (a) `spec/requests/pages_spec.rb` 2 examples (anonymous + signed in sur home publique, verrouille `skip_before_action :authenticate_user!, only: [:home]`) ; (b) `spec/requests/outfits_spec.rb` 13 examples (1 anonymous redirect + 7 owner happy + 2 unhappy + 3 IDOR show/update/destroy) ; (c) `spec/requests/garments_spec.rb` 15 examples (1 anonymous redirect + 2 index variants no filter / with filters smoke test + 6 owner happy + 2 unhappy + 4 IDOR show/edit/update/destroy). **Total** : 30 nouveaux examples request specs. Tous les fichiers Minitest source supprimés au fur et à mesure (`test/controllers/*_test.rb`).

  - **Chantier 2 — Dette sécurité sem 22 soldée** : `GarmentsController` n'avait **aucun test controller** depuis le CRUD livré 29 mai (PR #42/45). Les 15 examples de `garments_spec.rb` couvrent maintenant les 7 actions + ⭐ **4 IDOR cross-user sentinels** (show, edit, update, destroy — toutes les actions critiques avec `:id` qui utilisent `set_garment` = `current_user.garments.find`). **Pattern IDOR documenté** : `sign_in user_a` + tentative d'accès à la ressource de `user_b` → `expect(response).to have_http_status(:not_found)` ; repose sur (1) le scoping `current_user.garments.find` qui raise `ActiveRecord::RecordNotFound`, (2) Rails `show_exceptions = :rescuable` en test env qui catch + render 404 (vérifié dans `config/environments/test.rb` avant écriture des specs, préalable critique flaggé pour pas perdre du temps). Sur outfits, j'ai initialement sous-couvert (3 IDOR au lieu de 4 — j'ai oublié `GET /outfits/:id/edit` alors que `set_outfit` est aussi sur `edit`) → reconnaissance honnête en chat + ajout du 4e sur garments + backlog noté pour rétroactif outfits.

  - **Chantier 3 — Cleanup `test/` + CI** : (a) **`git mv test/fixtures/files spec/fixtures/files`** (préserve historique git via rename detection, déplace `valid.jpg` + `document.pdf` + `.keep` vers convention RSpec native) ; (b) **`git rm -r test/`** (suppression du dossier complet : 7 fixtures YAML Minitest plus utilisées + `test_helper.rb` + 6 sous-dossiers `.keep` vides — controllers/helpers/integration/mailers/models/system) ; (c) **suppression de la ligne `config.file_fixture_path = Rails.root.join("test/fixtures/files")`** dans `spec/rails_helper.rb` (le défaut RSpec `spec/fixtures/files` prend le relai automatiquement, plus de config custom à maintenir) ; (d) **simplification CI workflow `.github/workflows/ci.yml`** : job `test` passé de 4 commandes (`db:test:prepare && bin/rspec && db:test:prepare && bin/rails test`) à 2 (`db:test:prepare && bin/rspec`) ; **suppression complète du job `system-test`** (40+ lignes YAML qui exécutaient `bin/rails db:test:prepare test:system` sur un dossier `test/system/` vide depuis le début du projet — pur bruit CI, 2-3 min wasted par PR) → de 5 jobs CI à 4 (`scan_ruby`, `scan_js`, `lint`, `test`) ; (e) **branch protection rule MAJ côté GitHub Settings** : retrait du check `system-test` des required status checks (sinon la PR bloque en "Expected — Waiting for status to be reported" infiniment puisque le job n'envoie plus de status). 113 examples verts au total dans la suite RSpec après cleanup.

  - **Concepts nouveaux gravés** : (1) ⭐ **Les 3 niveaux de test RSpec** : model spec (logique métier Ruby pur, pas de HTTP) vs **request spec** (cycle HTTP request → controller → response observable : status/body/redirect/flash, simule un client HTTP `curl` sans browser) vs **system spec** (end-to-end depuis vrai navigateur via Capybara + Selenium/Cuprite). Chris confondait initialement request et system (a dit "on test que les liens cliquables amènent au bon endroit" = c'est du system spec). Recadré avec tableau + analogie `curl` scripté en mémoire. (2) **`type: :request`** signal magique qui active les helpers HTTP (`get/post/patch/delete`, `response`, `flash`). (3) **Convention `_path` vs `_url`** : request specs utilisent `_path` (relatif, lisible, host indépendant) ; `_url` réservé contextes qui exigent host absolu (mailers/redirections externes/sitemaps). (4) **Pattern AAA `context "with valid params"` / `"with invalid params"`** pour les actions de mutation create/update : sans le 2e context, le `else` du `if @model.save` n'est jamais exercé = régression silencieuse possible. (5) **`Devise::Test::IntegrationHelpers`** : helper `sign_in user` à inclure dans `rails_helper.rb` (`config.include Devise::Test::IntegrationHelpers, type: :request`) — préalable critique mandatory avant tout request spec sur action protégée. (6) ⭐ **Rails 7+ post-PATCH/DELETE redirige en 303 See Other** (pas 302 Found) pour garantir que les navigateurs Turbo switch en GET sur la redirection (vs 302 qui pouvait laisser certains navigateurs ré-émettre en PATCH/DELETE). Matcher `redirect_to` accepte n'importe quel 3xx. (7) **Smoke test intégration sur `index` avec filtres** : 1 example pour verrouiller `controller + GarmentFilter + pill_class helper + turbo_frame_tag` ensemble (pas re-test du filter, déjà couvert par `garment_filter_spec.rb`). (8) **Pattern `outfit` warm-up avant `expect { delete }.to change(Outfit, :count).by(-1)`** : sans la ligne `outfit` solo qui force la création AVANT le bloc, `outfit_path(outfit)` instancie l'outfit DANS le bloc → change voit +1 puis -1 = 0 = test fail. (9) **`reload + eq(original_name)` sur invalid update** : pattern plus rigoureux que `not_to eq("")` — verrouille "rien n'a bougé" exactement, pas juste "ce n'est pas une chaîne vide".

  - **⭐ Piège majeur du jour gravé — `do` manquant sur `context`/`describe`** : Chris a écrit `context "when signed in as the owner"` sans le `do` final → bug **sournois** : Ruby/RSpec n'erreure PAS au parsing (c'est un appel de méthode valide sur 1 arg string sans block) → le context devient vide → le `before { sign_in user }` qui suit et tous les `describe` enfants se retrouvent **réattachés au `describe "Garments"` racine** → conséquence : `sign_in user` s'appliquait à TOUS les examples y compris le context `"when not signed in"` → test "anonymous redirect" recevait 200 au lieu de 302 (car user effectivement signé in). Symptôme apparent : "un test sécurité qui passe sans rien faire". Règle à graver : si un test à l'air "trop indulgent" (anonyme qui n'est pas redirigé, sentinel qui passe sans assertion exécutée), regarder les `do` manquants sur context/describe parents avant tout autre debug.

  - **3 notes Obsidian rédigées** dans `Apprentissage/Rails/Testing/` : (1) **[[rspec-request-specs-fundamentals]]** (nouvelle, ~10 sections : philosophie + 3 niveaux + setup `type: :request` + helpers HTTP + matchers response/redirect_to/have_http_status + Devise integration + pattern AAA mutations + redirections 303 vs 302 + pièges dont `do` manquant + question style entretien 90 sec) ; (2) **[[idor-sentinel-pattern-rspec]]** (nouvelle, sécurité-focus : OWASP IDOR rappel + pattern sign_in user_a access user_b → 404 + condition `current_user.scope.find` + `show_exceptions :rescuable` mandatory en test env + choix quelles actions tester + redondance vs sécurité défensive + question style entretien 90 sec "comment tu détectes une IDOR en code review ?") ; (3) **enrichissement [[minitest-to-rspec-migration-patterns]] section 11 "Patterns vus EN LIVE vendredi 12 juin (controllers)"** avec mapping Minitest controller test → RSpec request spec + statut migration controllers complet (3/3 + cleanup). Index Rails Testing MAJ.

  - **Méta erreurs Claude session capturées** : (a) ⭐ **HEREDOC `$(cat <<'EOF' ... EOF)` ne fonctionne JAMAIS dans le setup de Chris** (zsh + AZERTY + paste) — il se retrouve bloqué dans le prompt `quote>` à chaque fois et ne sait pas en sortir ("q" ne marche pas, il faut Ctrl+C × 2). Mentionné explicitement par Chris "ça ne fonctionne jamais ton heredoc". Engagement gravé : **commit message TOUJOURS sur 1 ligne avec `-m "..."`** ; body détaillé TOUJOURS via interface web GitHub OU `gh pr create --body-file`. À ré-appliquer strictement. (b) **3 IDOR au lieu de 4 sur outfits** initialement (j'ai oublié `GET /:id/edit` alors que `set_outfit` est aussi `only: %i[edit update destroy]`) — reconnaissance honnête + correction sur garments + backlog noté. (c) **Numéros de ligne donnés (114-159 pour system-test) périmés après edit 4a** (le fichier avait raccourci de 4 lignes après simplification job test) — Chris a challengé ("mon système test commence à la 110 qu'est ce que tu me racontes") → reconnaissance + règle gravée : utiliser ancres textuelles (`grep -n "system-test:"`) plutôt que numéros figés quand plusieurs edits successifs. (d) **Préalable `Devise::Test::IntegrationHelpers` flaggé en passant** (au lieu d'un encadré ⚠️ explicite mandatory) — Chris avait demandé hier (jeudi) que je flagge mieux les préalables critiques, à appliquer plus strictement encore. (e) Pour le concept request spec, j'ai initialement utilisé un questionnement socratique trop long ("dis-moi 3 choses observables...") avant que Chris me dise direct "je ne sais pas pour ça que je t'utilise pour m'apprendre" → recadré sur "montrer direct le code + décortique ligne par ligne" qui est plus efficace pour concept neuf.

  - **Méta Chris (typos cumulées en série sur garments_spec)** : 4 typos en une seule série de fixes (`fasl` ×2 au lieu de `flash`, `flashå:notice` au lieu de `flash[:notice]` — caractère AZERTY Mac Option+A à la place de `[` raté, `"original_name"` string littérale au lieu de la variable, `path` au lieu de `patch`). Pas un pattern systémique, plutôt fatigue cognitive de fin d'aprem (cumul Action Cable matin + Tailwind avancé + 3h coaching dense). Pas de règle pédago à graver.

  - **Bilan technique fin d'aprem** : 113 examples RSpec verts (suite complète : 68 models + 32 request controllers + 13 query + autres) ; dette technique sem 22 soldée intégralement (CRUD Garment livré sans tests le 29 mai → maintenant 15 examples + 4 IDOR sentinels) ; `test/` directory totalement supprimé ; CI workflow simplifié et accéléré (~2-3 min gagnées par PR) ; convention RSpec adoptée à 100% (`spec/fixtures/files` au défaut RSpec). PR #106 mergée et pullée par Chris en fin de session.

  - **Reportés samedi 13 juin** : (a) **investiguer warning bullet POST /outfits create** (cf. entrée clôture session ci-dessous + `to-do.md` Dettes techniques) ; (b) **4e IDOR sentinel sur outfits** (`GET /outfits/:id/edit` à ajouter pour cohérence avec garments — non bloquant car scoping garanti par `set_outfit` partagé, juste explicit doc) ; (c) **exos dirigés sandbox 4 soirs cumulés** (lun + mar + mer + jeu sem 24 + ven 12) — spec Mailer `after_commit` + RSpec `have_enqueued_mail` + spec Tailwind Transforms modal + spec Solid Cable + Turbo Streams focused (décision tranchée vendredi matin pour remplacer le tuto Odin "Basic Messaging App" legacy 2018-2021) ; (d) **personal to-do vault Obsidian** (Huntr Le Wagon 🔴 + contentieux serrurier 🔴 + relire Turbo Frames/Streams 🟡 + décision SPA vs MPA NtF 🟡).

- **Vendredi 12 juin (clôture session — audit bullet, Claude Code CLI)** — Audit `bullet` exécuté en fin d'aprem après brainstorm "skip vs bullet vs pause" en milieu de routine fin de session (option B retenue par Chris). Branche `feat/audit-bullet` créée, **aucun fix appliqué, branche supprimée sans PR**.

  - **Méthode** : (1) Vérif setup bullet — gem installée + 5 channels actifs dans `development.rb` (`alert`, `bullet_logger`, `console`, `rails_logger`, `add_footer`) depuis PR #74 sem 23. (2) **Audit statique d'abord** (relecture controllers + vues critiques + partials `_collage`/`_tags`) : aucun N+1 identifié, tous les `index` ont l'eager loading correctement posé (`current_user.garments.includes(:category, photo_attachment: :blob)` + `current_user.outfits.includes(garments: [:category, { photo_attachment: :blob }])`). (3) **Audit dynamique** : `truncate -s 0 log/bullet.log` + `bin/dev` + navigation **9 flows critiques** dans le browser (home + `/garments` sans filtre + `/garments?q[color]=...` + `/garments?q[search]=...` + show garment + new garment + show outfit + new outfit + edits).

  - **Résultat** : ✅ 9 flows verts — aucun popup `alert()` JavaScript, aucune entrée bullet pour ces flows. ⚠️ **Seul warning persistant** sur **POST /outfits create** : `USE eager loading detected: Garment => [:photo_attachment] — Add to your query: .includes([:photo_attachment])`, call stack pointant `outfits_controller.rb:19 (if @outfit.save)`.

  - **Tentative fix → échec → revert** : ajout de `.includes(photo_attachment: :blob)` au `set_garments` before_action (suivant le pattern des autres flows N to F). Bullet a immédiatement répliqué avec **warning inverse** "AVOID eager loading detected" sur `_form` ligne 18 (`f.collection_select :garment_ids, @garments, :id, :name`) parce que le form ne référence **pas** `photo`. Diagnostic clair : eager loading inutile sur ce flow. Revert immédiat de la modif. État restauré, branche supprimée.

  - ⚠️ **Cause exacte du warning initial non identifiée** : (a) le model Outfit est minimaliste (validates name + garments presence, has_many through outfit_garments), (b) le concern Taggable n'accède jamais à photo, (c) le `_form` outfit ne référence pas photo non plus, (d) le Stimulus controller `select_controller.js` est vanilla Tom Select sans render customization avec photos. Hypothèses restantes : cas edge bullet sur `has_many through:` avec setter `garment_ids=` (1) ; middleware/callback caché qu'on n'a pas identifié (2). **À investiguer ~30-45 min samedi 13 juin ou sem 25** (priorité basse, action peu fréquente, pas d'impact UX visible). Entrée détaillée ajoutée à `to-do.md` Dettes techniques avec 3 hypothèses + plan d'investigation + résolutions possibles (fix legitime vs `Bullet.add_safelist` documenté).

  - **Méta erreurs Claude session capturées** : (a) ⭐ **Mea culpa diagnostic** — j'ai prescrit le fix `set_garments` includes à l'aveugle suivant la recommandation bullet, alors que le call stack pointait sur ligne 19 (`@outfit.save`) ce qui était suspect (un `save` qui déclenche un N+1 sur photo est incohérent quand rien dans le model/concern n'accède à photo). J'aurais dû flagger l'incohérence et investiguer AVANT de prescrire. Règle gravée : **devant un warning bullet, vérifier l'access pattern réel (grep + lecture du code) avant tout fix** ; ne pas faire confiance aveuglément à la recommandation `Add to your query` quand la trace est suspecte. (b) ⭐ **Imprécision dans la guidance pas-à-pas** — Chris m'a flaggé en milieu d'audit "tu n'es pas précis sur ce que je dois faire et depuis ou et pourquoi" → reset complet et reformulation avec **localisation explicite** (📂 où : terminal X, dossier Y, navigateur ?) + **commande exacte** + **raison du quoi**. Règle gravée : guidance multi-étapes = template strict "où + quoi + pourquoi + ce que tu vas voir". (c) ⭐ **HEREDOC encore raté** sur la création de la PR planning — Chris s'est encore retrouvé bloqué dans le prompt zsh `quote>` ("ça ne fonctionne jamais ton heredoc"). Workaround appliqué : commit avec `-m "1-line title"` + body PR via `gh pr create --body-file /tmp/...md`. Règle déjà mémorisée mais à RE-appliquer strictement. (d) ⭐ **Confusion `> log/bullet.log` vs `truncate -s 0 log/bullet.log`** — zsh refuse les redirections sans commande à gauche selon config Chris ("la première commande ne fonctionne déjà pas"), j'aurais dû donner `truncate` direct. Règle gravée : commandes shell = **toujours avec commande explicite à gauche** (`truncate`, `rm`, `: > file`), jamais de redirections nues.

  - **Pas de PR sur `feat/audit-bullet`** : aucun fix appliqué → branche supprimée localement (`git branch -D feat/audit-bullet`). Aucun changement à propager sur main côté code de prod.

  - **PR planning fin de session** : PR #107 mergée (`chore/planning-fri-jun-12-afternoon`) pour la routine end-of-day initiale (MAJ PHASE-1 + SUIVI + 2 nouvelles notes Obsidian [[rspec-request-specs-fundamentals]] + [[idor-sentinel-pattern-rspec]] + enrichissement [[minitest-to-rspec-migration-patterns]] section 11 + index Rails Testing) — **AVANT** l'audit bullet. Une **2e PR planning** `chore/end-of-day-fri-jun-12` est créée maintenant pour couvrir l'audit bullet (cette entrée SUIVI + MAJ to-do.md Obsidian Dettes techniques).

  - **Reco 2 vidéos fin de session** (GoRails Pro, Chris a abo actif — cf. memory `user-learning-resources`) : (1) [A Simpler Build with Broadcasts Refreshes](https://gorails.com/episodes/a-simpler-build-with-broadcasts-refreshes) (26 mars 2025, 17 min) — **forward-looking** : Turbo 8 morphing via `broadcasts_refreshes`, alternative au pattern `broadcast_append_to` vu vendredi matin Action Cable + mentionné en bonus dans [[turbo-frames-streams-and-format-negotiation]] (Odin chap. 36 lun 8 juin) ; application directe sem 25 sur Likes/Comments N to F (Likes vote → liste recompose entièrement vs append surgical, trade-off à mesurer en live) ; (2) [Handling Parameter Tampering Safely with Rails](https://gorails.com/episodes/handling-param-tampering-safely-with-rails) (2 mars 2026, 11 min) — **backward-looking** pile sur les 7 IDOR sentinels posés dans [PR #106](https://github.com/ChrisCroc/nine_to_fine/pull/106) aprem (param tampering = même surface d'attaque que IDOR cross-user). Dates < 2 ans pour les 2 → pas de flag "syntaxe à vérifier".

  - **Bilan vendredi 12 juin global** : Phase 1 bouclée techniquement le matin + chap. Action Cable lu + Tailwind Customization avancé v4 lu + exo PR #104 anti-double-submit livré + PR #106 migration 3 controllers RSpec + IDOR sentinels + cleanup `test/` + audit bullet exécuté (9 flows verts, 1 warning à investiguer). 113 examples RSpec verts dans la suite complète. ~12h cumulées de travail sur la journée (matin + avant-midi + aprem + clôture). **Session productive mais dense — repos vendredi soir mandatory** avant samedi 13 juin (rattrapage perso + investigation bullet + exos sandbox cumulés).

- **Samedi 13 juin (matin, Claude Code CLI)** — **Investigation warning bullet POST /outfits create bouclée + safelist documenté livré**. Dette technique reportée de vendredi 12 juin clôture session soldée en ~45 min de coaching pédago strict.

  - **Méthode appliquée (gravée pour les audits bullet futurs)** : (1) cartographie statique de l'access pattern AVANT tout fix (lecture parallèle des 5 fichiers du flow create — controller + form + Outfit + Garment + Taggable concern + OutfitGarment + version exacte `active_storage_validations` 3.0.5 dans Gemfile) ; (2) ajout `Bullet.stacktrace_includes = [ "app/" ]` dans `config.after_initialize` pour filtrer la stacktrace bullet aux frames `app/` (sans ça, dernière frame seule = insuffisant pour remonter à la cause) ; (3) reproduction propre des 2 chemins (success path + failure path) → confirmation que les 2 déclenchent le warning, donc origine = `@outfit.save` lui-même (pas le render `:new`) ; (4) ⭐ diagnostic SQL en `rails console` avec instructions atomiques (`.new` puis `.save` séparés pour isoler les queries de chaque étape).

  - **Cause identifiée — cascade `active_storage_validations` + `spoofing_protection: true`** : au save de l'Outfit, Rails matérialise les 3 OutfitGarments en mémoire (autosave par défaut sur `has_many :outfit_garments`). Chaque OutfitGarment a un `belongs_to :garment`. Quand Rails évalue les validations de ces OutfitGarments, la gem `active_storage_validations` hooke sur le model Garment et déclenche `validates :photo, content_type: { ..., spoofing_protection: true }` sur chacun des 3 garments associés. ⭐ **`spoofing_protection: true` télécharge le blob** pour vérifier le magic byte → 3 SELECT `active_storage_attachments` + 3 SELECT `active_storage_blobs` = N+1 documenté par bullet, queries observées entre `OutfitGarment Exists? × 3` et `Outfit Create` dans la transaction.

  - **Pourquoi le fix `.includes(:photo_attachment)` testé vendredi ne marche PAS** : la query qui charge les Garments pour le save est **interne à l'assignation `garment_ids=`** de Rails (`SELECT garments WHERE id IN (...)`), pas la query `set_garments` du before_action (qui sert au form `collection_select`). Aucune API idiomatique pour ajouter `.includes(:photo_attachment)` à l'assignation interne sans hack (override violent du setter `garment_ids=`).

  - **3 options présentées + recommandation A retenue** : (A — recommandé ✅) safelist bullet documenté précis sur `(type: :n_plus_one_query, class_name: "Garment", association: :photo_attachment)`, zéro impact prod (bullet dev-only), security préservée, intention documentée ; (B — rejeté ⚠️) désactiver `spoofing_protection: true`, fix à la source mais perte détection magic byte (attaquant peut renommer `.exe` en `.jpg`), trade-off sécurité non acceptable pour portfolio public ; (C — rejeté ❌) override `garment_ids=` setter avec includes, hack lourd, cargo cult anti-pattern (vu chap. 40 Section 5 [[solid-design-principles-and-anti-patterns]]).

  - **Livré (PR à venir)** : (a) `config/environments/development.rb` += `Bullet.stacktrace_includes = [ "app/" ]` (gardé permanent, utile investigations futures, peu coûteux) + `Bullet.add_safelist type: :n_plus_one_query, class_name: "Garment", association: :photo_attachment` + commentaire explicatif 7 lignes (cause + raison fix impossible + estimation overhead + trigger ré-évaluation) ; (b) validation par `truncate -s 0 log/bullet.log` + redémarrage `bin/dev` + 1 retest POST /outfits → 0 nouveau warning ; (c) note Obsidian enrichie : [[active-storage-validations-and-pitfalls]] § 7 **PIÈGE N°5 — N+1 cascade au save d'un parent `has_many :through:`** (8 sous-sections : warning + diagnostic console + mécanisme exact + pourquoi le fix .includes échoue + le fix safelist + options rejetées + quand creuser plus loin + méthode de diagnostic reproductible à graver + 1 ligne ajoutée au récap pièges + 2 liens internes ajoutés [[bullet-n+1-detection-and-eager-loading-fixes]] et [[solid-design-principles-and-anti-patterns]]) ; (d) `to-do.md` Dettes techniques : section "Investiguer warning Bullet" remplacée par section "✅ RÉSOLU" avec lien vers note Obsidian + résumé 2 lignes.

  - **Méta erreurs Claude session capturées (mea culpa)** : (a) ⭐ **Tentative initiale d'Edit `PHASES/phase-1.md` sans Read préalable** — outil Edit a refusé correctement (sécurité), réparé par Read + Edit séquentiels ; règle gravée : même si fichier dans `@import` auto donc déjà en contexte, l'outil Edit exige un Read explicite dans la session courante.

  - **Décisions de design tranchées (à ne pas re-proposer)** : (1) safelist en `config/environments/development.rb` plutôt que créer `config/initializers/bullet.rb` (cohérence : bullet est dev-only, sa config vit déjà dans development.rb, créer un initializer global ajouterait du bruit sur les autres envs) ; (2) `Bullet.stacktrace_includes = [ "app/" ]` gardé en permanent (utile pour les audits bullet futurs, no-op si pas de warnings, ajout 1 ligne).

  - **Reportés samedi 13 juin (train + après)** : (a) exo Stimulus debounce sandbox `~/code/sandbox/stimulus/` (spec complète déjà dans SUIVI sem 24 mardi + `to-do.md`, faisable 100% offline dans le train) ; (b) lecture relecture [[turbo-frames-streams-and-format-negotiation]] (note Obsidian locale, offline-safe) ; (c) personal (Huntr Le Wagon, contentieux serrurier) en fin de journée ; (d) 4e IDOR sentinel outfits `GET /outfits/:id/edit` reporté sem 25 (non bloquant, scoping garanti par `set_outfit` partagé).
