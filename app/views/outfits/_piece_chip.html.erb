<span class="inline-flex items-center gap-2 rounded-full bg-zinc-900 text-champagne px-3 py-1 text-sm">
  <%= garment.name %>
  <%= link_to "×", outfits_path(q: current.except(:garment_id)),
        class: "hover:text-white", aria: { label: "Remove piece filter" } %>
</span>
