<%# Replace the always-present container on th epage with th emodal shell.
    The modal holds #ai_suggestion (spinner) - the job broadcast replace that %>
<%= turbo_stream.replace "ai_suggestion_modal" do %>
  <%= render "suggestions/modal" %>
<% end %>
