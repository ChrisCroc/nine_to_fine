<% label = local_assigns.fetch(:label, attribute.to_s.humanize) %>
<% options = local_assigns.fetch(:options, {}) %>
<% errors = form.object.errors[attribute] %>

<div class="flex flex-col gap-1">
  <%= form.label attribute, label, class: "text-caption font-medium text-zinc-700" %>
  <%= form.send("#{type}_field", attribute, options.merge(class: "w-full rounded-lg border #{errors.any? ? "border-red-400": "border-zinc-300"} border-zinc-300 bg-white px-3 py-2 shadow-sm focus:outline-none focus:ring-zinc-900/20")) %>
  <% errors.each do |message| %>
    <p class="text-caption text-red-600"><%= message %></p>
  <% end %>
</div>
